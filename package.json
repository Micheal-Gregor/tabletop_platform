{
  "name": "tabletop-build",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "vitest run",
    "check:tiers": "node tools/check-tiers.mjs",
    "build": "tsc -b packages/engine packages/presentation packages/patterns",
    "ci": "npm run check:tiers && npm run build && npm run test"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
