# 3D ROADMAP — the object-by-object program (owner-ruled 2026-08-02, I-61)

> **THIS DOCUMENT SURVIVES SESSION COMPRESSION.** The owner's standing instruction:
> the conversation will compress multiple times before the 3-D forms perform for
> players in a polished way — so THE REPO carries the program. A fresh or compressed
> session resumes by reading, in order: `CLAUDE.md` → `INSTRUMENTS/drift-ledger.md`
> (the Interpretation Register, I-1..I-61+) → `INSTRUMENTS/RESOLUTION_RECORD.md` →
> THIS FILE → the per-object status table below. Then: `git pull --tags`, run
> `npm run ci && npm run gate:visual && npm run gate:target`, and continue at the
> first ⬜ row. Never re-decide a ✅ or a recorded ruling — supersede on the record.

## The ruling and the program

**ODG-p1 superseded (2026-08-02):** 3D (three.js, bundled devDependency) is the BUILD
TARGET. The SVG realization remains the CERTIFIED REFERENCE — V-9 and the SVG gates
keep running; nothing certified is abandoned.

**Three phases, strictly in order:**

- **PHASE A — BOTY in 3D, object by object** (with the owner, one object per gated
  increment): every v2 object from `governance/audits/V2-LIVE-OBJECT-INVENTORY.md`
  gets its 3-D form, driven by the SAME LayoutDefs, animated by the SAME flourish
  presets under HK-11, fed by the SAME projection (S-6 law: project() reads,
  emit()+submit writes — the 3D bench becomes PLAYABLE, not just visible).
- **PHASE B — DRAFT 3D parents:** with BOTY whole, review the parent vocabulary
  object-by-object for its 3-D representation; produce DRAFT parent revisions
  (data-first — I-50 law; mesh builders adapt to data, never the reverse). Owner
  rules on each draft.
- **PHASE C — the 10-game corpus** (`governance/game-design/`, DEFERRED not dropped):
  parent-robustness via refusal/suppression signals · core-vs-child promotion
  economics via measured repetition · the borrowable layout catalog. Runs against the
  post-Phase-B vocabulary.

## Standing law that carries into every 3D increment (pointers, not copies)

- Geometry source = LayoutDefs ONLY (I-60a); physical aspect/depth = realization
  freedom (I-48b); regions are law, refusals at the extension door (I-50).
- Theater: animations are FLOURISH data presets; HK-11 at completion — displayed ≡
  seeded, truth wins (proven in 3D at the spike, I-60b).
- Camera: focusPresets data mapped pure (I-60c); stateless toward the game.
- Unskinned until the owner opens the skin era (D-1); the v1 grammar waits in
  `packs/boty/skin-token-candidates.md`.
- Gates: DOM/scene-geometry assertions + computed pins; NEVER pixel hashes (I-57c);
  wait on STATE never clocks (headless rAF throttles, I-60f); kill-first-then-claim
  (I-58 corrective); every increment: instruments → build → gates → K7 (distinct
  reviewer, mutations); register wording grep-falsified (I-58, 8 strikes on record).
- Redaction: own-cards only on the viewing seat's surfaces; VG6 data-true pattern.
- Recorded survivors bind to REGISTER triggers (M-AR + slice-ordering precedents).
- FIDGETING is PURE THEATER (owner-ruled 2026-08-02): touch-cycles animate meshes only,
  never state, never the log; seat + table are selectable but NEVER fidgetable; the
  selected-card broadcast mutes + silences not-your-card (owner-gating is data law).
- DIFFUSED LIGHT ONLY (owner-ruled 2026-08-02): no source lights, no shadows, ever.

## PHASE A worklist — v2 objects → 3D (one ✅ per gated increment; owner playtests each)

| # | Object (v2 def) | 3D form | Animations (flourish presets) | Interactions (verbs via emit) | Status |
|---|---|---|---|---|---|
| A1 | The stage: table (boty:town-table) + camera + lighting-free ground | table plane flat, region quads, world scale | camera glides between presets · READ VIEW (I-63): table flat-overhead, boards face-on, fit-to-frame, pan-scroll | preset buttons · wheel dolly · click-to-focus · read/scene toggle · drag-to-pan in read | ✅ SEALED (A1+A1b; owner 2026-08-02: "the zoom to flat is GORGEOUS") |
| A1c | The stage completed: SIX seats two-sided + the bounded zoom continuum | 6 boards (far row −z, π-flipped), backs = shop graphic ONLY (no data), 6-up exhibit pack (DRAFT shop names pending owner) | ORBITAL glide around the table (never over/through) · wheel = bounded continuum (in → focus read, out → TABLE read, both walls hold) | wheel walls fire read transitions organically · far-seat click/preset from its own side | ✅ SEALED with A1d (owner played it 2026-08-02; wheel semantics re-ruled → the LADDER) |
| A1d | The zoom LADDER + table-region anchors (the owner's exact walk is law) | table regions (deck · discard · dice · windows · global-play · standings · log · art-banner) are ANCHORS — click one, zoom in to read THAT section | anchor-READ ↔ anchor-SCENE ↔ OVERVIEW ↔ TABLE-READ on one wheel axis; read zoom-in DISABLED; the anchor survives the whole walk | clicks re-anchor (boards · felt · table regions) · wheel walks the ladder | ✅ SEALED (owner 2026-08-02: "camera movements are good- approved") |
| A2 | Deck + draw | OWNER-RULED 2026-08-02: a stack of 60 ACTUAL cards (height tracks the projection count); the READING BOARD ('onion'): dark-translucent surround, card centered, pops up/closes | draw arc: top card lifts → flies → FLIP under HK-11 → lands in the reading board; deck fidget (top 5: loose → re-scatter → neat, pure theater); discard fidget (peek → spread last 5 → neat) | click deck (your turn) → draw verb; clicks cycle the fidget states; STANDING A2 OBLIGATIONS from I-62b: re-check VG8b/VG8d after a draw + re-run frozen-state hardcode kills M4a/M4b + consume rowHash/moveCount + I-63h depth-blind fit re-kill (first THICK read target) | ✅ SEALED (owner 2026-08-02: "the deck is visible and works well") · **PARTIAL SUPERSESSION ruled 2026-08-03 playtest-2 (RESOLUTION_RECORD + I-82):** draw = FLIP then display in the onion, NO fly animation · deck = one of EVERY card (content-tier, I-82a) · discard = a REAL face-up ordered pile, explorable, real 3D movement, grab/toss → 2s return-to-order → increments Q-1/Q-2/Q-6 |
| A3 | Fortune/character card (boty:fortune-card) | front/back card mesh (spike-proven) | flip · zoom-to-camera (modal-as-card in 3D, I-51a) | click to zoom/dismiss | 🔄 A3 (I-69) + A3b opaque-fix (I-70) · both K7 PASS · awaiting owner re-playtest |
| A4 | Round preamble + round card (boty:round-preamble/round-card) + THE DIE | sequenced cards through the reading board; a real 3D die on the table | OWNER-RULED 2026-08-02: the roll is PREDETERMINED (engine seeded RNG) then ANIMATED — the die is 'picked up and tossed', stopping with the determined face upright (HK-11: displayed ≡ seeded); fidget: not your turn → touch = lazy dead roll on a random side, NO game effect | click advances the queue (I-55a sequence) · touch the die to fidget | 🔄 I-73 superseded by K-E free-tumble (I-81) · **playtest-2 2026-08-03: CHANGES RULED** — die WAY TOO BIG (→ ~half, owner seals) · start position FIXED at the dice-region home, not random (I-82c) · the physics-dice source ARRIVED (`Projects/dice shaker/`, RAPIER) → P-1 now, R-1 physics program later |
| A5 | Shop board (boty:shop-board) | standing board at the seat edge | none (static frame) | click regions route like the SVG bench | ⬜ spike partial (static form exists) |
| A6 | Tradesperson (boty:tradesperson-card) | mini standing card in the crew rack | select-lift · assign-hop to portion slot · work-bounce | select → assign → work (the v4 loop, in 3D) | 🔄 BUILT (I-136) — rides the owner battery + playtest |
| A7 | Cards in play (boty:job-card + tradespeople + equipment + session) | **PLACEMENT RULED 2026-08-03 (playtest-2):** rows IN FRONT of the seat board — TRADESPEOPLE row (attached equipment underneath-but-visible) + SESSION-card row; a GLOBAL CARD ROW on the table for global cards; ledger LEFT edge of the seat area, hand face-down RIGHT edge | lay-down on route-in · equipment drag attach/detach · grabbed/thrown cards RESET to anchor | drag equipment between tradespeople (verbs = engine-lane flag I-82f) | ⬜ → increments Q-3 (seat rows) / Q-5 (global row) |
| A8 | Windows/prompts + the SELECTED-CARD BROADCAST | amber prompt card in the reading board | pop-in; OWNER-RULED 2026-08-02 BROADCAST LAW: a selected card presents in the reading board for EVERY player — not-your-card = top indicator + slightly MUTED color + NO sound effect (owner-gating is data-law now; sound assets are skin-era) | option click → decide verb | ⬜ |
| A9 | Hand fan (redaction-honest, I-59d) | fanned card meshes at the viewing seat only — OWNER-RULED 2026-08-02: FACE DOWN when not anchored, RIGHT of the ledger | FIDGET (owner-ruled): touch → face-down cards shift a little → touch → spread more → touch → neat stack → cycle; anchored+zoom → flips up + spreads readable; zoom again → READ mode with a bottom SCROLL BAR (left/right) + dark surround | touch to fidget · anchor to reveal · ladder to read | ⬜ spike partial |
| A10 | Books panel (boty:books) | OWNER-RULED 2026-08-02: a physical LEDGER sitting FLAT in front of the player | FIDGET: click to anchor + shuffle (slight LIMITED rotation); zoom → flips up and OPENS; zoom again → read mode (P&L + Balance Sheet) | tab = ui-state; fills from projection (I-56d); the ladder carries it | ✅ OWNER-SEALED 2026-08-03 ("approved! … Animation looks very good. BTW, no botches.") as P-2/P-2b/P-2c (I-84/85/86): the folded-sheet FOLDER (left-hinge flap, shorter than the back, sheets' right edges peeking) + the THREE-OBJECTS law (persistent sheet objects, folder↔scene reparenting, UUID-asserted) + ANCHOR-PER-REPORT (click a report → its reading view; the K-C pan-switch retired). k7-pass tag rides the owner-side full battery |
| A11 | Rivals + gallery | carousel of standing rival-summary boards · card grid | page-turn | prev/next/close · filter chips (CARD_KINDS) | ⬜ |
| A12 | Standings + log + chrome | flat panels on the table + HTML chrome (I-51d) | active-row pulse (structural) | row click → camera focus | ⬜ |
| A13 | Persistence + drills parity | — | — | autosave/resume/export; GD + VG gates extended to the 3D bench | ⬜ REQUIRED for Phase A completion |
| A14 | Dice + spinner + tile-lay forms | placeholder meshes (no verbs in slice — nothing false) | throw/spin/lay presets AUTHORED as data, exercised in exhibit mode | — until mechanics increments | ⬜ |
| A15 | The GAME BOX (owner-ruled 2026-08-02) | an open board-game box, lid off, to the RIGHT of the board — as if unpacked | recorded FUTURE upgrade: full unboxing-as-game-start animation (pieces inside → unpacked → set up in the overview); "the box is fine for the moment" | selectable (ladder) · not fidgetable yet | 🔄 I-75 superseded by K-D (I-80) · playtest-2 2026-08-03: NO verdict (unmentioned) — still awaiting; the ~667u lid-distance flag stands (I-82g) |
| STYLE | The MANIFESTED backdrop (candidate brand style, owner 2026-08-02) | immediate background WHITE → pastel haze + light dark-ink effect → fading to another off-white ("the play area has manifested into existence") | none — it is a backdrop, zero lighting cost | — | ⬜ prototype; owner rules "if it looks good" → game-template default |
| A16 | Tradesperson deck + equipment deck (owner-ruled 2026-08-03) | TWO more card stacks on the table | bottom-return: fired tradesperson → BOTTOM of the tradesperson deck; sold/stolen equipment → BOTTOM of the equipment deck; NO discard pile for either | mechanics verbs = engine-lane flag (I-82f); bench stages the presentation | 🔄 HIRE + BUY REAL (I-137) — pools in state, count-true stacks, click-to-hire/buy through the doors; REMAINING: bottom-return theater (fired/sold) + the economics numbers (owner's open question) + bbb/networking mechanics |
| A17 | PHYSICS-FIDGET LAW (owner-ruled 2026-08-03) | RAPIER rigid-body theater (source: `Projects/dice shaker/dice_shaker_2.html`) for every object EXCEPT the immutable seat + table | flick · throw · drag/drop · limited physics; touch (phone/tablet fingers); die via shaker approach | PURE THEATER — HK-11 truth wins, physics never decides state (I-82e); stages toward AR/VR = FUTURE version, not this project | 🔄 R-1a BUILT (I-109, 1add656) — die first; R-1b cards / R-1c touch follow |
| LAW | DIFFUSED LIGHT ONLY (owner-ruled 2026-08-02) | no source lights, no shadows — unlit/diffuse materials everywhere (performance + nothing between the player and the game); the bench already conforms; every future increment must | — | — | ✅ standing law |

**Definition of Phase-A done:** the full BOTY loop of the SVG bench v7 playable in 3D
(A1–A13), every increment K7-passed, the 3D gates green, and the owner has playtested
each object ("really perform for the players" is the owner's acceptance bar).

## Status ledger (append per increment — id · commit · K7 tag · owner playtest verdict)

| Increment | Commit | K7 | Owner verdict |
|---|---|---|---|
| Spike (feasibility exhibit) | 6294d06 | k7-pass-3d-adoption | ruled: adopt (2026-08-02) |
| A1 the stage (game3d shell) | 2a699ad | K7 PASS (tag rides the playtest) | 2026-08-02: "It feels good to move around the board" — movement ACCEPTED; ruled a read view required (flat overhead for the table, 90° face-on for seats, scroll when content outruns the frame) → A1b |
| A1b read view (I-63) | 5688a73 + closure 0cad6ab | RETURN (D1–D5) → closed same-day → PASS · k7-pass-a1b-read | 2026-08-02: ACCEPTED — "the zoom to flat is GORGEOUS"; A1+A1b SEALED |
| A1c zoom continuum + six seats (I-64/I-65) | f525ad4 + closures 74fb990/b008050 | RETURN (D1–D6) → closed → RETURN (D4′ false record) → superseded + falsified → PASS · k7-pass-a1c-stage | 2026-08-02: played; wheel semantics re-ruled (read zoom-in off · read-out → anchor scene · the ladder) → A1d |
| A1d the zoom ladder + region anchors (I-66) | c77e4c9 + closure 34da5fa | RETURN (D1–D5) → closed same-day → PASS · k7-pass-a1d-ladder | 2026-08-02: "camera movements are good- approved" — SEALED |
| A2 deck + draw + the reading board (I-67) | 8c5da32 + closures aaaf52d/a4740f7 + A2b b1bda05 | PASS · k7-pass-a2-deck · A2b (thin-box pile, height-true) PASS · k7-pass-a2b-stack | 2026-08-02: A2b re-playtested — "the deck is visible and works well" — A2+A2b SEALED |
| A3 fortune card in 3D (I-69) | b839d26 · tag `k7-pass-a3-fortune` | PASS (D1 minor closed — I-58 strike 11) · distinct K7 subagent reproduced ci 279 · gate:visual 36/36 · gate:target 21/21·11/11 and reconstructed all 5 mutation kills live | 2026-08-02: playtested — the reading card read TRANSPARENT (the 55% veil over an opaque card) → fixed in A3b |
| A3b reading card opaque (I-70) | f938500 · tag `k7-pass-a3b-opaque` | PASS (3 minors closed: veil inert-tag disclosed · strike-11 comment aligned in code · depthTest boundary note) · distinct K7-A3b reproduced ci 279 · gate:visual 37/37 · gate:target 21/21·11/11 and all 3 VG8l kills live | 2026-08-03: owner re-playtest of the fix PENDING |
| game3d.ts module split — PURE REFACTOR (I-71) | a3aa802 · tag `k7-pass-split` | distinct K7 verifies behavior-identical (pins BYTE-UNCHANGED · gate:visual 37/37 · ci 279 · gate:target 21/21·11/11 · tsc clean) + pure-move | N/A — pure refactor, ZERO visual change (no owner playtest needed) |
| gate-port enabler (I-72) | 30336a0 | verified behavior-identical on non-default ports | N/A — parallel-program infra |
| A4 die + round cards (I-73) | 6eee282 · tag `k7-pass-a4-die` | distinct K7 PASS (worst 8) · iso gate 42/42 · integrated 50/50 | 2026-08-03: owner playtest PENDING (die roll + round-card sequence) |
| A10 books ledger (I-74) | e17053a · tag `k7-pass-a10-ledger` | distinct K7 PASS (worst 7; callout-fill drift + zero-state balance trigger recorded, I-76) · integrated 50/50 | 2026-08-03: owner playtest PENDING (flip-open ledger; confirm the callout fill) |
| A15 game box (I-75) | bcdd9c4 · tag `k7-pass-a15-box` | distinct K7 PASS (worst 8) · integrated 50/50 | 2026-08-03: owner playtest PENDING |
| BATCH-1 INTEGRATION (I-76) | 29e2b82 + 50e0715 (serial union merges) | gate:visual **50/50** · ci 279 · gate:target 21/21·11/11 · pins byte-unchanged (no re-pin needed) | first parallel-worktree batch — 3 built concurrently, distinct K7 each |
| K-A component harness (I-77) | 0b37bcd · k7-pass-ka-harness | PASS (worst 8) | N/A — pure refactor, behavior-identical |
| K-B gate split + size gate (I-78) | b16b61f · k7-pass-kb-gate | PASS (worst 8) | N/A — infra; full battery 50/50 reproduced |
| K-C books two-page (I-79) | 9297bff · k7-pass-kc-books | PASS (worst 7) · integrated 52/52 | 2026-08-03 playtest-2: **CHANGES RULED → P-2** (folder + rise-up + anchor-per-report; balance sheet unreachable via the pan-switch) |
| K-D box (I-80) | b27b823 · k7-pass-kd-box | PASS (worst 8) · integrated 54/54 | 2026-08-03 playtest-2: no verdict (unmentioned); lid-distance flag stands |
| K-E dice free-tumble (I-81) | 249dc4b · k7-pass-ke-dice | PASS (worst 8) · integrated 56/56 | 2026-08-03 playtest-2: **CHANGES RULED → P-1/R-1** (too big · fixed home · RAPIER physics program) |
| P-1 dice size/home (I-83) | bfa5612 | K7-P round 1 RETURN → D1–D3 closed (I-87); battery owner-side | 2026-08-03: size sealed ("size now looks correct") |
| P-2/2b/2c ledger folder + three-objects (I-84/85/86) | 677d1c5 | K7-P closed same-session | 2026-08-03: **SEALED** ("approved! … no botches") |
| Q-1 full deck (I-88) | 4b48088 | **K7-Q RETURN (I-98) — B1 BLOCKING: the deck is UNDRAWABLE past card 3 (wired catalog is BOTY_PACK, deck is PACK6; draw #4 throws GX-10 — reproduced by execution)** | 2026-08-03: rode the T-1 "Good." |
| T-1 v2 table arrangement (I-89) | a74c9f2 | **K7-Q RETURN (I-98) — M2: `v2-table-arrangement` is a def-data tautology; the whole arrangement reverts and the battery stays green** | 2026-08-03: **SEALED** ("Good.") |
| Q-2/2b flick-to-flip draw (I-90/91; contract v2 grab protocol) | 6d1ea10 | **K7-Q RETURN (I-98) — B2 BLOCKING: the HK-11 forced-mismatch drill is dead (`VG8j:303` drives it with a click Q-2b made incapable of drawing). The REVEAL-ORDER LAW passed and is stronger than claimed** | 2026-08-03: owner-confirmed BEHAVIOR ("good"); **gate RETURNED — seal narrowed per I-98** |
| Q-2c slot routing derived view (I-92) | 2987363 | **K7-Q RETURN (I-98) — M1: `slot-partition-law` pins a construction-true identity, never the render; `.slice(0,6)` renders cards 7..N zero times. Derived-never-stored + the TOTAL fallback both PASSED** | 2026-08-03: owner-confirmed BEHAVIOR ("good"); **gate RETURNED — seal narrowed per I-98** |
| Q-3 seat play area + crew projection (I-93) | 01e0ebe | **K7-Q RETURN (I-98) — M7: `assets-count-true` is vacuous (0===0); M8: the reset glide is indistinguishable from a teleport. Registry-order disjointness VERIFIED TRUE; ledger left-edge oracle has real teeth** | 2026-08-03: rode the "go" |
| Q-6/6b live discard + gesture pool (I-94/95) | c0cb235 | **K7-Q RETURN (I-98) — B3 BLOCKING: `discard-multi-card` draws inside the window `draw-theater-hk11` asserts invariant; M5: the fidget counter advances when the tween is REFUSED → the pile SNAPS. I-95's pool bookkeeping PASSED on every traced path** | 2026-08-03: owner-confirmed BEHAVIOR ("good"; "other animations and the pop-up are perfect"); **gate RETURNED — seal narrowed per I-98** |
| G-1 gate repairs (I-101; the K7-Q closure program, first of G-1→S-1→W-1) | ba12531 | closes B2 (real-drag drill, repositioned as the second draw) · B3 (consumed-close window re-scoped) · M10 (arithmetic sub-threshold drags) · the Q3-D2 counter red (normalize + tween waits, 1e-6/I-80 fold) · M1/M2/M6/M7/M8 teeth (rendered oracles + motion traces) · run.mjs friendly failure | **owner battery GREEN 78/78 (I-105) · K7-S PASS (I-106) · tag `k7-pass-g1`** |
| G-1b VG8a count law (I-102) | eedbd08 | expectedFromDefs learns P-2c's two persistent sheets (+2×BOOKS_PANEL); **owner-confirmed 94≡94 (I-105)** | records the DOUBLE refutation of I-96's ALL-GREEN (last true full green was 56/56 at K-E) |
| S-1 contract v3 spine closures (I-103) | f18fc67 | per-pointer claims + capture/cancel · finally-release with abort-on-throw · wheel gated on the claim · onGrabAbort protocol obligation + resetDraw (M4) · M5 counter fix · table-oracles.ts extraction · VG8q NEW (5 kill-first) | **owner battery GREEN: VG8q 5/5, full 78/78 (I-104/I-105) · K7-S PASS (I-106) · tag `k7-pass-s1` · R-1's ENTRY GATE MET** — two carries ride R-1's first row (MAJOR-1 onGrabMove throw path + VG8q #6 · MAJOR-2 the refused-fidget check) |
| S-1c the K7-S carries (I-107; owner-ruled micro-increment before R-1a) | bc79bda | MAJOR-1 closed (onGrabMove mirrors the release path + `grab-move-throw-aborts`) · MINOR-5 save/restore · **the M5 kill at last** (`fidget-refused-holds-counter` — the mutant that passed all 78 now fails) · backdrop.ts extraction | **owner battery GREEN 80/80 (I-108) — SEALED**; R-1a (die-only RAPIER, owner-ruled GO) is the open row |
| R-1a die-only RAPIER (I-109; the A17 physics program opens) | 1add656 | the @dimforge/rapier3d-compat@0.19.3 excursion · RECORD-AND-REPLAY with the reconcile offset (PHYSICS NEVER DECIDES — proven 8/8 in node pre-build; spread 600×472 over the exact first-6 seeded rolls) · the shaker port (live-table rails, structural containment) · grab-flick via contract v3 with the click fall-through protecting every VG8m drive · VG8r NEW (5 kill-first incl. reconcile-not-decide, THE R-1 law check) | sandbox-verified (tsc/size 30·die.ts 299/tiers/gate-surface 86/vitest 279/esbuild); **owner: `npm install` ONCE, then battery — prediction VG8r 5/5 · VG8m 10/10 holds · 85/85 total**; distinct K7-R and the owner playtest ride |
| R-1a2 playtest closures (I-110; the owner's three verbatim rulings) | ac905c5 | the 3.2 m/s FLICK CAP + the ESCAPE GUARD (off-board always returns; proven headless at 8/15/30 m/s) · the TOP-CARD DRAG-FOLLOW (flip where released; flip-not-fly superseded on the record) · THE STACK PROOF (tap → top-5 nudge — 'an actual stack, not the image of one') | sandbox-verified; **owner battery PENDING — prediction VG8j 18/18 · VG8r 6/6 · 88/88 total**; the owner's FEEL seal + distinct K7-R (covering I-109+I-110) ride |
| R-1a3 third-tap re-center (I-111) | 88c2588 | every 3rd deck tap re-centers the top five ('so the pile doesn't get too loose'); deck-tap-nudge extends to the 3-tap cycle | rides R-1a2's pending battery — prediction still 88/88 |
| R-1a4 the grabbed card IS the top card (I-112; owner-caught — the P-2c defect class live in the draw theater) | 8506745 | the real top BOX is scene-attached (same size, same edges), re-attached on the weak settle, honest two-sided flip (the underside slot takes the face); identity UUID-asserted in draw-drag-follows | rides the pending battery — prediction 88/88; the R-1a family (I-109…I-112) then takes ONE feel pass + the distinct K7-R |
| R-1a5 the directional flip (I-113) | 0db0c62 | flicked right → clockwise, left → counter-clockwise; speed/lift velocity-scaled within tight clamps, completion GUARANTEED; + the I-112 latent end-pose fix (the box showed its BACK at reading/routing) | **owner battery GREEN 88/88 at abf4335 (I-114)** · **K7-R: RETURN (I-114)** — the reconcile law PROVEN by execution, but 6 MAJORs (two owner-facing: the TOP card never nudges · a human/touch die-click may not roll) + 6 record defects; `k7-pass-r1a` does NOT ride; R-1a6 = the ordered closure list in `governance/audits/K7_AUDIT_REPORT-R.md` |
| R-1a6 the K7-R closures (I-115) | dcbddbd | ALL SIX MAJORS closed with their kills (the tap nudges the REAL top · pointer-frame travel + the off-centre human-click leg · both flip directions asserted, flipDir inits 0 · the faceUpAtEnd leg EXISTS · the deterministic cap kill · readiness before the LCG + honest status) + draw-route.ts extraction + the ARCHITECTURE dependency note | **owner: feel SEALED (I-116) · the closure battery 88/89 at 333f4a9 · VG8r 7/7 at 8e44c9c (I-118, provenance per I-119/RB-3)** |
| R-1a6b off-centre drive geometry (I-116) | 333f4a9 | drive-only; the diagnosis was WRONG (superseded at I-117/I-119-RB-1) — the check was right | gate file only |
| R-1a6c THE ONE-FRAME FIX (I-117) | 8e44c9c | the M4 class across two PLANES — the seed and every move now share one table-plane raycast (34.7u of cross-plane error removed, measured at K7-R2) | VG8r 7/7 (I-118) |
| **K7-R2: PASS — `k7-pass-r1a` RIDES; THE R-1a FAMILY (I-109…I-118) IS SEALED** (I-119) | tag at 8e44c9c | all six closures verified genuine by execution; 7 minors + owner drills carried (double-tap the deck fast · drag-STOP-release the die · touch-tap the die) | `governance/audits/K7_AUDIT_REPORT-R2.md` |
| W-1 wire PACK6 (I-121; K7-Q B1's closure, owner-ruled with R-1b before ONE combined test) | 0184a43 | `wireBoty(registry, pack = BOTY_PACK)` + game3d wires BOTY_PACK6 — the WIRED catalog ≡ the HOSTED pack, all 36 cards drawable; game.ts FROZEN untouched by construction; the 36-draw empirical probe pinned as a committed regression (vitest 279→283) | sandbox-verified (tsc·size·tiers·gate-surface 93·vitest 283·esbuild); rides the combined W-1+R-1b owner test |
| R-1b discard toss physics (I-122; the physics program's second family) | 9e2c697 | `simulateSlide` — a BURST sim replayed by the pool's new 'sliding' phase (record-and-replay: no live session, a die drag never contends) · card cuboid from the LIVE mesh bbox, rotations LOCKED to yaw (the planar law) · plane-window velocity only (I-117) · the 3.2 cap + escape guard reused · low-friction card (Min combine — real travel under 3× gravity, verified headless: 26u@0.4 · 153u@1.0 · 458u capped, tilt 0.0000) · falls back to loose-where-dropped when cold/degenerate (registered) · `discardSlideTrace {steps,dist}` + VG8j `discard-toss-slides` (kill: remove the sim call → NULL trace fails by name) | sandbox-verified (tsc·sizes 279/246·tiers·gate-surface 94·vitest 283·esbuild); rides the combined W-1+R-1b owner test — prediction VG8j 20/20 · full ~90 |
| R-1b2 the discard time-gate (I-125; K7-T B-1's closure, owner-ruled option 2) | pending | STALE_MS 150 in beginSlide — a stopped hand drops the card dead (the die's stale-window quick roll stays, ALSO owner-ruled) · the I-124 dead guard LEAVES · VG8j `discard-stop-release-drops-dead` (kill: remove the gate → the stale window launches a slide → trace rewritten → fails by name) · K7-T mutation (a) EXECUTED by the builder: rewired to BOTY_PACK → draw #4 GX-10 `vnt-project` — the W-1 regression's teeth proven live | sandbox-verified (tsc·size 284·tiers·gate-surface 94·vitest 283·esbuild); prediction VG8j 21/21 · full 91/91; K7-T re-verification + the owner's stop-release feel check ride |
| R-1c the flick floor (I-126; the owner's shake-legitimacy ruling) | pending | FLICK_FLOOR 1.6 m/s in dragEnd — a weak-but-real flick scales UP to the floor (direction preserved, spin floors with the components; responsive floor→3.2 cap) · die-only (the seeded roll already launches 2.0–3.4; the discard slide untouched — a card is not shaken) · headless-proven: floored weak flick 3.17 revs vs 0.30 bare (a 10× tumble gap), all contained · VG8r `die-weak-flick-floored` (kill: remove the floor → eff ≡ raw < 1.59 → deterministic fail) | sandbox-verified (tsc·size 251·tiers·gate-surface 94·vitest 283·esbuild); prediction VG8r 8/8 · VG8j 21/21 · full 92/92; the owner's weak-flick feel check + K7-T re-verification ride |
| A5 shop-board data parity (I-128; the first open roadmap row, opened under I-127) | pending | the boards stamp the LIVE sheet through the projection: building-tier + crew count · jobs-list real `crew ⇒ venture` (public per I-59b) · AR/AP · active-aware actions · art-banner · presentation EXCURSION NAMED (projector.ts crew type widens by `assignedTo?` — a truth-declaration; the field already flowed) · `shopFillsTrue` render-walk oracle + VG8g2 `shop-board-data-true` (text-equal — teeth at zero crew; kill: remove the fills → fails by name) · regionCount INVARIANT by construction (fills swap quads for panels, never add regions) | sandbox-verified (pkg+bench tsc·size 145·tiers·gate-surface 95·vitest 283·esbuild); rides the batch battery |
| A8 window prompts (I-129; URGENT post-W-1 — the HK-5 soft-lock) | pending | new `windows-prompt.ts`: open windows render as amber prompt cards at the windows region (SVG anatomy); decider law — yours = clickable options → `decide` through the doors (window:resolve, a real verb) → rebuild; not-yours = MUTED + indicator + 'awaiting…' · engine flow PROVEN headless (draw3=crossroads → w1 open → end-turn REFUSED GX-8/R-6 → decide ok → end-turn ok, active pete) · VG8j `window-prompt-decides` (draw #3 + option click, moves+2; downstream ownDiscard pins moved 2/new-van → 3/crossroads ON THE RECORD) · deferrals registered: the muted-branch drive (turn-cycling drill) + broadcast-to-others (transport/F7) | sandbox-verified (tsc·size 122·tiers·gate-surface 97·vitest 283·esbuild); rides the batch battery |
| L-1/L-2/L-3 the table re-row + onion prompts + THE MEDAL (I-130; the owner's layout program, first tranche) | pending | TOWN_V2 re-rowed (packs excursion named): dice FAR RIGHT of the deck row · trades+equipment row B · BBB+networking row C (two NEW staged decks) · `windows` SUPPRESSED altogether · `medal` region bottom-right · prompts RE-ANCHORED to the onion layer (camera-parented, front-and-center — the owner's stated preference; transient, zero table footprint) · the 3D BOTY medal placeholder (disc+ribbon+bow form, flat diffuse, D-1 holds; skin ref saved at governance/assets/) · v2-table-arrangement RE-PINNED to the new rows + windows-GONE + medal legs · VG8h2 `medal-in-region` NEW | sandbox-verified (all tsc·sizes 124/106/271·tiers·gate-surface 98·vitest 283·esbuild); L-4 seat-front rows awaits the owner's 4-vs-5-row word · L-5 corner seats + P-3 shuffle follow |
| P-3 per-seat shuffled decks (I-131; 'all decks and the discard pile are all seat/player unique') | pending | every seat's deck = the FULL 36-card set; seeded Fisher-Yates per (sessionSeed, seatId) at genesis — deterministic per session, unique per seat (moe's maple-hollow order opens rtm-referral · iwn-settle · … · crossroads@5; pete differs) · ALL 36 shuffled draws PROVEN ok headless · EVERY card pin in VG8j now DERIVES from `deckOrder(seat)` (the vector discipline) · the decide leg is ORDER-AGNOSTIC (draws until a window opens; for this seed the drill's 2nd draw opens iwn-settle) · pete's-deck pin 2→36 | rides the batch battery |
| L-4 seat-front dynamic rows + hand (I-131) | pending | the PURE `planSeatRows` planner (trades front-top · 5+ splits two rows GROUPED · pairs widen 1.6× and never leave trades rows · equipment ONE compressing row (the owner's amendment — 4-row budget holds) · locals bottom, overlap past fit) — SIX vitest laws (289 total) · seat-play renders the plan · session/local cards MOVED from the table to the viewer's bottom row (Q-2c placement superseded; family tags travel — the partition oracle keeps counting) · the HAND stages face-down BELOW THE BOOKS (SVG count law; A9's fan rides later) · VG8g4 `seat-rows-law` (+ hand-below-books) · NOTE: rows step along z — the diagonal alignment for corner seats is a registered feel refinement | rides the batch battery |
| L-5 corner seats 45° (I-131) | pending | corners (0,2,3,5) at (±470,±470) yawed ±45/±135° facing their players; MIDS (1,4) HOLD (the seat-1/seat-4 read-pose pins valid by construction; the read camera derives face-on from the board's own normal — corners read correctly for free) · the shortest-arc wall (190°) holds (corner arcs are shorter) · VG8g3 `corner-seats-45` (kill: revert → 0/180 yaws fail by name) | rides the batch battery |
| L-5b the seat frame law (I-132; the owner's screenshot rulings) | pending | mids PULLED BACK to the corner near-edge line (derived: 470 + 130·sin45° ≈ 562) · seat-play rows, the LEDGER, and the HAND lay out in the BOARD'S OWN FRAME (lateral/normal from its quaternion — corner furniture runs at 45°, 'full visibility of table') · flat footprints and standing cards yaw WITH the board · the ledger-left + in-front + below-books oracles RE-DERIVED frame-relative (the world-axis forms were placement-specific; the laws are frame laws) · the box gap 50→170 ('a little further away') · VG8g3 gains the mid-pullback leg | sandbox-verified (tsc·sizes 273/188/165·tiers·gate-surface 102·vitest 289·esbuild) |
| L-5c the yaw truth (I-133; the owner's screenshot catches) | pending | the FOLDER FLAT fix (the world-yaw PREMULTIPLIES the built pose — a .rotation.y write had composited the Euler and stood the folder on edge) · seat presets APPROACH ALONG THE SEAT'S YAW (`SEAT_YAWS` in stage.ts becomes the single yaw source for seats.ts + camera.ts + the VG8c law, which re-derives its want from the same data — mids byte-identical by trigonometry) · box position SEALED by the owner | sandbox-verified (tsc·tiers·gate-surface 103·vitest 289·esbuild) |
| **K7-U: RETURN → closures landed** (I-135; `governance/audits/K7_AUDIT_REPORT-U.md`) | this commit | ALL TEN increments' behavior code CONFORMANT AS BUILT; the blockers were instrument-layer: B-1 (a stale hand pin made `state-change-recheck` unsatisfiable — the battery would have redded by name; both deck counts now DERIVE from `deckOrder`) · M-1 (the falsified I-122(a) text lived on as a comment — rewritten) · M-2 (the decide ACTOR seam — registered latent, resolves at the turn-cycling drill) · 9 new minors + 6 K7-T carries on the report | the OWNER BATTERY + PUSH now head the queue; the re-verifying K7 reconstructs B-1 live |
| A6 the v4 working loop (I-136; owner: 'A6 next') | pending | select → assign → work, the certified SVG semantics through the projection + closed verbs: `crew-loop.ts` state machine (select-LIFT pure theater · assign-HOP to the slot then the verb at arrival · work-BOUNCE post-state) · `components/ventures.ts` (projection ventures as job cards + portion slots in global-play; want ≡ rendered) · the SPAWN chrome door (`spawn-venture {botyJob()}` — the SVG's own exhibit button) · seat-play routes sub-8u crew clicks to the loop (VG8p's >40u reset drive untouched by predicate) · OWNERSHIP: viewer's own crew only · busy cards wear '⚒ working' · engine flow PROVEN headless (spawn ok → assignedTo {J1,0} → work ok, binding clears) · VG8s NEW (4 kill-first: spawn-renders · select-lifts · assign-hops-and-binds · work-bounces) | sandbox-verified (tsc·sizes 112/108/295·tiers·gate-surface 107·vitest 289·esbuild); rides the owner battery — prediction grows to ~101 (VG8s 4) |
| A16-real: HIRE + BUY (I-137; owner: 'no game until that's set up' — the I-82f flag discharged by his word) | pending | ENGINE-LANE: `pools {tradespeople, equipment}` state (PoolCards carry data INLINE — engine content-agnostic; 8+8 DRAFT cards, one seeded shuffle per SHARED pool) · `crew:hire` / `outfit:buy` intents (S-3 module-native, the venture:spawn precedent; EFX/HookPoints/VerbSets UNTOUCHED — admission recorded at I-137) · empty pool → GX-30 BY NAME · cost>0 levies via EffectEngine (R-24), cost 0 posts nothing (GBC-63) — DRAFT COSTS 0, the economics numbers are THE OWNER'S OPEN QUESTION · projection pools = counts only (v1 states project unchanged) · emitter 'hire'/'buy-equipment' · the tp/eq stacks BIND to counts; click YOUR turn → the verb through the doors · VG8j arrangement derives from poolCounts · VG8s grows to 6 (pile-click-hires · pile-click-buys — the I-93 assets nonzero trigger FIRES) · 5 vitest pool laws (294 total) · instruments FIRST (object-model + GBC-63 base cases appended before the feature) | sandbox-verified (all tsc·tiers·gate-surface 108·vitest 294·esbuild); prediction ~103; the hire-flight flourish = the registered polish row |
| O-1 + O-4 (I-138, the objects program's first tranche) | pending | **O-1** the books OPEN-POSE follows the seat frame (`setSpreadYaw` — the risen P&L/Balance spread along the seat's lateral, FACE the player, pushed off the board; the overlap dies) · **O-4** TEST CARDS IN PLAY at genesis: `gbl-union` ('labour unionizes') presents in the GLOBAL row + `svc-insurance` ('buy insurance') in each player's LOCAL row — both start in every seat's genesis DISCARD (the partition presents them; family data already existed) and LEAVE the draw (`genesisDrawFor` = ONE derivation feeding genesis AND every gate pin; one-of-every-card holds; decks 36→34) · the deck-count/partition pins re-derive (sums 3 = 1 global + 1 session + the first draw's pile) · the hand starts at 2 (belowBooks non-vacuous at last) | sandbox-verified (all tsc·tiers·gate-surface 108·vitest 294·esbuild); O-3 (pool deck-logic + bottom-return + BBB/networking pools) → O-2 (per-seat full sets) → O-5 (thickness) → O-6 (board wiring) QUEUED per I-138 |
| THE COUNTERTOP + O-3 (I-139) | pending | the owner's alignment concept ACCEPTED + strengthened: `surface.ts` = ONE LAW (the visible slab + COUNTER_Y/restOn; two strata — counter and table-top; consumers migrate at O-5) · O-3: BBB + NETWORKING are REAL CARD DECKS — 5+5 new pool cards (bbb-insurance/training/accountant/security/arbitration · nwk-favor/rush/slick-lawyer/referral/gala — the owner's own list), session family, catalog joined · `pool:draw` intent (GX-30 empty · onTurn) pops the top INTO the seat's discard → the LOCAL row presents it ('BBB card drawn, buy insurance, put in local card area') · click YOUR turn = draw; off-turn = the deck's tap-nudge on ALL FOUR piles (object-logic parity) · arrangement wants: all four piles derive from poolCounts · BOTTOM-RETURN registered with its trigger (the fire/sell verbs — nothing discards pool cards yet; nothing faked) · 3 new pool laws (vitest 297) | sandbox-verified (all tsc·tiers·gate-surface 108·vitest 297·esbuild); O-2 → O-5 → O-6 continue |
| PA-1 the RADIAL PLAY-AREA TEMPLATE (I-141; the owner's own law) | pending | `playarea.ts` — R = max(chord-claim / 2·sin(π/N), tableHalfDiag + clear); seats EQUIDISTANT on the ring, yaw = angle, SEAT-1 anchored near-mid (the certified seat-1/seat-4 read pins hold BY CONSTRUCTION at N=6) · supersedes the L-5/L-5b corner special-case BY the template · SEAT_YAWS now DERIVES (ringYaws) · CORNER/MID_Z die · seat presets LOOK at their ring stations (mapPreset re-anchored; VG8c re-derives from ringLook/seatYawData — the I-133 pattern) · VG8g3 becomes `ring-stations` (every board's radius+yaw ≡ the template; kill: hand-place any seat → diverges by name) · N is a PARAMETER at last — the play area is a parent template in truth | sandbox-verified (tsc·tiers·gate-surface 110·vitest 297·esbuild); O-2 stations ride the ring next |
| PA-2 the box joins the ring (I-142; the template's first prop) | pending | RING_N = seats + props (7); the BOX takes slot 6 (yawed tangent; the beside-the-table placement + its edge check superseded → `box-on-ring` derives from ringSlot) · all angles re-derive at 2π/7, SEAT-1 still anchored · THE SEAT-4 180° READ PIN DIES LAWFULLY (154.3° now) — the far-read want re-derives from seatYawData (the law is face-the-board) · THE ALIGNMENT PROOF: heading(n) = atan2(sinφ·cos0.25, cosφ·cos0.25) ≡ φ — the tilt cancels; every seat faces the center at exactly 90° to its radius BY CONSTRUCTION, gate-asserted <1° · R₇ = 700.1 (table-bound: stations claim 553, the table 700) | sandbox-verified (tsc·tiers·gate-surface 111·vitest 297·esbuild) |
| I-144 the card taxonomy + the fixed table (owner-ruled) | this commit | LOCAL cards (tradespeople · equipment · BBB · networking) start in THEIR decks, DRAW to play, sit BEHIND the board with the folder + hand (the rows RELOCATED to the player side — I-131's table-side placement superseded), and BOTTOM-RETURN to their own deck on removal · GLOBAL cards: main deck → global space → discard pile · the MAIN DECK'S NAME open (proposed: EVENT DECK) · strata: y-up, counter top = y=0, DECKS ON the table · THE TABLE FIXED at 0,0,0 (pivot rejected); regions = camera anchors (already law) | rows relocated + oracle flipped; the station box (O-2) packs it next |
| I-145 the table mode + EVENT DECK (owner-ruled) | this commit | `tableMode: 'fixed' or 'rotate-to-active'` — a TEMPLATE option (mode is data; the rotate branch premultiplies the active seat's yaw at rebuild; BOTY configures 'fixed' — 'cool camera rotations between seats'); drill door `setTableMode` + `tableYawDeg` oracle · EVENT DECK approved — the player-facing vocabulary (status text now says it); the internal region id 'deck' persists as legacy anchor (a full id sweep = recorded mechanical debt, avoiding a mid-program re-pin storm) | rides the batch |
| O-2 + O-5 + O-6 (I-146; the objects program CLOSES its build phase) | pending | **O-2** STATION_BOX in playarea.ts (±240 × 300 behind the board) — folder column left (lat −180), rows right (MAXW 340), hand below; `stationBoxInfo` containment + VG8g5 · **O-5** THICKNESS: the table's 16u slab + six 4u board backings (no region tags — the count law holds); `slabCount` + VG8g6 (want 7) · **O-6** crew-zone + equipment-rack RETIRED (suppressed at SHOP_OVERLAY — the layouts falsifiability pin re-pinned ON THE RECORD; expectedFromDefs auto-adjusts) + the ACTIONS strip LIVE (your board, your turn → END-TURN; VG8s leg 7 `board-actions-end-turn`) · size-gate extractions: seat-play-oracles.ts + pile-actions.ts (VERBATIM moves; 247/277 lines) | sandbox-verified (all tsc·sizes·tiers·gate-surface 114·vitest 297·esbuild); THE SINGLE TEST awaits the owner's word |
| F-1…F-7 the mockup review fixes (I-148; the owner's screenshot findings) | pending | F-3 the table SITS ON the counter (raised — 'not cut in two') · F-4 the medal = a skinnable colored CYLINDER (ribbon retired; VG8h2 re-pinned ≥2 parts) · F-5 `propSlot` — footprint-aware ring placement (the box at R+320, clear of seat-0; VG8o derives) · F-6 TABLE_CLEAR 130→60 (the ring comes in; R₇ ≈ 630) · F-7 object-size-aware READ framing (small objects pull back 1.7×; boards 4% closer; the corner-true no-crop floor holds) · **F-1/F-2 = C-1 THE CARD PARENT OBJECT (queued next): ONE Card3D (thin-box body + face anatomy + the same grab physics) instantiated for EVERY card in play — the flat textured planes retire; seat cards stand properly** | sandbox-verified (all tsc·tiers·gate-surface 114·vitest 297·esbuild; the scratch env REBUILT after the workspace restart — the repo's Windows junctions don't traverse the mount, recorded) |
| **THE OBJECT PERMANENCE CONSTITUTION (I-149; owner-ruled — GOVERNS ALL CARD WORK)** | recorded | nothing is created or destroyed from game start to end — persistent Card3D instances MOVED/regrouped, never rebuilt (P-2c generalized) · THE LOCATION GRAMMAR: tradesperson {deck ↔ local; local → deck BOTTOM only} · equipment {+ attached-beneath-a-tradesperson ↔ equipment local} · BBB {deck → local → bottom} · NETWORKING {deck → THE HAND (face-down group; flip-all to pick up; drag-drop into play; revealed; → bottom} — CORRECTS O-3's local-row flow · the DICE RING: home = a small circle ON the R≈570 circumference (the board's corners touch it), rotating to the ACTIVE player; toss boundary = that circle in to 3/4 of the table area | the C-1 program builds under it |
| C-1a Card3D + the persistent instance registry | 🔄 BUILT (this commit) | `card3d.ts` (thin-box body · stamped face · deck-class colored back · setFace re-stamp) + `card-world.ts` (248 instances created ONCE at genesis from CARD_SET_6; recreate attempts COUNTED; conservation oracle) · the STATION cards are instances now (crew/equipment/BBB locals — the same physical object every build, re-claimed and posed) · the OLD HAND STAGING RETIRED (it duplicated in-play cards — permanence forbids; the real networking hand = C-1d) · VG8s `card-world-conserved` (instances ≡ genesis · recreates 0 across five verbs) | sandbox-verified (all tsc·tiers·gate-surface 115·vitest 297·esbuild) |
| C-1b decks as real instance stacks | ✅ COMPLETE (I-149/I-153/I-154) | ALL SIX piles = world-space instance stacks (supply four + EVENT DECK + DISCARD) · the viewer's deck carries the remaining ORDER — the flicked object IS the drawn card · draw theater object-aware (flatBase; instance flip = continuous X rotation, mirror-free; setFace reveal; +z face oracle) · discard fidget = POSE TWEEN (no parallel group — it would steal the instances) · ghost footprints (clickable at zero) · renderedRegionRects inverts world-stack ghosts to def units (heals the C-1b arrangement blindness) · I-153: genesis builds pools from the passed-in table (no predecessor lookup) | sandbox-verified; owner confirmed pools at genesis |
| C-1c the location grammar moves | 🔄 FLICK DOOR BUILT (I-156) | supply-draw.ts: grab→drag→FLICK on all four supply piles → verb → flip (identity-true via genesisPoolOrders) → travel to the seat → rebuild seats the same instance · on-turn pile click retired (hint) · drills flick now · I-157: BOTTOM-RETURNS live for crew+equipment (crew:release / outfit:sell → pool END; drop-on-pile gesture; GX-31 refusals incl. assigned crew; seatReturn oracle) · REMAINING: attach-beneath · BBB/networking returns (ride C-1d — data catalog) · prompt/venture Card3D conversion | sandbox-verified |
| G-A the AnchorGrid parent object | ✅ I-160 | world grid centered 0,0,0, ASSIGNABLE spacing (data) · every object: {anchor, faceTarget (default 0,0,0), span} · ring-radius snap + anchorsWithinRadius(x) query · spherical bound · absorbs: playarea ring/stationPos/seatAngle (become grid queries), OBJECT_SCALE feeds spacing | |
| G-B the seat play surface (7×4 child grid) | ✅ COMPLETE (I-163/I-164/I-165) — grid · spans · grouped sort · stick-on-drop · folder in the ledger span · hand zone marked | transparent surface behind each board; spans: ledger (1,1)-(3,2) · hand (4,1)-(4,2) · postings (1,3)-(4,7) · GROUPED SORT (I-159 supersedes L-4): BBB → tradesperson → equipment, top-left row-by-row; pairs = ONE grouped object on ONE anchor · flick → first open anchor · active-player drag-down STICKS to the released anchor · L-4 planner + tests re-derived on the record | |
| G-C child grids on every object | ✅ G-C1+G-C2 (I-169/I-170): the UIObject base-case + parent library + kingdom/chain laws · attach-beneath LIVE (crew:attach/detach, the pair on one anchor via the equipment-under socket, drop-to-attach + click-to-detach, pairsInfo law) — token perches await their first token | recursion: each object carries its own grid (spacing independent of parent, faces ITS anchor) · attach-beneath = the tradesperson card's socket (the grouped object) · token-on-card modifiers · the table board's own grid; the dice ring home = a ring anchor | |
| DOCTRINE (I-162) | — | polish = INTERACTION (gesture · motion · snap · sound · readability), never lighting/shadow/post-FX; flat stylized light IS the brand; responsive-and-light rules every render decision · ODG-p1 options enumerated at I-161, decision open at its trigger |
| G-D upright reads + F-8 | ✅ I-166/I-167: upright law + settle-snap + VG8n/ledger-upright · seat-read-equality VG8g7 (six numbers, 2% law) · P&L↔Balance switch drilled at VG8n/ledger-anchor-switch | ledger/P&L/Balance pop-ups UPRIGHT: aligned to the player board, 90° about y — never 'off at an angle'; plus F-8's seat-0 read equality + in-read P&L↔Balance switch | |
| C-1d the networking HAND | ✅ I-171 | hand slot in state (card data rides) · ownHand/handCount projection (redaction) · face-down fan on the HAND strip · flip-all pickup · drag-to-play → reveal → pool BOTTOM · hand-cycle law in a16 | battery hand+pair drills ride the next gate addition |
| D-1 the dice ring | 🔄 D-1a DONE (I-174) — D-1b RE-RULED at I-175(1): the throw area is its OWN object (independent of the tabletop; radius = table corner radius AS CONFIG; centered 0,0,0); the home anchor +10° past the seat center; the object-to-object grouping relationship (dynamic/fixed) joins the ontology; toss rails re-derive; turn glide; drill |
| PB (I-175 playtest batch) | ⬜ NEXT, in order | PB-1 seat-0 read hijacked by the ledger anchor (diagnose with seatReadEquality; the fix is the anchor law, not the camera) · PB-2 play-area read = DIRECT OVERHEAD + cards movable in read (organize mode) · PB-3 ✅ I-177 · PB-4 the box loses its lid · PB-5 seat boards off the card row (lift or one grid step inward) · PB-6 the deck/supply area gets a read anchor BOX; the dead dice square REMOVED · PB-7 the table shrinks slightly (cards closer); focus boxes keep size except global-width; center-left = PLAYER SUMMARY · PB-8 detach drops to the first OPEN anchor (verify against planPostings) · PB-9 THE PATH SYSTEM: flick → animated path to the open slot (no teleport); paths as a parent-UI asset (motion-guides) for child games + BOTY flow · PB-10 the per-seat spherical circumference as data | the die's rotating home circle + the annulus toss boundary (RAPIER rails re-derive) | |
| F-8 camera equality + report switching | ⬜ | seat-0 read ≡ seats 1-5 (equality oracle + gate); the in-read P&L ↔ Balance switch restored | |
