# K7-R2 — THE DISTINCT RE-REVIEW OF THE R-1a CLOSURES (I-115…I-118)

**Scope:** R-1a6 (dcbddbd) · R-1a6b (333f4a9) · R-1a6c (8e44c9c) against the K7-R return
list. Fresh reviewer, no builder context; the real repo never written; mutations in a
disposable scratch, restored byte-identical. No Chromium — source analysis plus
node-EXECUTED three.js geometry and the deterministic LCG.

## Closure verification — all six MAJORs GENUINELY CLOSED
- **M1**: reattach-before-nudge confirmed in order; the top-card assert has a
  DETERMINISTIC 16× margin (executed: tap 1 moves idx-35 by 4.79u vs the 0.3 assert);
  under the old order it moved exactly 0 — fails by name. The reattach cannot fake it.
- **M2**: the mutant EXECUTED again — flipDir stays 0, both direction asserts die. The
  LEFT drive is real (−48px); the drill covers +1. Both halves of the ruling.
- **M3**: drawFaceUp exists, asserted >0.9; the ±π math EXECUTED — +1.000000 both
  directions; the end-pose mutant reports −1.000000 → the leg fails. Real kill.
- **M4/I-117**: ONE plane end-to-end. Reconstructed the live camera (elev 45.8°):
  the cross-plane gap for one pixel = 34.7u (inside I-117's 26–54 band, 5.8× the
  threshold); the honest drive's travel = 2.60u < 6 → rolls; the mutant reads 15.07u →
  no roll. Kill preserved. No remaining frame mixture in the grab path.
- **M5**: speeds recorded; the uncapped mutant fails deterministically; the stale-trace
  path dead via live3b. (K7-R's "never cleared" was itself inaccurate — dragBegin
  allocates fresh; the REAL stale path was a claimless drive, now killed.)
- **M6**: readiness precedes the LCG in both draw paths; honest statuses; the surface
  key real; VG8r's wait real.
- **The extraction**: draw-route.ts byte-verbatim; dieScreenXY→adapter identical; no
  cycles; headroom 299→280 / 298→286 restored honestly.

## Findings: BLOCKING none · MAJOR none · seven MINORS (carried, named)
MINOR-1 (NEW): the top card is now a nudge-tween target — a second grab inside the
~200ms tween window momentarily teleports it (self-healing, gate-invisible; needs a
guard + row). MINOR-2 (NEW): small drags <6px SNAP home instead of gliding (≤8u pop).
MINOR-3 (NEW): the plane-miss fallback silently reinstates the thrice-bitten hit.point
frame (unreachable today; refuse instead). MINOR-4 (pre-existing): the sliding 6-sample
window makes a drag-then-stop-then-release read as a TAP → teleport-home + a seeded
roll ("a real drag FLICKS" contradicted; no gate leg drives this shape). MINOR-5: the
die.ts header trim dropped the (LAW)/(D-1) citations — contradicting I-115's "without
meaning-loss". MINOR-6: the honest status rests on a DUPLICATED guard, not a derived
boolean. MINOR-7: 6u ≈ 4.4–5.5px — touch taps with >~5px of finger travel still flick
(consistent with the sealed card threshold; SHOW on a touchscreen).

## Record defects RB-1…4 (superseded at I-119, ordered before R-1b)
RB-1: I-116's ray-miss diagnosis is geometrically FALSE — the die's silhouette is
48×60px with 23.3px of +x room; (+15,+6) was ON the die; both drives failed on the
frame. I-117's "may have been real" hedge is not supportable. RB-2: "≈8.4u off-centre"
is wrong in value AND mechanism — measured 15.07u, and a dead-centre click reads 17.89u
under the old frame (the projection parallax dominates; the offset REDUCES it). RB-3:
I-118's provenance ("88 green from abf4335 … nothing behavioral changed") is false as
written — the honest chain is 88/89 at 333f4a9 + VG8r 7/7 at 8e44c9c, with the
components/die.ts-only delta argued via VG8m's moveless clicks. RB-4: the roadmap was
stale on the very family being tagged (R-1a6 "PENDING"; a6b/a6c rowless).

## Scores: fidelity 8 · falsifiability 8 · lane 8 · record accuracy 7 (at the floor)

## VERDICT: PASS — `k7-pass-r1a` RIDES (1add656…8e44c9c)

Owner drills to SHOW, not discover: double-tap the deck fast · drag the die, STOP,
then release · tap the die on a real touchscreen. Carries into R-1b/W-1: MINOR-1..7 +
the standing K7-R/K7-S carries (gate-surface grammar with `phase`/`card` live · M9 ·
M11/D10 disposal · the escape hold · the toss-open orientation snap · the 2× live pace ·
the clamp band · nudgeCount's scope · the attach-shear class).

## Could not verify
Browser behavior (no Chromium); the absolute pixel coordinates (the ratios are robust);
the hard-flick drive's real CDP m/s (fails safe); MINOR-1/2/4 are outside every drive
shape the battery emits — the owner's hands are the instrument there.
