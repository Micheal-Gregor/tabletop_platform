# Drift Ledger + Interpretation Register — TABLETOP build

**Teeth:** no new work on a module scoring < 7 on any dimension until the score is raised.
Scores are set by the DISTINCT K7 reviewer — never by the builder.

## Drift entries

| ID | Location | Drift | Score (K7) | Status |
|---|---|---|---|---|
| D-1 | core.ts HK-1 | hook not falsifiable (mutation A survived); injection test masked by no-applier branch | axiom 6 → blocked | FIXED: refusal path narrowed to `legal === false`; hook blocks all else; K7-recipe on-path test added — awaiting K7 re-score |
| D-2 | core.ts/intentlog.ts | logged intents aliased to caller objects — row tamperable post-success (GX-3/GX-4 breach) | axiom 5 → blocked | FIXED: intent structuredClone+freezeDeep at submit door; tamper-stability regression tests — awaiting K7 re-score |
| D-3 | guard.ts | unregistered `state.seats` schema contract; appliers could mint seats | fidelity 6 → blocked | FIXED: seats passed from the row (I-7); mint-attack regression test — awaiting K7 re-score |
| D-4 | tools/check-tiers.mjs | bare-import + relative-path evasions (HK-6) | major | FIXED: all specifier forms + relative-escape resolution |
| D-5 | instruments | S3 "self-heal" unmapped | minor | FIXED: object-model note — self-heal = Transport's consumption of rebuild() at F7 |
| D-6 | statetree.ts | NaN/Infinity → null conflation in hash | minor | FIXED: throw on non-finite (I-5′) |
| D-7 | rng.ts / repo | dead draws getter; I-4 collision caveat absent; tsbuildinfo committed | minor | FIXED: getter pruned; I-4′ recorded; artifact gitignored |
| DF2-1 | packloader turn:pass | HK-5 real-path wiring theater (MUT-2 survived) | blocking | FIXED: D1 on-path suborned-Guard test; builder re-ran MUT-2 → killed |
| DF2-2 | turn.ts passSeat | HK-3 real-path call unfalsifiable (MUT-4 survived) | blocking | FIXED: D2 forged-wrap passSeat test; MUT-4 → killed |
| DF2-3 | windows.ts auto path | depth-1 law unverified on auto (MUT-13 survived) | blocking | FIXED: D3 auto-trap fixture; MUT-13 → killed |
| DF2-4 | packloader HK-4 | "schema valid" leg absent — NaN cash committable | blocking | FIXED: per-descriptor arg schema, recursive over window options; D4 tests |
| DF2-5 | windows/packloader | nonexistent decider bricks the game | blocking | FIXED: decider ∈ seats at load; absent-decider = auto-eligible; D5 tests |
| DF2-6 | windows.ts | silent auto-index repair (?? option 0) | major | FIXED: refuse at runtime + range-check at load; D6 tests |
| DF2-7 | packloader/effects | deck_inject card smuggling; uncataloged draw silent no-op | major | FIXED: catalog check at load + draw-applier halt; D7 tests |
| DF2-8 | packloader | TOCTOU — pack unsealed after validation | major | FIXED: structuredClone+freezeDeep at loadPack AND wirePack doors; D8 test |
| DF2-9 | effects.ts | unregistered levy exemption | major | FIXED: registered I-15, cited in code |
| DF2-10 | effects.ts | phantom citation "I-05" | moderate | FIXED: cites S1 v2.0 AX-5/S-3 |
| DF2-11 | packloader | wirePack's own HK-4 leg untested (MUT-6b) | moderate | FIXED: D11 direct test |
| DF2-12 | windows.ts | eliminated decider could also resolve (two legal deciders) | moderate | FIXED: I-16 refusal; D12 test |
| DF2-13 | turn.ts | 'ended' dead vocabulary; unbounded closing rounds unregistered | minor | FIXED: registered I-17 (F5 slot deferral) |
| DF2-14 | effects.ts | dead EffectContext.drawInt | minor | FIXED: deleted |
| DF2-15 | instruments | stale "proposed" rows; windowSeq/packId beyond I-8; onTurn mis-cite; deck no-ops unregistered | minor | FIXED: rows updated; I-8′/I-18 registered; cite corrected to M5/turn-order |
| DF2-16 (NEW-1) | packloader HK-4 | zero-option gated window validates → no path to decision, game bricked (GX-8; falsified the closure's own header claim) | blocking (K7 re-verify) | FIXED: options.length ≥ 1 at load (mutation-verified: disabling it fails only the NEW-1 test); trap fixtures given a real inner option |
| DF3-1 | admission door | HK-7 theater at admit() (MUT-F3-1 survived) | blocking | FIXED: EX-2 predicate moved INTO kinds.ts, gating enroll AND supersede; refusal-leg tests routed through gate.admit(); builder re-ran the mutation → 2 named tests fail |
| DF3-2 | kinds.ts | registry had three doors, one gated (P1 enroll bypass, P2 supersession laundering) | blocking | FIXED: every door runs HK-7; P1/P2 regression tests |
| DF3-3 | relations.ts | Representation predicate ignored state (P3) + prototype-chain path escape (P4) | major | FIXED: from ∈ components; Object.hasOwn-only path walk at formation AND read |
| DF3-4 | wire.ts | ontology intents undisciplined (P6) | major | FIXED: onTurn on all five (I-24); SP-4 drafted for the model question |
| DF3-5 | core.ts rebuild | tampered F3 move → raw domain throw, not DivergenceError (P7b) | moderate | FIXED: rebuild wraps applier throws (I-25); D5 test |
| DF3-6 | tests | no F3 replay test; title overreach | moderate | FIXED: D6 compose/place/form/dissolve rebuild ×2; title corrected |
| DF3-7 | surfaces.ts | two untested guards (MUT-F3-11/12) | moderate | FIXED: both refusal tests added |
| DF3-8 | relations/surfaces | duplicate compositions/relations; dangling placement location; retire didn't dissolve (P5/P8) | moderate | FIXED: duplicate-id + duplicate-relation refusals; Placement dissolve clears location; retire dissolves compositions emitting on-dissolve |
| DF3-11 (P11) | surfaces/relations | K7-F3 r2: double-placement possible; stale Placement dissolve wiped LIVE location | moderate (blocking-narrow) | FIXED: already-placed refusal at placeComponent; location cleared only when the dissolved relation IS the current placement; both regressions |
| DF3-12 (P12) | surfaces.ts | K7-F3 r2: retire stranded components placed ON the retired surface | moderate (blocking-narrow) | FIXED: retire dissolves Placements onto the surface (locations cleared by the Placement rule); regression |
| DF3-9 | kinds.ts | relationsGrantable dead data | minor | REGISTERED as I-26 (F4 deferral) |
| DF3-10 | instruments | wire row falsely claimed HK-7 on the real path | minor | CORRECTED append-only below |
| DF2-17 (OBS-A/B/C) | effects/packloader/index | unverifiable AX-5 citation; packGenesis unsealed when used directly; forceRoundWrap on the public surface | minor | FIXED: cite → seam S-3 + CLAUDE §9 do-not; packGenesis seals at its own door; forceRoundWrap demoted off the public index (tests import the module directly) |
| DF4-1 | contributions.ts | K7-F4 BLOCKING: shallow effect validation re-opened brick classes (P1 zero-option, P2 nested window, P3 nested unknown fx) through the contribution door | blocking | FIXED: recursive checkEffectShape (mirrors the F2 door: options≥1, no nested open_window, nested EFX closure, auto range, gated reserved); P1/P2/P3 tests |
| DF4-2 | registry dispatch | GX-19 total order unproven (MUT-2 survived — commutative test) | blocking | FIXED: non-commutative deck_inject order test; builder re-ran MUT-2 → killed (1 named test) |
| DF4-3 | registry activation | GX-24 derived activation unfalsifiable (MUT-3 survived) | blocking | FIXED: form→dissolve→pump test (fires nothing); builder re-ran MUT-3 → killed |
| DF4-4 | registry.register | unsealed door (P6 TOCTOU: post-register mutation smuggled Infinity via capitalize) | major | FIXED: structuredClone+freezeDeep at register; assertFiniteResult added to fxCapitalize; D4 seal test; list() hands sealed rows |
| DF4-5 | slots/registry banks | prototype-keyed lookups (P5: id '__proto__' / slot 'constructor') | major | FIXED: hasOwn-guarded lookups everywhere; reserved ids/slot names refused at validation; tests |
| DF4-6 | completion ledger | no F4 rows | moderate | FIXED: rows appended (MR1 NOT-COMPLETE by rule on V-7/V-8) |
| DF4-7 | contributions.ts | malformed shapes crashed raw (P11-class) | moderate | FIXED: validConditionShape + array-shape checks, all NAMED; tests |
| DF4-8/9 | register | snapshot + hasOwn claims put on the record | moderate/minor | REGISTERED I-32; GBC-28 title corrected |
| DF4-10 | wires | third onTurn copy | minor | FIXED: shared kernel/discipline.ts, three consumers |
| DF4-11 | misc | dead declarations(); confused GBC-26 test; loose R-14 cite; RulesetView partial exposure | minor | FIXED: method removed; test cleaned; cite corrected; view exposes condition/effects/slotWrites in full |
| EXT3-A | contributions + packloader checkFx | external audit 3, MODERATE: null/undefined elements in fx arrays raw-crashed (DF4-7/EXT2-3 closures narrower than their claims — the pattern again) | moderate | FIXED: object-with-string-fx guard at BOTH doors; named refusals; tests |
| EXT3-B | registry slotWrites | increment silently coerced non-numeric slots | minor | FIXED: refused-not-coerced; test |
| EXT3-C | validateUniqueDef | duplicate contribution ids validated (register-parity gap) | minor | FIXED: dedup check; test |
| EXT3-D | contributions bearer | relationType unvalidated → silently-inert dead rules | minor | FIXED: relationType ∈ the five; test |
| EXT3-3B | packloader test gap; registry increment value; UniqueDef null element | external audit 3B: pack-door null-guard unfalsifiable (theater); increment-BY-non-number coerced; UniqueDef null element raw-crashed — the signature pattern, three one-liners | moderate/minor | FIXED: pack-door falsifying tests (card + option shapes); increment value type refused; UniqueDef element-shape named; auditor pre-authorized sign-off on these closures |
| DF5-1 | library/wire + kernel | K7-F5 BLOCKING (Probe-1): turn:pass bypassed the entire weave — legal logged wrap with no TFX tick/lapse/dispatch/resets (GX-29 falsified on the live path) | blocking | FIXED: kernel supersession door (core.supersedeIntent/Guard.supersede, existing-registration + named-ground required); wireLibrary supersedes turn:pass (refuse + defense-in-depth applier throw); I-37; D1 tests |
| DF5-2 | F5 intent doors | K7-F5 BLOCKING (Probes 2a/2b/2c): doors committed brick values — NaN charge → unhashable state; Infinity charge → permanent wrap brick; payoff to unknown seat → Reckoning unreachable; debts[] shapes unvalidated | blocking | FIXED: HK-4-style validation at every F5 door (checkSpecShape/checkDebtsShape/checkTfxShape + upkeep overhead) refusing typed+unlogged; module doors harden finiteness/seat-existence too; D6 tests |
| DF5-3 | library/wire turn:end | K7-F5 BLOCKING (survivor L2): hookHk5BeforeSeatAdvance on turn:end was theater — deletable, suite green; CC-6 claim falsified | blocking | FIXED: suborned-Guard on-path test (D2, mirrors f2-k7-closures D1); mutant L2 re-run → killed |
| DF5-4 | library/wire weave legs | K7-F5 BLOCKING (survivors M1/M3/M4): on-round-wrap dispatch, lapseExpired, and per-round resets all deletable with a green suite — GBC-39 prose unbacked | blocking | FIXED: D3 (on-round-wrap contribution fires + per-round slot resets in the same weave) + D4 (deadline lapse through turn:end); mutants M1/M3/M4 re-run → killed |
| DF5-5 | upkeep unloaded-Ledger | K7-F5 MAJOR (Probe-3): due debts DELETED unpaid and overhead threw LedgerRefusal in the opt-out configuration (GX-25/GBC-37 broken by structure) | major | FIXED: EffectEngine fallback for both legs (I-39); D7 test proves settlement lands and no ghost ledger entries |
| DF5-6 | ledger/ventures/timedfx guards | K7-F5 MAJOR (survivors P/F/O): non-finite-leg check, route-non-open check, duplicate-tfx-id check all untested | major | FIXED: falsifying tests D6h/D5/D6c; mutants P/F/O re-run → killed |
| DF5-7 | ventures.ts + axioms + object-model | K7-F5 MODERATE: phantom citation tokens T2/T3/T6 resolved nowhere in governance/ | moderate | FIXED: corrected to RC-A′/RC-E (Stage-2b) / excised in all three places, correction noted on the record |
| DF5-8 | I-36 registration | K7-F5 MODERATE: routing decision had no binding force beyond the gate — under-registered | moderate | FIXED: registered as I-38 (window = gate + ceremony; route args solely authoritative; option-binding = content policy); GX-27 text extended |
| DF5-9 | ventures/outfit lapse | K7-F5 MODERATE (Probe-4): lapsed ventures accepted work; crew stranded; object-model claimed an unbuilt penalty path | moderate | FIXED: workCrew refuses non-open ventures; lapseExpired releases assigned crew; penalty = pack policy registered N/A-by-absence; row + GX-26 corrected; D4 tests |
| DF5-10 | ledger.post | K7-F5 MINOR: entry append read the stale pre-application state; a seat named 'bank' would silently escape application | minor | FIXED: reads NEXT; reserved-name collision refused; D8 test |
| DF5-11 | F5 persisting doors (spawn row, route debts, tfx attach) | K7-F5 r2 BLOCKING (NEW-1, the EXT2-1/EXT2-4 signature): known fields validated but the CALLER'S OBJECT stored — an unknown NaN field rode a legal logged intent into state and broke hashability (GX-3) | blocking | FIXED both legs: doors REFUSE unknown keys (spec/portion/payoff/debt/tfx — refusal-not-repair, never strip); modules CONSTRUCT stored rows from validated named fields only; D9 falsifying tests per door + structural-leg test |
| DF5-12 | supersession door test + object model | K7-F5 r2 minors: D1b title overclaimed (empty-ground leg unasserted — DF3-6 precedent); kernel/core object-model row missing the supersession door | minor | FIXED: empty-ground assertion added; core row amended append-only (NEW-4) |
| EXT4B-1..4 | spawn door legs + outfit + weave | EXTERNAL AUDIT 4B (the SEVENTH closure-narrower-than-claim): FA-1 deadline/work/party validation unfalsifiable at both doors (guards present and correct — a falsifiability gap, not a live hole); FA-2 ventures module-door masking unregistered; FA-3 outfit portion-exists/portion-done untested; FA-4 per-turn slot reset untested | moderate + 3 minor | FIXED: D10 falsifying door tests (deadline/work/party, each named); I-40(b) extended to the ventures masking; D11 (out-of-range + done-portion refusals); D12 (per-turn sweep after a non-wrapping turn:end, slot seeded at genesis) |
| DF7-1..8 | transport fan-out + tier checker + records | K7-F7 round 1: D1 BLOCKING (throwing subscriber poisoned the controller: writer threw post-commit, fan-out desynced permanently); D2 BLOCKING (fan-out delivered the caller's raw intent alias — the kernel D-2 class); D3 MODERATE (content branch of check-tiers was theater under MUT-C3); D4 MODERATE (records promised a 'defaults' catalog field the code doesn't ship); D5-D8 minors (GX-32 wording; unauth leave() unnamed; no tampered-resume test; headerless F7 rows) | 2 blocking + 2 moderate + 4 minor | FIXED: sealed-clone fan-out + per-listener containment/eviction + listenerFaults() (I-45); D1/D2/D7 falsifying tests; bare-specifier content probe (kills MUT-C3); GX-33/GX-32/RESOLUTION_RECORD corrected append-only; I-42 extended; F7 section header added |
| DF6-1..6 | presentation emitter/render/records | K7-F6 round 1: DF6-1 BLOCKING (shallow R-23 door — nested function/thenable emissions crossed the seam, detonated untyped; M7 survivor); DF6-2 BLOCKING (a11yAudit unfalsifiable — M8 survivor); DF6-3 MODERATE (EP-1′ + fidelity-min-formula named by S3 with no recoverable definition — N/A silence); DF6-4/5/6 minors (D-2 home drift; unregistered own-deck reading; a11yAudit row misfiled) | 2 blocking + 1 moderate + 3 minor | FIXED: deep structuredClone door w/ typed refusal + aliasing severed + poisoned-args tests (M7 killed); a11y negative cases (M8 killed); SP-6 F-backflow + N/A-BY-UNRECOVERABILITY on the record + labels-split correction; RESOLUTION_RECORD corrected; I-48 registered; row fixed; VERB_NAMES dead surface removed |
| EXT2-1 | packloader HK-4 | **external audit 2, BLOCKING:** window whose sole/any option opens a window validates → statically dead under depth-1 → brick/landmine (the NEW-1 SIBLING — DF2-16's closure was narrower than its claim) | blocking | FIXED: nested open_window refused at load ("statically dead"); trap fixtures moved to FORGED GENESIS (engine-side) so runtime R-17 tests stay on-path; mutation-verified (disabling the check fails both R2-1 tests) |
| EXT2-2 | effects.ts | finite-in ≠ finite-out: two max-double pays committed AND logged cash:Infinity; illegality surfaced lazily at hash time | major | FIXED: assertFiniteResult at every numeric mutation (pay/levy/grant_favor) — refused at application; regression proves log holds only the legal move and the row replays clean |
| EXT2-3 | packloader HK-4 | non-array fx (card or option) → raw TypeError, breaking the "names defects" contract | moderate | FIXED: array-shape checks named in the refusal |
| EXT2-4 | packloader wirePack | wirePack's OWN seal unfalsifiable (MP6 survived; D8 only proved loadPack's door) — DF2-8's closure narrower than its claim | moderate | FIXED: direct-wirePack TOCTOU regression (genesis from separate copy); builder re-ran MP6 → killed |
| EXT2-5 (I-19) | effects/packloader | content controlled the unvalidated `gated` flag (unregistered invention; gated:false windows silently skippable) | minor | FIXED: gating ENGINE-RESERVED — every content window gates (S-8); content declaring `gated` → load refusal; registered I-19 |
| EXT2-6 (OBS-1/3) | effects/packloader | dead isFrozen leg in HK-9 (unobservable — frozen objects cannot unfreeze); all-eliminated packs loadable | minor | FIXED: dead leg removed; "no LIVING seat" load refusal added. OBS-2 (mapSeat defensive throw) RETAINED and registered as expected-survivor defensive depth |

## Interpretation Register (decisions the handoff did not make)

| ID | Decision taken | Class (benign/latent/conflicting) | Route (local / F-supersession / AE-link) |
|---|---|---|---|
| I-1 | MoveLogRow carries pack-ref (id, version, integrity hash) — AX-4/V-2 well-definedness | latent → **RESOLVED** | F-supersession EXECUTED as SUP-1 (owner-approved 2026-07-25); interpretation SANCTIONED — no longer ahead of spec |
| I-2 | Genesis state = PackLoader's validated setup declaration over (pack, seats, seed); empty log = genesis | benign | local; depends on I-1 |
| I-3 | Non-active-writer submit semantics (refuse vs queue) | benign | local; registered at F7 build, AE-linked to lockstep spec |
| I-4 | RNG algorithm = mulberry32 over FNV-1a fold of (seed ⊕ stream name) — pure JS, portable, byte-stable | benign | local (rng.ts) |
| I-5 | State hash = FNV-1a 64-bit over canonical (key-sorted) JSON — pure, no platform imports (ER-7); NOT cryptographic; tamper-evidence stays a production concern (S3 §8) | benign | local (statetree.ts) |
| I-6 | Applier misbehavior (no state produced) → HookViolation throw, state+log untouched — engine defect surfaces loudly, never repaired into a refusal | benign | local (core.ts, HK-2 injection test) |
| I-7 | Seat legality checked against the ROW's authoritative seats (passed to Guard.check), never a state schema — kernel pack-agnostic; appliers cannot mint seats; genesis need not carry a seats key | benign (K7 defect 3 closure) | local (guard.ts, gx3-log-integrity tests) |
| I-4′ | I-4 caveat appended (K7): fnv1a32 stream-name fold is 32-bit — a name collision would silently merge two streams; stream names are engine-chosen (closed set per pack), not user input; revisit if packs ever name streams | benign | local (rng.ts) |
| I-5′ | I-5 caveat closed (K7 defect 6): canonicalJson now THROWS on non-finite numbers rather than conflating with null | benign | local (statetree.ts) |
| I-8 | F2 engine state regions: `seats[]{id,cash,favor,assets[],sueRights[],eliminated}` · `turn{round,seatIdx,phase,wrappedRound,maxRounds,status}` · `decks{ref:{draw[],discard[],reserve[]}}` · `windows[]` — card DEFINITIONS stay in the loaded pack (derived-never-stored; packRef pins them) | benign | local (packloader genesis) |
| I-9 | Five phase names = start · draw · resolution · maintenance · cleanup (owner's stated flow); at F2 phase stepping is tracked state, with binding discipline arriving when rules/library attach to phases (F4/F5) | benign | local (turn.ts) |
| I-10 | F2 pack schema depth: `{id,version,efxVersion,maxRounds,seats[],cards{id:{fx[],flavor?}},decks{ref:{cards[],owner?}}}` — flavor carried, never read | benign | local (packloader.ts) |
| I-11 | At F2 the EffectEngine's feeds are card-borne fx only; registry feed = F4, library feed = F5 (explicit deferral, AE-linked to those slots) | benign | local (object model note) |
| I-12 | Elimination MECHANISM is F5 (Outfit viability policy); F2 recognizes the `eliminated` flag: skipped in rotation, auto-policy-eligible for windows | benign | local (turn.ts/windows.ts) |
| I-13 | Window depth tracked via apply-context (depth 0 = normal fx; depth 1 = inside window resolution); open_window at depth ≥ 1 → refused (GX-11) | benign | local (effects.ts) |
| I-14 | V-1 ownership corrected: MINIMAL needs F5 modules; V-1 discharges after F5. V-2/V-3 discharge at F2's R gate | benign | object model + completion ledger |
| I-15 | Table levy exempts eliminated seats (the table charges the living; estates = F5 Outfit territory) | benign (K7-F2 defect 9 closure) | local (effects.ts) |
| I-16 | An eliminated seat may not act as decider — auto-policy owns its windows; one window never has two legal deciders | benign (K7-F2 defect 12 closure) | local (windows.ts) |
| I-17 | Status 'closing' = the M15 Closing-Round SLOT: rounds continue unbounded and 'ended' is reserved vocabulary until M15 opts in at F5 (explicit deferral, AE-linked to the F5 slot) | benign (K7-F2 defect 13) | local (turn.ts) |
| I-18 | M6-level silent no-ops (drawTop unknown deck → none; toReserve unknown card → unchanged) are LEGAL at the deck layer; catalog integrity halts at the draw APPLIER (GX-10); "absent" (vs eliminated) deciders = F7 presence territory, deferred | benign (K7-F2 defect 15) | local (deck.ts/packloader.ts) |
| I-8′ | I-8 amended: genesis also carries `windowSeq` (window id counter — replay-deterministic); `packId` REMOVED from state (duplicated the row — GX-6) | benign | local (packloader.ts) |
| I-20 | The record says "12 kinds" but NAMES 11 (Board, PlayerBoard, Card, Token, Die + Tile, Figure, Spinner, Slider, Dial, Timer) — the count appears to include a pre-unification kind (Counter→Token or Meeple→Figure merges). Roster seeded with the 11 NAMED; admission-by-rule makes the count non-load-bearing. FLAGGED to owner as a record question (potential F-channel wording fix) | benign | local (kinds.ts seed) |
| I-21 | S-4 emission before F4 exists: on-form/on-dissolve events RECORDED on-state (`relationEvents[]`, append-only, replay-deterministic); the F4 HookBus CONSUMES them when built — explicit deferral, AE-linked to the F4 slot | benign | local (relations.ts) |
| I-22 | F3 state regions added to I-8: `components{id:{kind,surface?,position?}}` · `surfaces{id:{topology,composedOf?}}` · `relations[]` · `relationEvents[]` · seq counters. The kind REGISTRY stays engine-side (derived from platform seed + pack — derived-never-stored) | benign | local (ontology/*) |
| I-23 | R-13's forbidden input at F3: the five built-ins all carry predicates, so "relation without formation predicate" is reachable only via an UNKNOWN relation type or a forged predicate-less type — both refuse; content cannot define relation types (closed set of five) | benign | local (relations.ts) |
| I-24 | Ontology intents are PLAYER intents under turn discipline (same onTurn law as F2); WHO may reshape ontology beyond turn-order (and whether content-triggered formation needs the docketed form_relation EFX member) is a MODEL question routed to the F-channel as SP-4 | benign (K7-F3 defect 4) | local (wire.ts) + SP-4 |
| I-25 | Two refusal channels, registered: player-path domain refusals surface as typed throws (EffectRefusal/RelationRefusal/SurfaceRefusal — loud, state intact, unlogged); Guard refusals return typed Refusal objects; at REBUILD both always surface as DivergenceError | benign (K7-F3 defect 5) | local (core.ts rebuild) |
| I-26 | relationsGrantable is admission METADATA at F3; enforcement at formation lands with F4 rule dispatch (contributions consult the registry) — explicit deferral, AE-linked to the F4 slot | benign (K7-F3 defect 9) | local (kinds.ts) |
| I-28 | HookPoints v1.0 member NAMES are interpretation (the record gives counts: 7 turn + 6 lifecycle + on-form/on-dissolve × 5 relations = 23); names enumerated in vocabularies.ts | benign | local |
| I-29 | Turn/lifecycle hook EMISSION from engine paths (TurnMachine/Deck/windows calling the bus) lands at F5 when library modules bind to phases; at F4 the bus consumes F3 relation events + direct dispatch | benign (explicit deferral, AE-linked to F5) | local (hookbus.ts) |
| I-30 | Contribution effect args validated at F4 to membership + finite-number depth; full pack-context binding (seat/deck refs) at pack-integration (F5) | benign | local (contributions.ts) |
| I-31 | Relation-borne activation DERIVED (formed relation of bearer type exists at dispatch) — stateless, replay-deterministic; kind-borne contributions always active | benign | local (registry.ts) |
| I-32 | F4 registered semantics: Condition DSL (always | eq/ne/gte/lte(path,value) | and/or(terms); `ne` fires on unresolved); SlotWrite set/increment; `relationEventsProcessed` cursor region; per-firing snapshot = STRUCTURALLY guaranteed at F4 (no reentrant path; becomes falsifiable at the F5 weave, AE-linked); runtime hasOwn walk = defensive depth (OBS-2 class) | benign (K7-F4 D8/D9) | local (rules/*) |
| DF4-12 (NEW-2) | registry.register + pack doors | validate-then-clone let a lying getter seal an unvalidated list | moderate | FIXED: clone-FIRST-validate-the-clone at registry.register, wirePack, loadPack; getter regression test |
| I-32′ | I-32 extended per K7: the dispatch-bank hasOwn guards and fxCapitalize finite check are registered EXPECTED-SURVIVOR defensive depth (NMUT-F/NMUT-H — unreachable behind validated doors) | benign | local |
| I-33 | F5 state regions: seats[].role · crew[] · ventures[] · debts[] · receivables[] · timedEffects[] · ledger{loaded, entries[]} · results. The WOVEN pass intent is 'turn:end' (library tier). ~~kernel 'turn:pass' remains the primitive~~ — the coexistence leg was RECLASSIFIED by K7-F5 Probe-1 (a raw pass falsified GX-29 on the live path): superseded by I-37 | benign → superseded leg reclassified (K7-F5 D1) | local (library/*) |
| I-34 | Venture spawning is a LIBRARY INTENT at F5 (venture:spawn with pack-supplied params as args); content-TRIGGERED spawn awaits the docketed spawn_venture EFX member (SUP-2 pattern) | benign | local (ventures.ts) |
| I-35 | MINIMAL's overhead schedule is pack-script-driven: upkeep takes {overhead} as an ARGUMENT (AX-3 human-inputs-as-arguments); Stage-2b arithmetic charges overhead in r1 only | benign | local (V-1 runner) |
| I-36 | Windowed routing: the gated IWN records the CHOICE (empty-fx options — EFX cannot carry library semantics); effectuation is the subsequent logged 'venture:route' intent with the decision as arguments; auto/AI parity holds (both are logged intents) | benign | local (ventures.ts/wire.ts) |
| I-37 | turn:pass SUPERSEDED by wireLibrary (K7-F5 D1): with the library wired, the weave owns the pass — a raw turn:pass refuses (typed, unlogged) because it would wrap without TFX tick, lapse, on-round-wrap dispatch, or resets (a legal logged intent falsifying GX-29 otherwise). The kernel supersession door (core.supersedeIntent, Guard.supersede) requires an existing registration + a named ground — R-14 discipline on the kernel's own registry. F1-F4 wirings without the library keep the primitive | benign | local (library/wire.ts + kernel door) |
| I-38 | I-36 extension (K7-F5 F8): the routing window is the recorded GATE + decision ceremony; the logged venture:route intent's ARGUMENTS are solely authoritative (a resolved 'decline' does not forbid a later route — the venture stays open to its deadline, then lapses; binding option-to-effect coupling = content policy at the pack tier) | benign | local (ventures.ts/wire.ts) |
| I-39 | Unloaded-Ledger upkeep (K7-F5 D5): where ledger.loaded is false, wages and debt settlement fall back to EffectEngine levy/pay — exactly the tickTimedEffects/reckon pattern; debts are SETTLED then removed, never deleted unpaid. GBC-37's balanced-post form is the loaded configuration | benign | local (library/wire.ts) |
| I-40 | I-32′-class EXPECTED SURVIVORS registered per K7-F5 r2 NEW-2: (a) Guard.supersede and core.supersedeIntent each mask the other's existing-registration check (redundant pair — deleting either alone stays green); (b) module-side checks masked by their TESTED wire door on the intent path (direct-API depth): timedfx charge/scope, AND — per EXT-4B FA-2 — the ventures module legs (payoff-amount, payoff-to, portions-len, deadline, work, party; the D9e/D10 door tests kill the door copies, the module copies remain belt-and-suspenders); (c) ledger.post entry-append reads NEXT — structurally unfalsifiable today (EffectEngine never touches the ledger region; precautionary hardening, K7 r2 observation N9). All are defensive depth behind validated doors, not theater | benign | local |
| I-41 | ODG-3 RESOLVED (owner, 2026-07-30, Option 3): the pattern catalog is DATA-FIRST with THIN BUILDERS — entry = {family, doc, build(params)→fragment}; builders EMIT DATA ONLY (no engine call, no state touch); fragments re-validated at the ENGINE's doors; V-4 pins built-fragment behavior (VK-8). Counts are law: 6 VNT · 3 RTM · 9 IWN · 2 TFX + closing defaults (BOTY inventory §2/§5, stage-2b §6) | benign | local (packages/patterns) |
| I-42 | Transport realization: an IN-PROCESS lockstep controller (AE-c6-CF shape) hosting an EngineCore via the S-2 contract only; network/realtime infra, host election, presence timeouts = production concerns (S3 §8). Writer-discipline refusals are TRANSPORT-tier (mirror the kernel Refusal shape, never logged — the engine Guard never sees them). EXTENDED per K7-F7 D6: join/leave/takeover are UNAUTHENTICATED in-process — leave-on-behalf (and thus seat hijack of a present client) is possible by any in-process caller; client identity/auth = production concern (S3 §8), the surface is hereby NAMED | benign | local (transport/lockstep.ts) |
| I-45 | Listener-fault policy (K7-F7 D1): a throwing subscriber is CONTAINED and EVICTED at the fan-out door — the writer's result, the log, and surviving listeners are unaffected; faults surface via listenerFaults() in order — CORRECTED append-only per K7-F7 r2 NEW-1: containment holds for ANY thrown value (throw null/string/object extract safely; the original claim was proven for Error throws only), and fault entries are FROZEN at push (NEW-2). Fan-out delivers a SEALED clone (D2 — the kernel D-2 aliasing law at the transport door) | benign | local (transport/lockstep.ts) |
| I-46 | BOTY-slice registrations (ODG-4 Option A): (a) with the Ledger loaded, CONTENT EFX cash moves (card fx, window options, contribution effects) are cash-affecting but ledger-TRANSPARENT — RC-D is scoped to library-mediated moves; the slice test proves the sharper reconciliation cash ≡ derivedBalances + known-EFX-deltas; a full EFX→ledger mirror = F-backflow/ExtensionContract candidate if a universe needs it. (b) GX-27 routes ALL unassigned portions to ONE counterparty — the 3-trade GC slice routes to one shop; per-portion multi-target routing = content policy or docket. (c) Contribution firing in this stack = on-round-wrap (+ relation pumps); a per-draw hook awaits the docketed draw_card member | benign | local (packs/boty) |
| I-47 | F6 realization (ODG-p1 completed, owner-approved 2026-07-30): HEADLESS presentation model + SVG-STRING renderer — pure functions SeatView → SVG documents; flourishes/skins/contracts are DATA; live DOM binding, assets, sound playback, input = Phase 5 utilization. The SeatView brand is a frozen marker field; redaction law: opponent deck contents and non-decider window options are ABSENT from the view (not merely hidden). The presentation clock is LOCAL (ODG-e1 untouched) | benign | local (packages/presentation) |
| I-48 | F6 registrations (K7-F6 D5): (a) "own deck" ⇔ the deck whose ref EQUALS the seat id (fails closed — an unknown ref yields nothing, more redaction never less); (b) inline SVG geometry (rect sizes, text coordinates) is FRAME STRUCTURE, not skin — no color/asset/typeface literal exists outside bound tokens (consistent with D-1: the frames are the platform's, the skin is the content's); (c) the emission door LAUNDERS prototype-carried behavior to pure data (structuredClone strips methods — sound, K7-F6 r2 obs) while functions/thenables/symbols REFUSE; cloneable non-JSON forms (Map/Date/TypedArray) cross as data and the ENGINE seam defends — TypedArray untyped-TypeError there recorded as an engine-ledger minor candidate (state-safe, unlogged) | benign | local (packages/presentation) |
| I-49 | Phase 5 target-occasioned core touch (K8 finding 1, registered): vectors/scenarios.ts carried a DUPLICATE `import { RuleRegistry }` (the V-1 block re-imported what line 183 already held) — removed for esbuild browser bundling. Type-level dedup, ZERO behavior (all 9 pins re-derive in vitest AND Chromium); the only diff in packages/packs/vectors/tools since phase4-complete | benign | local (vectors/scenarios.ts) |
| I-50 | Layout contracts (owner-ruled): parents = data (regions {id, role, x/y/w/h/z} in a 0..100 unit space + optional outline polygon); children = overlays {override/add/suppress} validated at extension (LayoutRefusal names the defect); geometry free, regions law. The camera is a pure viewBox computation (2.5D: table-plane tilt + z-sort + shadows) — presentation-local, stateless toward the game. Showcase = bench-tier executable of the PARENT vocabulary | benign | local (presentation/layout.ts + bench showcase) |
| I-51 | v1-extraction rulings (owner "go" 2026-07-31; source = the owner's nine BOTY-v1 screenshots, measured): **(a) modal-as-card** — v1's round interstitial and character/fortune popup are CARD CHILDREN rendered at a camera focus preset, not dialogs; no modal machinery enters the template. **(b) art dominance** — v1 proves art at 55–70% of card height; realized as child OVERRIDES of `art`; the parent's 38% stands (parents stay minimal, v1 structure arrives as declared overlays). **(c) children live at the CONTENT tier** — packs/boty/src/layouts.ts imports @tabletop/presentation downward (lawful; HK-6 direction holds); the presentation package is untouched, so the V-9 pageSvg pin is safe by construction. **(d) chrome is bench furniture** — header (place·season·round·turn·toolbar), alert banner, CPU presence dots, footer nav are utilization-tier composition, never layout law. **(e) skin tokens recorded, not painted** — v1's grammar (dark #161a22 ground; gold = money/active/accent; green = confirm/advance; red = the ONE danger item on screen; gold outline = active entity; bold-line + muted-subline rows; deck-shows-its-count) captured as candidate tokens in packs/boty/skin-token-candidates.md; frames stay unskinned per D-1 until the owner opens the skin era | benign | local (packs/boty + utilization/bench) |
| I-52 | Bench crew read (registered at K7-v1x D3): SeatView carries no crew projection (crew ids/assignment are F5 state the F6 projector was never charged with), so bench v5 reads `state['crew']` from the rebuilt row for ALL DISPLAY-SIDE READ-ONLY FILLS (tokens, standings/board crew+working counts) AND CLICK ROUTING (purpose widened per K7-v1x re-verify minor 1) — display-side, read-only, every mutation still travels emit()+controller.submit. Sibling registration (K7-v1x re-verify obs 3): the bench table-log fill reads `controller.row().moves` — a bench-owned artifact (the bench is host and persists the row), display-only. Bench v6 carries the SAME registered reads (the rivals-carousel crew fill included) — no new raw-read class. Everything the projection DOES carry (seats, decks, windows, ventures, debts, receivables, results) is read from project() alone (AR/AP moved onto the view per K7-v1x D3). Closing the gap properly = a SeatView crew projection — recorded as a next-increment candidate with the CPU-opponent work, since redaction policy for crew (what an opponent may see) is an owner call | benign | local (utilization/bench/src/game.ts) |
| I-53 | AUDIT-EXT-5 dispositions (external visual audit, RETURN → closures 2026-07-31; report governance/audits/K7_AUDIT_REPORT-5.md): **F1 (blocking, CLOSED)** — boty:fortune-card art was coded 52% vs the auditor's measured 65–75% (and the increment's OWN 55–70 band): recoded to 66% with the lower regions reflowed; the test now pins the BAND (55 ≤ art.h ≤ 70) AND fortune-art > round-art — the ≥50 assertion that let 52 ship is superseded. Root cause registered: the builder pinned "taller than parent" instead of "inside the measured band" — band claims in instruments must be pinned AS bands in tests. **F2 (major, REGISTERED — live-product drift + audit-coverage gap):** boty-web.vercel.app now gates play behind a one-time email-code sign-in absent from the archived flow; the auditor rightly did not authenticate; landing verified FAITHFUL live, screens 02–09 verified against ATTESTED archived sources + internal consistency (no fabrication indicators). The archived screens remain the increment's fixed source documents (that is why they were archived); completing the authenticated live walk requires the OWNER's sign-in — owner-optional at the EXT-5 re-verify. **F2 CLOSED (auditor's updated Report-5): the owner performed the sign-in and the auditor completed the full live walk — ALL NINE screens verified FAITHFUL against live v1; the coverage gap is gone. Live v1 additionally corroborates F1 (art ~60–80% live) and surfaced two additive non-regressions (auth gate; a "Who goes first?" preamble modal preceding the round card).** **F3 (minor, REGISTERED):** the 09 character-modal "Next ▶" is the dismiss affordance, not card anatomy — the bench's click-card-to-dismiss realizes it (I-51a); not modeled, correctly. **F4 (minor, REGISTERED):** shop-board local-play/hand are carried parent regions with no distinct v1 counterpart — honest bracketed placeholders, kept (the parent contract survives the child) | benign | local (packs/boty + governance/audits) |
| I-54 | V2-LIVE-OBJECT-INVENTORY adopted as a SOURCE DOCUMENT (owner-authenticated live walk, archived governance/audits/V2-LIVE-OBJECT-INVENTORY.md, commit ee27f6a). Reconciliation against the build at adoption: (1) its F1 note (fortune art 52% under-encodes) was compiled PRE-closure — already closed at d7e50bc (art 66%, band-pinned test); (2) its F5 (Books view has NO v2 object) and F6 (town-table lacks the art-banner region) are ACCEPTED as EXT-5 supplementary findings via the inventory — the numbered docket below; (3) its object/parameter/function tables are the PARITY CHECKLIST for future increments (nothing in v1 goes missing silently — anything not built must be recorded as deferred, never dropped); (4) its closed vocabularies (6 trades · 3 difficulties · 6 card kinds · seasons · $/W display unit at $50/W · net-30/net-90 · J1–J6 ladder) are content-tier vocabulary DATA reserved for the mechanics increments — none enters the engine outside its doors. **Docket (owner-ranked, each its own gated increment):** (a) town-table `art-banner` add (small, pin-safe); (b) `boty:books` — BLOCKED on two prerequisites: a Books source screenshot (none exists in sources/ — our measured-source law forbids guessing geometry) and an owner ruling on the parent (new PANEL kind in the certified presentation tier vs fold onto BOARD_PARENT at content tier); (c) `round-preamble` card child; (d) rivals-carousel (compact shop-board reuse); (e) cards-gallery (fortune-card tile grid + the 6-kind filter vocabulary); (f) promote job/tradesperson/equipment to first-class card children with §4 parameters; (g) product-shell chrome (auth/settings/how-to/save-and-leave) = utilization tier, with the mechanics increments | benign | local (governance/audits + docket) |
| I-55 | v1-parity layouts interpretations (owner-ruled scope 2026-08-01; sources = the archived nine + the auditor's live-walk evidence in Report-5/inventory §4–5): **(a)** the "Who goes first?" preamble is a CARD child (`boty:round-preamble`) that PRECEDES the round card — two modals in sequence, both card-law, no new machinery; its lead-off callout derives from the projected active seat (the K7-v1x D2 law carries). **(b)** the rivals carousel decomposes into a LAYOUT child (`boty:rival-summary` — BOARD_PARENT with local-play/hand SUPPRESSED: a rival's play zones are not shown in v1's carousel) + bench paging chrome (prev/next/index = ui-state, I-51d). **(c)** the cards gallery is bench chrome composing EXISTING card children as tiles; the 6-kind filter set (tradespeople·equipment·jobs·persistent·playable·global) enters as content-tier vocabulary DATA (`CARD_KINDS`), not engine law. **(d)** job/tradesperson/equipment cards: geometry for regions the archived screens show (job rows in 05/06; tradesperson rack card in 05; equipment rack/owned in 05/06) is measured. ~~regions the inventory names but no still captures at card scale (crew-rate variance note, last-review, training) are carried as roled regions~~ — SUPERSEDED per K7-parity D1 (the original claim overstated the code): the TRUTH is — the crew-rate variance note is CARRIED in the `text` region (a documented carrier, not a dedicated role); **last-review, training (§4.4), and the job ladder rung J1–J6 (§4.3) are DEFERRED** — no region exists, pending card-scale source screens; the parity checklist (I-54.3) holds because the deferral is now on the record, never dropped. **(e)** `boty:job-card` is PROMOTED from bench-local to the pack (same id — the bench definition is superseded, not duplicated); bench fills only the regions it has data for (renderLayout placeholder-fills the rest, honest by construction) | benign | local (packs/boty + utilization/bench) |
| I-56 | Books realization (arc step 1; sources 10-books-balance + 16-how-to-play + Report-5 A.2 + inventory §3.4): **(a)** `PANEL_PARENT` (kind 'panel') enters the CERTIFIED vocabulary — a per-player REPORT PANEL is not honestly a card, board, or table; its generic anatomy measured off capture 10: title · mode-tabs · line-items body · total row · footnote. The kind union grows ('card'|'board'|'table'|'panel') — presentation-package touch, K7 mandatory; renderTable untouched, V-9 pin safe. **(b)** `boty:books` = a child of PANEL_PARENT (v1's Books IS the generic panel; the P&L cash-vs-paper CALLOUT is the one BOTY-specific add). ~~geometry measured off capture 18 when it lands; until then the region is ABSENT~~ — **DEFERRAL DISCHARGED 2026-08-01: source 18 (18-books-pnl.jpg) landed and was measured; the `callout` add (role cash-callout, y76 h22, below the total row at ~76–99% of panel height) is now IN the child, shadowed {added:['callout']}, pinned by the updated GBC-62 test. Source 18 also documents two skin-era grammar candidates recorded in SOURCES.md: the red dashed TIMED-EFFECT levy banner and the struck-through+skull ELIMINATION treatment.** **(c)** statement-tabs selection is UI-STATE (presentation-local, no game write — inventory §3.4); the financial INVARIANTS (gross-margin ≡ revenue−COGS · net-income ≡ GM−overhead · assets ≡ liabilities+equity) are ENGINE-side (M13 Ledger law) — the panel only projects them. **(d)** bench fills: Balance-sheet numbers are REAL from the projection (cash · AR sum · AP sum · equity = the identity the reckoning already proves); P&L is a single visibly-bracketed TAB-LABEL placeholder — no P&L rows are rendered at all (wording tightened per K7-books D2; the slice tracks no revenue/COGS breakdown, content-tier concept awaiting the mechanics increments); no invented figures. **R1 (K7-books re-verify, RECORDED GAP with a named trigger):** GD5b cannot falsify popBooks's AR-projection wiring while no drill-reachable action creates a receivable (the M-AR mutant survives at AR ≡ $0) — WHEN the first AR-creating action lands in the game slice (mechanics increments), the GD5b script MUST extend through it and the M-AR kill-test re-runs; until then this sentence is the record | benign | local (presentation/layout.ts + packs/boty + bench) |
| I-57 | Visual regression gate (arc step 2, owner "go ahead" 2026-08-01): **(a) DOM-vs-LAW is the primary assertion** — in real Chromium, WITHIN THE CANONICAL SCENES (the game stage + the books modal; scope-qualified per K7-vg D3, REWORDED per K7-vg D5/strike-4 — the TRUE coverage: showcase = pin only; books modal = DOM-vs-law + pin; the four other popped children (preamble · round-card · rival-summary · fortune) = UNIT-TIER ONLY, no visual-gate coverage; extending domVsLaw + pins to them = the recorded next-touch candidate) every rendered `[data-region]` rect's x/y/w/h attributes must EQUAL its LayoutDef region (the contracts are the law; the renderer is checked against them, not against itself), and every def region must be PRESENT in the DOM (a missing region is drift, not minimalism). **(b) scene pins are COMPUTED, never hand-written** — the vector discipline at the bench tier: canonical DETERMINISTIC scenes (fresh seeded game · overview camera · popups dismissed; the books modal at the fresh state; the static showcase) hash their rendered SVG strings into utilization/bench/visual-pins.json via a --discharge run; every normal run re-derives and compares; a mismatch FAILS the gate; re-pinning happens only in a commit that names the cause (the V-9 re-discharge pattern, bench-tier authority: builder commit, not owner gate — these pin COMPOSITION, not certified law). **(c) pixel-hash REJECTED on the record** — PNG bytes vary across Chromium versions/AA; geometry + SVG-string assertions carry the same information robustly; screenshots are saved as human artifacts each run, never compared. **(d)** camera purity (the camera must PROVABLY MOVE, then re-derive byte-equal — hardened per K7-vg D1 after its first form compared a dead camera to itself; setCamera now refuses unknown presets loudly) and the a11y floor (every region titled, canonical-scene scope) are asserted IN-DOM, not just in unit tests. **(e) the M-E self-verification boundary (K7-vg D2, RECORDED):** a sabotaged comparator passes 5/5 — no gate can prove its own aliveness; the gate's teeth are proven by K7 mutation rounds (M-C/M-D/M-F/M-H/DI all killed live) and the gate is WIRED as `npm run gate:visual` + the operations-pack health check so a green build cannot silently omit it. Gate = utilization tier (visual-gate.mjs); packages/ untouched | benign | local (utilization/bench) |
| I-58 | **RECURRING PATTERN REGISTERED (3rd strike, K7-vg D3): instrument wording overclaiming code structure.** Three consecutive K7 rounds caught register rows asserting structure the code never carried — K7-parity D1 ("carried as roled regions" for absent regions), K7-books D1 ("title override" for a fill), K7-vg D3 ("every rendered region" for a scoped scope + a stale "+18 pending"). This is the register-side twin of the build's signature defect ("closures narrower than their claims"). STANDING CORRECTIVE: before any instruments commit, every STRUCTURAL word in a new/edited row (override · carried · roled · every · all · pending · absent · **last · newest · oldest · everything**) must be grep-falsified against the code by the builder, and the K7 charge inherits the check permanently. **Strikes 5–6 (K7-v7 D2/D3, recorded): "last three draws" (structurally the oldest three — ORDER words join the checklist) and GBC-64's "everything popped = full gate" (the gallery was uncovered; closed by adding it to the gate rather than narrowing the claim). Strike 7 (K7-v7 D1-R): the D1 closure's own wording ("fails the gate by name") was falsified by the reviewer's data-only M-A — the assertion was label-deep; corrective extended: a claimed GUARD must be proven by the killing mutant BEFORE its instrument sentence is written (kill-first, then claim). Strike 8 (K7-v7 seal round): the D1-R closure clause overclaimed the ORDERING leg ("both fail by value") in the very commit recording strike 7 — superseded on the I-59d row with the survivor bound to its >3-card-deck trigger. Strike 9 (K7-3d D3): the ODG-p1 proposal's claim 4 said the gate infrastructure "drives it" PRESENT-TENSE while nothing committed drove the spike — the proposal is a ruled historical doc, corrected HERE not by edit; the claim became TRUE at VG7 (the spike joined the gate: geometry-vs-law, the committed forced-mismatch drill, preset-consumption assertion). Strike 10 (K7-A1 D2): I-62b claimed 'a hardcoded panel diverges and fails' at a FROZEN state where the hardcode class is equivalent-mutant — superseded on I-62b with the kill-test BOUND to A2 (the first state change). Strike 11 (K7-A3 D1): I-69(b) said "fills MIRROR the certified SVG bench … text = the flavor/effect line" — the reviewer grep-falsified it (game.ts:404 sets the modal `text` to a CONSTANT; game3d openOnion renders the card's FLAVOR): title+subtitle mirror, `text` does NOT — closed on the I-69(b) row (reworded to "follow … title+subtitle mirror; text is richer, not a mirror"), the word MIRROR now scoped to the two regions that carry it. Strike 12 (K7-A4, parallel batch-1): I-73 said die.ts "REUSES … camera/onion by import" but die.ts imports camera VIA stage (not camera.ts) and does NOT import onion — it re-implements the modal (the lane forbids editing onion.ts); corrected on the I-73 row.** | benign | global (process) |
| I-59 | SVG maturity to v1 parity (arc step 3, owner "proceed" 2026-08-01; UNSKINNED throughout — D-1 stands, structure not paint): **(a) crew tokens BECOME the tradesperson child live** — the shop crew rack renders each crew member as a mini `boty:tradesperson-card` frame (the promoted child at token scale) with REAL fills (name; status idle/SELECTED/working {venture}); selection/busy = frame-outline treatments (structural, not skin). **(b) jobs-list rows are REAL** — per-assignment "crew ⇒ venture" lines from the registered crew read; empty = "no jobs in queue". **(c) the discard top is a FORTUNE card** — v1's fortune deck discards fortune cards, not job cards (source 05/09); the JOB_CARD discard fill was a v1-extraction-era approximation, superseded. **(d) the hand fan is REDACTION-HONEST** — the VIEWING seat fans ~~its own last three draws~~ its NEWEST three draws (superseded per K7-v7 D2/strike-5: ownDiscard is newest-first, `slice(-3)` selected the OLDEST three — latent only while decks cap at 3 cards; now `slice(0,3)`); rival boards show only the public discard top — more redaction never less (I-48a spirit), and the law is now ASSERTED in-DOM by VG6 in DATA-TRUE form (K7-v7 D1-R closure — the first VG6 was LABEL-DEEP, testing the renderer's own ternary, and the reviewer's data-only M-A survived it, I-58 strike 7): rendered-card ids are compared BY VALUE against the __GAME__.handTruth() projection surface (own ≡ newest-three ownDiscard; each rival ≡ [public top] or zero cards) — ~~the leak class and the ordering class both fail by value now~~ (I-58 strike 8: this clause overclaimed the ordering leg in the very commit recording the kill-first corrective): the LEAK class is KILL-PROVEN (data-only M-A dies by value); the ORDERING comparison is order-sensitive (JSON id-array) but the ordering mutant (slice(-3) at the fan site) is a **RECORDED SURVIVOR** while every deck caps at ≤3 cards (slice(-3) ≡ slice(0,3) for arrays ≤3, pack.ts decks 3/2+1/3) — the kill-test BINDS to the first >3-card deck (mechanics increments; the M-AR pattern: this sentence is the record and the trigger). **(e) dice = a labeled placeholder frame** — the BOTY slice has no die verb; the region renders "[dice — no die verb in the slice]", asserting nothing false. **(f) equipment rack stays bracketed** until the mechanics increments (no invented actions). **(g) the I-57 next-touch candidates DISCHARGE** — all FIVE popped children now get DOM-vs-law + computed pins + in-modal title checks (VG3/modal:* per child); the four uncovered modals join the gate. **(h) scene pins re-discharged** with this row as the named cause (composition changed by a–e; the re-pin discipline of I-57b) | benign | local (utilization/bench) |
| I-60 | The 3D spike (arc step 4, owner "go" 2026-08-02): a bench-tier EXHIBIT, not a realization change — **ODG-p1 remains 2.5D-SVG law until the owner rules on the written proposal** (governance/ODG-p1-3D-proposal.md). The spike's charter: prove the LAYOUT CONTRACTS and the THEATER LAW survive a renderer swap UNCHANGED — (a) card/board meshes are built FROM LayoutDef regions (the same unit space; physical aspect = realization freedom per I-48b); (b) the flip animation runs UNDER HK-11 — REWIRED per K7-3d D2 (the first form discarded the FlourishInstance and asserted displayed=seeded, a tautology): the instance returned by beginFlourish is KEPT and carries the seed into completeFlourish; the displayed value is READ FROM THE DISPLAYED OBJECT (the card face's fill on its userData), never asserted; TRUTH WINS proven by the COMMITTED VG7c forced-mismatch drill (displayed 'wrong-card' → ok:false, result = the seed — kill-first honored); (c) the camera consumes the SAME focusPresets data (cx/cy/zoom → orbit target/distance — a pure mapping, stateless toward the game); (d) three.js enters as a devDependency BUNDLED at build (no runtime network); (e) the spike reads defs only — no engine, no projection, no state (an exhibit of vocabulary, like the showcase); (f) headless-WebGL viability is a DATUM the spike measures for the proposal (CI risk), not an assumption. **(g) K7-3d re-verify closures (kills run BEFORE these sentences):** the displayed value is read from the RENDERED title label's own stamp (label() records the text it was ASKED to draw — the pixel-tier survivor (delete fillText, stamp intact) is the recorded I-57c boundary; the mirror class M-MIRROR now dies at VG7b — proven: displayed '[title]' ≠ seed); VG7d asserts on SEAT-0 (whose mapped position differs from the default on every axis — table's cx equals the world center, which let the dead-camera M-C-noop pass; proven killed: 14/15) with x/y/z re-derived from preset data, provable-move (I-57d carried into 3D), and purity; BOUNDARY recorded: a byte-identical hardcoded copy of the same mapping survives — the accepted equivalent-mutant class (K7-v1x precedent); VG7 waits fail NAMED on timeout, never crash the gate | benign | local (utilization/bench spike + proposal doc) |
| I-61 | 3D adoption (ODG-p1 superseded, owner-ruled 2026-08-02): **(a) dual realization** — the 3D bench is the BUILD TARGET; the SVG bench stays the CERTIFIED REFERENCE (V-9 pin + VG gates keep running; nothing certified is abandoned — RSI: extension, not rewrite). **(b) the object-by-object program** is governed by governance/3D-ROADMAP.md — the COMPRESSION-SURVIVING worklist (the owner's insight made law: the repo carries the program, the conversation only executes it); every 3D object increment follows the standing rhythm (instruments → build → gates → K7) and consumes LayoutDefs as its sole geometry source (the I-60a charter carries). **(c) 3D gate discipline**: geometry assertions (mesh transforms vs law — the VG1 pattern in 3D) + state-waiting (headless rAF throttling measured at I-60f — never clock-waits); pixel-hash stays rejected (I-57c). **(d) the 10-game corpus DEFERRED** to after the draft-parents phase — recorded, never dropped (I-54 docket updated by reference). **(e) draft-parents phase**: after BOTY-in-3D is whole, the parent vocabulary gets a DRAFT 3D representation review object-by-object — vocabulary changes stay DATA-first (I-50 law); mesh builders adapt to data, never the reverse | benign | local (utilization/bench + governance/3D-ROADMAP.md) |
| I-62 | Roadmap A1 — the 3D STAGE (owner "go A1" 2026-08-02): **(a) engine-bound through the SAME doors** — game3d hosts a LockstepController and reads ONLY project() + the I-52-registered raw classes (controller.row().moves for the log; PLUS controller.stateHash() on the gate surface — the same bench-owned display-only class as the sealed SVG surface, NAMED per K7-A1 D5; no crew read at A1 — standings show cash only, view-carried); NO verbs at A1 (the stage renders and moves the camera; actions arrive at A2+); the spike stays a FROZEN exhibit (game3d duplicates its mesh builders rather than mutating a sealed increment — supersession target when A-rows converge). **(b) fills are STAMPED-AT-DRAW** — panel textures record the lines they were ASKED to draw (userData.renderedLines; the asked-text class with the I-57c pixel-tier boundary, per I-60g precedent); VG8b compares the stamp against an expectation the GATE derives from the projection surface. ~~a hardcoded panel diverges and fails~~ — SUPERSEDED per K7-A1 D2 (I-58 strike 10): at A1's FROZEN canonical state (no verbs → the projection never changes) a hardcode matching that one state is an EQUIVALENT MUTANT and survives (M4a/M4b proven); the RECORDED TRIGGER: at A2 (the first state-advancing increment) the gate MUST re-check VG8b/VG8d after a draw and re-run the hardcode kill-tests — after one state change the frozen-hardcode class dies; this sentence is the record. **(c) the camera GLIDES** — animated application of the SAME preset mapping (lerp toward the mapped position; purity asserted AT REST: glide-complete position ≡ the law, provable-move + re-glide equality; wheel dolly = 'custom' camera, outside the preset law by declaration); click-to-focus raycasts boards → seat presets, felt → table. **(d) VG8 coverage lands WITH the increment, kill-first** — EXTENDED per K7-A1 D1: VG8e drives the REAL input paths (preset button click, raycast board click, wheel dolly, the unknown-preset refusal) — dead handlers proven fatal (M5 killed: board-click/wheel stayed 'overview' → named fail); the glide epsilon is 1e-9 (K7-A1 D3 — rest is an exact copy of the mapped target; the +0.15 drift mutant proven dead); glide waits fail NAMED on timeout (D4). BOUNDARY recorded: VG8a's expectation is computed by the page under test (defs are TS-only, not gate-importable) — the def-side self-attestation class, accepted with M1 (region-skip) as its live falsifier | benign | local (utilization/bench game3d + visual-gate) |
| I-63 | READ VIEW (A1b; the owner's A1 playtest ruling 2026-08-02: "the flat view gives the best view of data … for the tabletop view it's flat overhead, for the seats it's 90 degrees to the table" + scroll when content outruns the frame): **(a) two camera MODES per focus target** — 'scene' (the existing perspective glide) and 'read' (FLAT: table = directly overhead looking straight down; a seat board = face-on along the board's normal); read is a SECOND PURE MAPPING of the same focus target (the preset names the WHAT; the mode names the HOW — no new preset data enters). **(b) FIT-TO-FRAME is law in read mode** — the camera distance derives from the OBJECT'S bounding box vs the viewport frustum so the whole object is visible (the owner's no-crop requirement); the gate asserts the PROPERTY (every bbox corner inside NDC), never a duplicated formula. **(c) PAN-SCROLL** — in read mode, pointer drag translates the camera in the view plane (orientation UNCHANGED — asserted) with the look point CLAMPED to the object's bounds (you cannot scroll the object away); ~~wheel keeps dollying~~ (SUPERSEDED → I-66c: read-mode wheel no longer dollies — zoom-in disabled, zoom-out steps the ladder); re-toggling read RESETS pan (fit is the pure rest state — purity asserted on re-toggle). **(d) cross-device intent recorded**: pointer events (mouse+touch); read mode is the DATA-READING surface for phones/tablets/PCs and is the designated MODAL-AS-CARD camera for A3+ (a zoomed card = read view of a card). **(e) VG8f lands kill-first — KILLS RUN AND RECORDED before this row's commit**: dead read-toggle → VG8f/read-view-law FAILS (overhead:false fit:false pan-clamped:false) AND VG8f/read-board-face-on FAILS; fit×0.4 crop → BOTH VG8f checks FAIL (fit:false); unclamped pan → VG8f/read-view-law FAILS (pan-clamped:false); baseline restored → 22/22. **(f) corner-true fit — the gate's own first catch**: the first fit derivation used world-AABB half-extents as if they lay in the view plane; the TILTED seat board's near corners projected outside NDC and VG8f/read-board-face-on failed (fit:false, 21/22) BEFORE any commit. The fit now derives the distance from all eight box corners, each at its own depth in the camera basis. This is (b) working as designed — the gate asserting the PROPERTY, not a duplicated formula, is exactly what caught the formula being wrong. **(g) K7-A1b D1+D2 closure — the read pose and interaction semantics, registered and ASSERTED (kills run before these sentences):** ~~(g1) DEFAULT FOCUS — toggling read from a seat camera reads THAT board; from any non-seat camera ('overview', 'table', 'custom') the table is the default read target (the data-densest surface — the owner's "flat view gives the best view of data").~~ **SUPERSEDED → I-66a: the read toggle's default target is the ANCHOR (lastFocus) — under the ladder the anchor, not the camera pose, names what the player is reading.** (g2) POINTER SEMANTICS (supersedes the pointerdown raycast of the A1 stage — I-62c click-to-focus now fires on POINTERUP): a gesture that moved more than 4px is a DRAG and never refocuses (in read mode it pans; in scene mode it is discarded); a clean click refocuses in scene mode ONLY — read-mode clicks do nothing (pan and the mode button are read mode's verbs; VG8e still proves the scene-mode click path live). ~~(g3) WHEEL-IN-READ — the dolly stays in read MODE but names the camera 'custom' (the same out-of-law declaration as I-62c wheel; `${focus}:read` names only the PURE fit pose).~~ **SUPERSEDED → I-66c: no read-mode wheel dolly exists; the wheel in read mode only steps the ladder.** (g4) READ POSE FULLY PINNED — the gate asserts CENTERED (the focus bbox center on the optical axis, NDC origin ±1e-6) and SCREEN ORIENTATION (overhead read: layout-up = world −z maps to screen-up; board read: world +y maps to screen-up), both via an independent ndcOf probe; KILLS: up-vector sabotage (0,0,−1)→(1,0,0) → oriented:false; look-at-corner → centered:false on BOTH legs; the framed≥0.5 floor now guards BOTH legs (K7 D4) — the all-zero corner lie → framed:false on both. **(h) RECORDED SURVIVOR (K7-A1b D3, the I-59d pattern):** the depth-blind fitAlong mutant (drop the per-corner `on +` depth term) SURVIVES as equivalent-under-current-geometry — every read target is face-on, so corner depth spread ≈ region z-offsets (≤ ~0.6 world units), absorbed by the 1.06 margin. TRIGGER: the FIRST THICK read target (the A2 deck stack is the expected first) must re-run this kill — after a read target with real depth exists, the depth-blind mutant dies or the fit law is re-derived; this sentence is the record. **TRIGGER FIRED AT A2 (2026-08-02): the kill was re-run against the 3-card deck stack — the mutant STILL SURVIVES (35/35): the stack's depth is ~0.4 world units, far inside the 1.06 fit margin; the survivor remains equivalent-under-current-geometry. The trigger RE-EXTENDS: the first read target with depth commensurate to its face (a tall stack, the A10 ledger opened, or the A15 box) re-runs this kill — this sentence is the extended record** | benign | local (utilization/bench game3d + visual-gate) |
| I-64 | THE BOUNDED ZOOM CONTINUUM (A1c; owner-ruled 2026-08-02 with same-day amendment): **(a) the wheel is BOUNDED at both ends by READ POSES** — zooming IN past the focus object's fit pose transitions ORGANICALLY into that object's read view; ~~zooming OUT past the out-bound transitions to the TABLE read view (the amendment: the out endpoint is table READ, not the overview scene preset — seat-read → wheel-out → table-read is the flow)~~ **SUPERSEDED 2026-08-02 by the owner's A1c playtest ruling (I-66 ladder): read zoom-out goes to the ANCHOR'S SCENE view; table read is reached from OVERVIEW zoom-out.** **(b) in-bound rule**: scene-mode wheel-in dollies until the camera distance reaches the focus's read-fit distance; crossing it fires readView(focus) — the glide carries the camera to the exact fit pose (read entry is ALWAYS the pure fit pose; the pre-A1c "toggle from a deep dolly" distortion class is structurally gone). ~~**(c) out-bound rule**: in a non-table read, wheel-out first stops AT the fit pose (the wall), and the next out-notch from the wall fires readView('table') — the transition is DIRECT (the mode never drops back to scene on the way); in scene mode, wheel-out past OUT_FACTOR × the overview distance fires readView('table'); in table read, wheel-out CLAMPS at the table fit pose (the continuum's far wall).~~ **SUPERSEDED → I-66b (the ladder: read-out → anchor scene · scene-out → overview · overview-out → table read · table-read-out is the terminal no-op).** ~~**(d) read-mode wheel-in floor**: dolly toward the look point clamps at IN_FLOOR × fit — the owner's resolution-cap rationale made law (skins never need more resolution than this closest legal camera); IN_FLOOR=0.3 and OUT_FACTOR=1.15 are REALIZATION TUNING (I-48b freedom, owner-tunable data constants), not law — the LAW is that both walls exist and both transitions fire.~~ **SUPERSEDED → I-66c: in read view zoom-IN is DISABLED entirely (the fit pose IS the closest legal camera — the resolution cap survives, stricter).** **(e) focus memory**: the wheel keeps the last focused object (lastFocus; 'table' at start) so the in-bound targets what the player was looking at; seat/table presets, board clicks, and read entry set it — the overview preset leaves it unchanged (K7-A1c D6a). **(f) VG8h kill-first — KILLS RUN AND RECORDED before this row's commit**: dead organic entry → organic-in:false; pre-amendment endpoint (seat-read wheel-out detouring through scene) → direct:false — NOTE this mutant SURVIVED the gate's first endpoint check (an eventual-arrival check tolerated the scene detour) and the gate was TIGHTENED to assert directness before the kill; unwalled floor → floor:false; baseline restored → 28/28. **(g) K7-A1c D1 closure — the SCENE far wall's falsifier (kills run before this sentence):** VG8h/wheel-scene-far-wall drives real wheel-out from the overview scene pose until table read (named fail on exhaustion); the scene-wall-deleted mutant (M-K2) → scene-out-to-table:false. Every clause of (c) now has a live falsifier | benign | local (utilization/bench game3d + visual-gate) |
| I-65 | THE SIX-SEAT STAGE (A1c; owner-ruled 2026-08-02: "BOTY has up to 6 players … 3 more seats on the other side of the table positioned opposite the first 3"): **(a) two-sided preset law (ADDITIVE)** — focusPresets for seatCount>3 places seats 0-2 on the near row (the existing formula over min(n,3)) and seats 3+ on the FAR row (cy = h×0.18, cx by the same spread formula over its own row — AT THE SHIPPED 6-UP CASE seat 3 shares seat 0's cx, directly opposite (K7-A1c D3: for seatCount 4-5 the far row spreads over its own count and is NOT opposite-aligned — a 4th/5th-seat arrangement is UNRULED, recorded, awaiting need); seatCount≤3 output is UNCHANGED (the certified SVG behavior intact by construction — the F6 pin `focusPresets(3)` keys is untouched). **(b) side-aware seat approach** — the 3D preset mapping approaches a SEAT from that seat's own side of the table (far-row seats: pos.z = look.z − d×0.7; near-row unchanged); NON-seat presets (table/overview) keep the canonical near-side approach — the certified A1 camera law is a special case, not superseded. **(c) board backs are SHOP-GRAPHIC ONLY** — each seat board gets a BACK face (π about Y) rendering the shop identity alone (the shop name + an '[shop art]' placeholder until skins); NO data regions on the back (redaction-consistent: counters/data face the owning player; the table sees WHICH shop, never its numbers); the back mesh stamps userData.renderedLines like every panel (the asked-text class). **(d) ORBITAL GLIDE** — the camera glide interpolates AROUND the table (cylindrical: azimuth about the world center by the SHORTEST ARC, radius and height lerped; the look lerps linearly); crossing to the far side is the owner's "smooth rotation 180 degrees around the table" — a SIDE-CROSSING glide never flies over or through the table (overhead READ transitions necessarily pass above — they ARE the overhead approach, K7-A1c D6b); near-axis transitions (either endpoint's horizontal radius < 80: the overhead read poses) fall back to the straight lerp, where azimuth is noise; rest purity (VG8c 1e-9) is unchanged — the PATH changed, the DESTINATION law did not. **(e) six-shop DRAFT content** — BOTY_PACK6/botyGenesis6 as a SEPARATE exhibit variant (the certified 3-seat slice untouched; every existing pin safe by construction): three DRAFT far-side shops reusing existing fx-less cards only (content adds NO law); names/trades are PLACEHOLDER pending the owner's naming ruling — renaming is a data edit, not a supersession. The 3D bench hosts the 6-seat variant; the SVG certified reference stays on the 3-seat slice. **(f) VG8g kill-first — KILLS RUN AND RECORDED before this row's commit**: straight-line glide (orbital branch dead) → minR 40 vs wall 647; wrong-side approach (side always +1) → far-law:false; far-row presets dead (all six near, mutated in packages/presentation) → far-law:false; back leaks data ($cash on the back) → all six boards LEAK; far board unrotated → face-on dot 0.877583 (want −1); baseline restored → 28/28. **(g) K7-A1c closures:** (D2) the back must FACE backward — backFacingDot surface (back world-normal · board front normal ≈ −1, all six) joined VG8g/backs-shop-graphic-only; the unrotated-invisible-back mutant (M-K4) → opposed:false (dots 1.000×6); the stamp alone could not see orientation. ~~(D4) RECORDED SURVIVOR: deleting the shortest-arc normalization survives — every current rest-to-rest transition has |raw arc| ≤ ~180.4°, making the mutant equivalent-under-current-geometry; TRIGGER: the first transition class exceeding a half-turn (e.g. glides started mid-glide) re-runs this kill.~~ **SUPERSEDED (K7-A1c' — the recorded measurement was FALSE):** seat-0→seat-3 has a RAW arc of ~234.9° (preset azimuths ±117.44°) — a one-click transition ALREADY over a half-turn at ship; the normalization-deleted mutant swept the LONG way (−234.9°) gate-blind. The clause now has its OWN falsifier: VG8g/orbital-shortest-arc accumulates the swept azimuth on seat-0→seat-3 and walls it at 190° (lawful short arc ~125°); the mutant dies by name (swept −234.9° vs wall 190°) — kill run before this sentence. (D5) RECORDED: far-board world placement (−z) is asserted only collaterally (VG8e raycast occlusion); TRIGGER: the first A2+ increment touching board geometry adds a placement surface | benign | local (packages/presentation focusPresets + packs/boty pack6 + utilization/bench game3d + visual-gate) |
| I-66 | THE ZOOM LADDER + REGION ANCHORS (A1d; the owner's A1c playtest ruling 2026-08-02, exact walk in the RESOLUTION_RECORD): **(a) the ANCHOR** — the last-clicked/last-focused object (a seat board, the table, or a TABLE REGION); clicking RE-ANCHORS; the anchor survives the whole ladder walk (zooming out to table read and back in returns to the SAME anchor's scene). **(b) the LADDER** — one wheel axis, four rungs: anchor-READ ↔ anchor-SCENE ↔ OVERVIEW-scene ↔ TABLE-READ. Transitions: in anchor read, zoom-out → the anchor's SCENE view (a region anchor's scene = the table preset); in anchor scene, zoom-in dollies to the anchor's fit and crosses ORGANICALLY into its read (I-64b carries), zoom-out dollies out and past the overview distance glides to OVERVIEW; at overview, zoom-in → the anchor's scene preset, zoom-out → TABLE READ; at table read, zoom-in → OVERVIEW, zoom-out → no-op (the far terminal). **(c) read zoom-in is DISABLED** — read is the fit pose plus pan only; IN READ MODE no dolly below fit exists (the owner's resolution cap, now stricter than I-64d ever was). K7-A1d D4 boundary: the cap is PER-ANCHOR — scene-mode dolly legitimately passes below a LARGER object's fit while descending toward a smaller anchor's read (the organic region entry drives exactly that path); and the mode BUTTON at table read returns to the TABLE's scene view (sceneView targets the read focus) while the anchor stays whatever it was — the button toggles the READ TARGET's realization, never the anchor. **(d) TABLE REGIONS ARE ANCHORS** — every region of the table def (data-driven: deck · discard · dice · windows · global-play · standings · log · art-banner) is clickable in scene mode; clicking one glides to the table preset AND anchors `table:<region>`; zoom-in from there reads THAT REGION (overhead fit of the region's own bounds — same read law, smaller box); read of a region pans/clamps within the region. **(e) surfaces**: focus resolution handles `table:<region>` everywhere a focus names an object (read mapping, pan clamp, corners/center, fit); regionScreenXY drives the gate's real clicks. **(f)** placements ruled for LATER rows recorded at A7/A9/A10 in the roadmap (ledger flat-in-front flip-open · hand face-down right of ledger · cards-in-play pending the owner's A7 ruling) — nothing built ahead of its row. **(g) VG8h/VG8i kill-first — KILLS RUN AND RECORDED before this row's commit**: read zoom-in re-enabled → in-disabled:false; read-out hopping to table read (the superseded A1c behavior) → out-to-anchor-scene:false (table:read); ladder re-anchoring at table read → anchor-held:false AND overview-in-to-anchor:false (table); region click not anchoring → VG8i click-anchored:false (table); region focus resolving the WHOLE table → VG8i click-anchored:false (table:dice — the broken resolution even mis-aimed the gate's own click, caught twice over); baseline restored → 30/30. **(h) K7-A1d D1+D2 closure (kills run before these sentences):** VG8i now asserts the REGION leg of I-66c (three in-notches at region read move NOTHING, 1e-9) and I-66d's pan law (panProbe → look INSIDE the region box, orientation stable 1e-9); K7's surviving mutants re-killed by name — region-read zoom-in leaking to overview → in-disabled:false; pan clamped to the WHOLE table box → pan-clamped:false. The dead readFit surface removed; the table-read status text names the real gesture (wheel IN for overview) | benign | local (utilization/bench game3d + visual-gate) |
| I-67 | A2 — DECK + DRAW + THE READING BOARD (owner "go" 2026-08-02 on the presented scope): **(a) COUNT-TRUE STACKS** — the deck object renders ONE REAL CARD MESH per card in the ACTIVE seat's projected drawCount (shared back texture; a face texture exists only when revealed); **A2b (the owner's playtest report 2026-08-02, "the decks not visible"): a card is a THIN BOX with edge lines (CARD_T=0.9 world units, realization tuning under I-48b), so the pile has PHYSICAL height — paper planes foreshortened to a sliver at oblique angles; count-true is now also HEIGHT-TRUE (the gate asserts top-minus-bottom ≥ (n−1)×0.8; the flattened mutant dies height-true:false Δy 0.18 — kill run before this sentence);** the discard renders the VIEWING seat's ownDiscard (redaction-honest: that is the only discard whose contents the projection grants; a rival's discard would show discardTop only — the A2 bench shows the viewer's). The geometry IS the count — no painted number. **(b) THE READING BOARD ('onion', owner-ruled)** — a camera-parented overlay group (outside the preset camera law by the same declaration as I-51d chrome): dark translucent surround (opacity ≈0.55) + the card centered; opens at draw-flight completion; any click ON THE STAGE CANVAS closes it (the pointerup is CONSUMED — no raycast beneath; a drag-release also closes — recorded; BAR buttons act beneath the open board by design — chrome is outside the scene, the I-51d class); the ladder runs unchanged beneath it. **(c) DRAW THEATER** — the verb goes through the same doors (emit('draw', active, {deck:active}) + controller.submit); the SEEDED result is the post-draw projected discardTop; the top deck card flies + flips to the camera (rAF animation; GATES WAIT ON drawPhase STATE, never clocks — I-60f); at completion HK-11 fires via completeFlourish(displayed-stamp, seeded): on mismatch the verdict FLAGS and TRUTH WINS — the onion re-stamps the seeded card and says so; a committed forced-mismatch drill (forceFlipMismatch) keeps the path falsifiable (the VG7d precedent). **(d) DECK CLICK SEMANTICS** — a click on the deck ANCHORS (I-66d carries) AND fires the draw when it is the VIEWING seat's turn (the A2 bench draws only as the viewer — you touch your own deck; other seats advance by end-turn; hotseat cross-seat play is a later row); when it is NOT the viewer's turn the click steps the deck FIDGET instead; discard clicks anchor + step its fidget always. The gate's pure-anchor exemplar (VG8i) moved from deck to the LOG region — the deck click now carries a verb. **(e) FIDGET = PURE THEATER (owner's grammar)** — deck: top-5 loose → re-scatter → neat; discard: peek upright → spread last 5 → neat; offsets come from a SEEDED LCG keyed on (fidget state, card index) — deterministic, so 'neat' restores EXACT positions; the law surface: rowHash AND moveCount are INVARIANT across any fidget click (asserted); seat + table never fidget. **(f) END-TURN AFFORDANCE** — a bar button submits the engine's end-turn verb; required by the I-62b trigger (the frozen-standings hardcode class only dies when the ★ moves — a draw alone changes log+deck but not the ranking text). **(g) REBUILD-ON-STATE-CHANGE** — every successful verb rebuilds the scene groups from a fresh projection (fills re-derive; the stacks shrink/grow the moment the state does). **(h) THE MANIFESTED BACKDROP (prototype for the owner's ruling)** — a ground disc with a radial gradient (white → pastel haze → faint ink ring → off-white) + scene background matched to the outer tone; zero lights (the diffuse-only law conforms); the owner rules on the LOOK — style tokens wait for the skin era. **(i) STANDING OBLIGATIONS DISCHARGED AT THIS ROW** — VG8b/VG8d expectations re-checked AFTER a draw and an end-turn; the I-62b frozen-state hardcode kills re-run (they must DIE now); rowHash/moveCount consumed by the gate across the draw; the I-63h depth-blind fit re-kill re-run at the first thick read target (the deck stack) with the outcome recorded either way. **(j) WINDOWS honesty note** — drawing the third card (crossroads) opens a real engine window; A2 renders the projection's open-window count in the windows region as text (nothing false) and the decide verb arrives at A8; until then the engine's own refusals (HK-5) speak. **(k) VG8j kill-first — KILLS RUN AND RECORDED before this row's commit**: stack-count lie (deck constant 3) → draw-theater deck 3→3 FAILS + state-change-recheck FAILS; dead HK-11 (verdict fabricated null) → forced-mismatch flagged:false FAILS; truth-loses (onion stamps the displayed lie) → forced-mismatch shown:WRONG-CARD FAILS; fidget-mutates-state (a discard fidget click also draws) → fidget-pure rowHash+moves-invariant:false FAILS; neat-never-restores (seed drifts per rebuild) → neat-restored-exact:false FAILS; **the I-62b OBLIGATION DISCHARGED**: the frozen-panel hardcodes (M4a standings · M4b log) — equivalent mutants at A1's frozen state, recorded then with THIS trigger — now DIE by name after one real draw + end-turn (standings-rederived:false · log:false). Baseline restored → 35/35. **(l) K7-A2 closure (D1–D6; kills run before these sentences):** ~~(D1) ONE THEATER AT A TIME — a deck click while a flight is in the air is IGNORED with a status line (the begun flourish always reaches its completion verdict; no orphan flight mesh can exist); the gate double-clicks the deck mid-flight and asserts exactly ONE draw — the guard-removed mutant died (moves 0→2, deck 3→1, own-discard 3 by the walk's end — three checks failed by name).~~ **SUPERSEDED (K7-A2 D7 — the kill sentence was TIMING-FLAKY, not reproducible from the committed artifact):** the gate's two clicks landed in the SAME frame — after the first click's rebuild, the new table group's matrixWorld was stale, so the second raycast missed with or without the guard (a vacuous falsifier; the builder's original kill run happened to slip a frame). THE RE-CUT LEG waits on STATE (drawPhase ≠ 'idle') PLUS one rendered frame before the second click — never clocks (I-60f) — and the kill is now DETERMINISTIC: the guard-removed mutant fails 32/35 by name (draw-theater moves 0→2 deck 3→1 · forced-mismatch seeded:crossroads · state-change-recheck own-discard:3) in two consecutive runs; the guard itself (game3d doDraw idle-check) is unchanged and live-proven. This sentence is the corrected record. (D2) the close-click consumption has its falsifier — the gate closes the board by clicking the DECK'S OWN screen position and asserts no draw/fidget/hash side effect; the fall-through mutant died (close-consumed:false). (D3) the ownDiscard source has its falsifier — count 2 + top 'new-van' asserted after the second draw AND HELD INVARIANT across end-turn (the viewer's discard does not change when the active seat does); the active's-public-top mutant died (own-discard:1 · own-discard-held:false). (D5) onionVerdict clears on close — no stale verdict behind open:false. (D6) VG8j's table glide-rest fails NAMED | benign | local (utilization/bench game3d + visual-gate + packs untouched) |
| I-43 | RTM presets add NO intent and NO vocabulary: subcontract-debt = venture:route args w/ carried debt; commission-now = route args + an upfront EFX pay descriptor applied by content THROUGH the engine; deferred-referral = windowed first (I-36/I-38 family). The engine path is unchanged | benign | local (patterns RTM family) |
| I-44 | V-4 computation point: from preset-built fragments exercised through the engine (the GBC-44 floor grows to the full catalog sweep at discharge); pinned ONLY at the owner's R gate after the F7 K7. AP-survives (closing default) is the F5 status quo — reckon never touches debts — recorded as the inventory's 'outrunning vendors is legitimate' carried faithfully | benign | local (vectors + patterns) |
| I-19 | Gating is ENGINE-RESERVED: every content window gates (S-8); the `gated` field exists for future engine-opened advisory windows (F5); content declaring it → load refusal | benign (ext-audit-2 F2-R2-5) | local (effects.ts/packloader.ts) |
| I-68 | Windows path portability for baseline verification (owner-authorized 2026-08-02, "authorize the portability fix"): `run-target-check.mjs` computed `ROOT = new URL('.', import.meta.url).pathname`, which on **win32 under a path WITH SPACES** yields a leading-slash percent-encoded string (`/C:/Users/17783/CPA%20CMA%20Services/…`) — `%20` ≠ space, so `existsSync` fails, the bench server 404s, and Chromium throws `ERR_HTTP_RESPONSE_CODE_FAILURE` on `verify.html`. FIXED to `fileURLToPath(new URL('.', import.meta.url))` — the EXACT idiom `visual-gate.mjs` line 16 already uses (that runner was never broken). ZERO change to what the gate serves or asserts — a pure realization-tier portability fix, no pin, vector, or law touched. **Excursion recorded per ARCHITECTURE.md** (a gate runner outside the normal increment lane — this row names the file and why). Sibling note: both gate runners default `PLAYWRIGHT_CHROMIUM_PATH` to the Linux `/opt/pw-browsers/chromium`; `playwright-core` bundles no browser, so on this box the env var is pointed at system Chrome. The V-9 / VG pins were COMPUTED on Linux Chromium and are reproduced here on Windows Chrome — sound by I-57c (geometry + SVG-string assertions are version-robust; pixel hashes were rejected precisely for this). Any divergence would FAIL loudly (never silently re-pin — I-57b: re-pinning is an owner-gated commit naming the cause) | benign | local (utilization/bench/run-target-check.mjs) + ARCHITECTURE excursion |
| I-69 | A3 — THE FORTUNE/CHARACTER CARD IN 3D (owner "go on A3" 2026-08-02): **(a) the reading board presents the FORTUNE ANATOMY, not A2's text panel** — the drawn card pops as `boty:fortune-card`'s measured child (art · title · subtitle · text · payout) built by the spike-proven `card()` (front = layoutFace(FORTUNE_CARD); back = CARD_BACK_PARENT flipped π about Y) — v1's modal-as-card (I-51a), a CARD CHILD at a camera focus, never a dialog; the A2 onion text panel is SUPERSEDED. **(b) fills follow the certified SVG bench's fortune modal** (game.ts:404): title = the seeded card and subtitle 'Fortune' MIRROR it exactly; `text` renders the card's OWN flavor line (packs/boty pack flavor, e.g. 'a brake job walks in') — RICHER than game.ts's CONSTANT 'the card takes effect through the engine', and NOT a mirror of it (**D1 closure, K7-A3 = I-58 strike 11**: the first draft said "fills MIRROR … text = the flavor/effect line", which overclaimed parity the `text` region does not carry; VG8k asserts only text.length ≥ 1, so this is benign — the flavor line is richer/honest, not wrong); art + payout stay HONEST PLACEHOLDERS (D-1 — the slice carries no art asset or payout figure; nothing invented). **(c) the flip reveals a FORTUNE face** — the flight's mid-flip texture's art band + title/subtitle boxes are read FROM FORTUNE_CARD.regions (I-60a: the def is the SOLE geometry source), so the flip reveals an art-dominant card, not bare text; under the forced-mismatch drill the flying card shows the LIE and the reading board shows the TRUTH (R-20 carried from A2/I-67c — verdict.result = the seeded id). **(d) the onionState stamp is PRESERVED** — renderedLines[0] = the shown (truth-wins) card id, so A2's HK-11 gate (VG8j/draw-theater-hk11 + forced-mismatch-truth-wins) reads the verdict UNCHANGED; the anatomy rides UNDER the stamp, the A2 contract intact (36/36 with the fortune card, both HK-11 legs still green). **(e) ART-DOMINANCE is a RENDERED property (VG8k, I-57a)** — the gate reads the onion's rendered region bboxes: art.h > title.h AND artFrac ∈ [0.55, 0.70] (the EXT-5 F1 band that RETURNED the 52% under-encoding, now carried into 3D — measured 0.656 rendered); the band VALUES are DEF-ATTESTED (the I-57a self-attestation boundary — "defs are TS-only, not gate-importable"), with the region-skip mutant (M1) as the live falsifier — NOT a fresh live guard on the fraction, recorded as such per the I-58 no-overclaim corrective. **(f) VG8k kill-first — KILLS RUN AND RECORDED before this row's commit** (the null-guard makes each fail BY NAME, gate COMPLETES, never crashes — the first M1 cut crashed on an unguarded `art.h` and was hardened before this sentence, I-58 kill-first honored): anatomy-absent (drop the front face) → VG8k pass:false regions:[] (35/36); drop the back → front/back:false (35/36); wrong title fill (→'DECOY') → fills:false (35/36); baseline restored → 36/36 (VG8k pass: art/title 39.6>4.2 · artFrac 0.656 · front/back:true · title=['job-posting'] sub=['Fortune']). Full local baseline: ci 279/279 · gate:visual 36/36 · gate:target 21/21·11/11 | benign | local (utilization/bench game3d + visual-gate; packs/presentation UNTOUCHED — the FORTUNE_CARD def + the V-9 pin safe by construction) |
| I-70 | A3b — THE READING CARD RENDERS OPAQUE (owner playtest of A3, 2026-08-02: "the card isn't supposed to be transparent … the area OUTSIDE the card is the correct opaque … the card box isn't opaque"): **(a) the bug** — A3's onion card used OPAQUE materials (transparent:false); three.js renders the ENTIRE opaque pass BEFORE the transparent pass, so the opaque card drew FIRST and the 55%-transparent veil then painted OVER it — the card read as 55%-darkened ("transparent") while the surround read correct. **(b) the fix (the veil is UNCHANGED — owner: the surround is correct)** — the card faces move INTO the transparent pass at FULL opacity (transparent:true, opacity:1, depthWrite:false) at renderOrder 92 ABOVE the veil's 90; the card sorts AFTER the veil and fully covers it; depthTest stays off so the whole onion still floats above the 3D scene; the reverse-painter z-sort keeps the card's own regions layered (base behind, fills in front). VISUALLY CONFIRMED in-browser: the reading card renders SOLID over the darkened table, and the flight card too (art-dominant, opaque). **(c) VG8l/fortune-opaque kill-first (material/order STATE, never pixels — I-57c)** — the gate reads the onion card's paint state: every face opacity 1 (minOpacity) AND transparent-pass (transparentPass) AND renderOrder above the veil (over-veil). KILLS RUN AND RECORDED before this row: opacity→0.5 → opaque:false minOpacity 0.5 (36/37); transparent→false (the reported bug, card back in the opaque pass) → opaque:false transparentPass:false (36/37); renderOrder→89 ≤ veil 90 → over-veil:false (36/37); baseline → 37/37 (opaque:true · transparent-pass:true · over-veil:true 92>90). A3's VG8k (anatomy) UNAFFECTED — geometry/fills unchanged (36→37, VG8k still green). **(d) distinct K7-A3b PASS (worst dim 8), three MINORS closed on the record:** D1 — "the veil is UNCHANGED" refined: its material/opacity(0.55)/renderOrder(90)/geometry are byte-unchanged (the RENDER is unchanged), but it gains an INERT `userData.veil` tag the gate reads to find the veil's live order (non-visual metadata, not a hardcode). D2 — the openOnion fills CODE COMMENT still carried the strike-11 "MIRROR" overclaim (corrected on the I-69(b) row but not in code); the comment is now aligned (title+subtitle mirror; `text` = the card's flavor, richer, NOT a mirror) — the strike-11 correction reaches the code. D3 — VG8l asserts opacity · transparent-pass · renderOrder>veil but NOT depthTest/depthWrite (both named in (b)): these are NON-LOAD-BEARING for opaque-over-veil (governed by transparent-pass membership + renderOrder, both asserted; card and veil share depthTest:false so their mutual order is depth-independent) — recorded as a boundary note (the I-40/I-57a expected-boundary pattern), the defense-in-depth leg deferred as optional | benign | local (utilization/bench game3d + visual-gate) |
| I-71 | game3d.ts MODULE SPLIT — PURE REFACTOR (owner "run the recorded split" 2026-08-03; the ARCHITECTURE.md debt note, invoked EARLY to enable the parallel component program): game3d.ts (835 ln) split into bench-local modules under utilization/bench/src/ — **stage.ts** (26 ln: the shared three.js primitives ≥2 modules touch — scene/camera/renderer/focusGroups + WORLD/SEATS/presets + the status() DOM helper), **surfaces.ts** (96 ln: the 5 PURE builders verbatim — panelTexture/panel/layoutFace/card/fortuneFaceTexture), **stacks.ts** (74 ln: cardBack/lcg/cardStack), **camera.ts** (256 ln: the ladder + pose state + tickGlide() extracted from tick() + read-accessors), **onion.ts** (105 ln: the reading board — state + openOnion/closeOnion + onionState/onionRegions + setOnionVerdict). game3d.ts (→381 ln) keeps the engine binding, manifestBackdrop, buildScene, the draw theater, interaction, tick(), and the __GAME3D__ gate surface (delegating camera/onion reads to cam.*/onion.* accessors). **MECHANISM registered (minor deviation from the recorded FOUR, not silent — I-58 discipline):** a 5th shared module `stage.ts` holds the primitives multiple modules touch, so the extraction is behavior-identical WITHOUT a global-state web; the record named camera/stacks/onion/surfaces, stage.ts is the shared-primitives home. **The ONLY deliberate signature change:** cardStack gains a trailing `fidgetState` param (was reading the module global `fidget[rid]`; the buildScene caller now passes it) — behavior-identical; `setOnionVerdict` added so the draw-theater sets the verdict before openOnion (the camera.setLastFocus analogue). **BEHAVIOR-IDENTICAL PROVEN (a refactor is NOT an I-57b re-pin cause, so pins may not move):** visual-pins.json BYTE-UNCHANGED (git clean — zero re-pin); gate:visual 37/37 (VG8k+VG8l green); ci 279/279; gate:target 21/21·11/11; tsc clean. Code moved VERBATIM; ESM init order REORDERED but BEHAVIOR-NEUTRAL (K7-refactor D1: camera's import-time pose init + wheel-attach now PRECEDE manifestBackdrop/buildScene, whereas old game3d set the camera pose AFTER buildScene — proven neutral: mapPreset reads only presets/WORLD constants with no scene dependency, the wheel listener fires only on user events, and neither backdrop nor buildScene reads camera state, so the fully-initialized pre-first-tick state is identical; the byte-identical pins confirm it). Load order: stage primitives → camera/onion import-time init → game3d body (manifestBackdrop → buildScene → scene.add(camera) → bar wiring → tick() → final status()). **Distinct K7-refactor PASS (worst dim 8, pure-move spot-checked line-by-line, 3 guards re-killed by name in their new homes): D1 (this init-order wording, closed here) + D2 (stacks.ts exports `lcg` though only used in-file — harmless, kept internal, recorded — not a silent leave).** | benign | local (utilization/bench/src/*.ts — bench-local modules per the ARCHITECTURE debt note; engine/presentation/packs + SVG certified bench + spike + pins UNTOUCHED) |
| I-72 | GATE PORT PARAMETERIZED — parallel-program enabler (owner opted into the multi-agent parallel component program 2026-08-03): visual-gate.mjs (4174) and run-target-check.mjs (4173) hardcoded their server ports, which COLLIDE when parallel worktree builds each run the gate. FIXED: `GATE_PORT` / `TARGET_PORT` env vars (default 4174/4173 — behavior-identical when unset), threaded through `server.listen` AND every `page.goto`. Each worktree runs its gate on a unique port. VERIFIED behavior-identical on NON-DEFAULT ports: gate:target on TARGET_PORT=4186 → 21/21·11/11; gate:visual on GATE_PORT=4185 → 37/37, visual-pins.json BYTE-UNCHANGED (the gate's CONFORMANCE LOGIC — checks, pins, mutations — is IDENTICAL; only the server port binding is parameterized). Pure infrastructure prerequisite for the parallel program, NOT a component increment (so no k7-pass tag); the program's own per-increment K7s exercise the parameterized gate independently | benign | local (utilization/bench visual-gate.mjs + run-target-check.mjs) |
| I-73 | A4 — ROUND PREAMBLE + ROUND CARD + a SEEDED ROLLING EXHIBIT die (register I-73, gate namespace VG8m; the FIRST parallel-worktree component increment, GATE_PORT=4201). Built in a NEW bench-local module `utilization/bench/src/die.ts` + a THIN registration in game3d.ts (buildScene anchor · a `#round-btn` bar button · two interaction branches · one tick step · seven `__GAME3D__` accessors); reuses stage/surfaces/stacks by import and RE-APPLIES the onion.ts card-modal pattern as a DISTINCT modal (K7-A4 / I-58 strike 12 correction: die.ts imports camera VIA stage, not camera.ts directly, and does NOT import onion.ts — it re-implements the veil + opaque-over-veil modal locally because the lane forbids editing onion.ts; the "(edits none)" lane claim is TRUE, only the "reuses …camera/onion by import" phrasing was the overclaim, now corrected). **(1) THE ROUND SEQUENCE (I-55a)** — `openRoundSequence` opens ROUND_PREAMBLE FIRST, a click advances to ROUND_CARD, another dismisses; both render as the spike-proven `card()` (surfaces.ts) in a CAMERA-PARENTED reading board — the onion.ts card-modal pattern RE-APPLIED (a DISTINCT modal from the draw onion, which the lane forbids editing) under the I-70 OPAQUE discipline: a 55% veil at renderOrder 90 with the card faces moved INTO the transparent pass at opacity 1, depthTest/depthWrite off, renderOrder 92 ABOVE the veil, so the card COVERS the veil instead of being darkened by it (asserted as material/order STATE, not pixels — I-57c). The lead-off callout is DERIVED from the PROJECTED active seat the caller passes (`v.seats[v.turn.seatIdx].id` — the K7-v1x D2 law, theater never outruns truth). **(2) THE DIE (owner-ruled 2026-08-03 = a SEEDED ROLLING EXHIBIT)** — a cube (BoxGeometry, six pip-face materials 1–6, opposite faces sum to 7) sitting near the TOWN_TABLE 'dice' region (built ONCE at the dice-region world anchor from `focusObject('table:dice')` — NOT a table child, since the table's 9×7 scale would distort the cube; NOT rebuilt on state change). `rollDie()` (viewer's turn) plays a SEEDED toss — arc up (sin) + tumble (whole spins resolving to identity at t=1) settling EXACTLY on the seeded face upright; the seed is the BENCH-deterministic LCG (`lcg` from stacks.ts). HK-11 fires through `beginFlourish`/`completeFlourish` with the `'die-throw'` flourish: at settle the DISPLAYED up-face (a GEOMETRY-STATE read — the winning face-normal's world-y, never pixels) is fed to `completeFlourish`; on a mismatch the verdict FLAGS and TRUTH WINS — the die re-settles to `faceUpQuat(Number(verdict.result))` (the seeded value). A COMMITTED forced-mismatch drill (`setForceDieMismatch`, the VG7d/I-67c precedent) settles the toss on a DIFFERENT face (the lie) to keep the path falsifiable. **The die is an EXHIBIT** — the slice has NO engine die verb, so die.ts imports NO engine/controller and calls NO submit/emit; it touches NO game state and asserts nothing false (rowHash AND moveCount are INVARIANT across every roll — proven). FIDGET (owner grammar): when it is NOT the viewer's turn, a die touch does a lazy DEAD ROLL to a deterministic LCG side, no HK-11, no verdict, and structurally no state. **(3) DIFFUSED LIGHT ONLY (LAW): `MeshBasicMaterial` throughout, zero source lights, zero shadows; UNSKINNED (D-1): pip/veil canvases are placeholders, no skin tokens. **VG8m KILL-FIRST — each of the FIVE checks proven by a mutant that fails it BY NAME with the gate COMPLETING (41/42, never a crash), then restored:** die-face-count (dup a face value → faces [1,1,2,3,4,5]:false); die-roll-hk11 (normal toss settles on a wrong face → mismatch:true displayed 3 ≠ seeded 2:false); forced-mismatch-truth-wins (drop the re-settle → final up-face stays on the lie 3 ≠ seeded 2:false); die-fidget-pure (fidget branch calls endTurn → moves 1→2:false); round-sequence (open on 'round-card' → preamble:false). Baseline restored → **gate 42/42 (the 37 pre-existing + 5 VG8m)**, ci 279/279. **RE-PIN (I-57b): pins BYTE-UNCHANGED (VG2/pins-rederive 8/8, none orphaned)** — the die is a game3d WebGL exhibit and game3d has NEVER been pinned (every pin is an SVG-bench innerHTML hash; the certified SVG surfaces are untouched) — so A4 is NOT a re-pin cause (the I-71 precedent: not-pinned surface → zero re-pin; a discharge produced only a spurious CRLF rewrite, discarded). **tsc note (environmental, NOT A4):** `tsc -p utilization/bench/tsconfig.json` reports 2 PRE-EXISTING errors in main.ts (LockstepController.host) — a worktree node_modules-junction artifact (`@tabletop/*` symlinks to the MAIN tree while tsconfig includes the worktree's local packages, so the same class compiles under two roots = a nominal private-field clash); present on clean HEAD 30336a0 before any A4 change; die.ts + game3d.ts add ZERO tsc errors; not fixable in-lane (main.ts is out of lane). | benign | local (utilization/bench/src/die.ts NEW + game3d.ts thin registration + visual-gate.mjs VG8m; stage/surfaces/stacks/camera/onion + engine/presentation/packs + game.ts/spike3d.ts + pins UNTOUCHED) |
| I-74 | A10 — THE BOOKS LEDGER IN 3D (owner-ruled 2026-08-02, roadmap A10: "a physical LEDGER sitting FLAT in front of the player" that FLIPS OPEN to read): a new bench-local module `utilization/bench/src/ledger.ts` + a THIN registration in game3d.ts (buildScene build + one open-click raycast branch + the __GAME3D__ surfaces) + VG8n in visual-gate.mjs. REUSES stage.ts (camera) / surfaces.ts (layoutFace) / packs BOOKS_PANEL — none edited. **(a) the CLOSED book** — `ledgerBook()` builds a thick cover mesh lying FLAT (`grp.rotation.x = -Math.PI/2`), positioned in front of the VIEWING seat's board (seat-0 near row: world (-420, 2, 560)); it carries `userData.ledger` (the open-click marker) + `userData.focus='ledger'` and **NO region tag** — so the VG8a scene-region count is untouched (the panel's regions live ONLY on the open overlay). It is registered in `focusGroups['ledger']` each buildScene, so the LADDER selects/anchors it (a click calls `cam.setLastFocus('ledger')`; `anchorPreset('ledger')` falls through to 'overview' with no throw — no camera.ts edit needed). **(b) FLIP OPEN** — `openLedger(view, seat)` renders the measured BOOKS_PANEL def (I-56) via `layoutFace` as a camera-parented overlay over a 55% dark veil (the A2 reading-board pattern), so it survives buildScene and faces the reader. INTERPRETATION REGISTERED (not silent): the owner's "anchor+zoom flips open" is realized as CLICK-TO-OPEN (the click anchors via the ladder AND opens) — the wheel→open hook would require editing camera.ts, which is OUT OF LANE; the flip is pure theater (the overlay IS the open ledger), the open/close is discrete STATE the gate reads. **(c) REAL fills from the projection (I-56d), the GD5/GD5b law in 3D** — `computeBalance` reads the SAME projection as the certified SVG popBooks: Assets = cash + Σ receivables(holder=seat) · Liabilities = Σ debts(debtor=seat) · Equity = assets − liabilities (the identity the reckoning proves). Body renders the three Balance-sheet rows; total = `Liabilities + equity $…`; footnote states the identity; callout = the BOTY cash-vs-paper teaching line over the REAL cash. **P&L is a SINGLE bracketed TAB-LABEL** (`[P&L — next increment] · Balance Sheet`) — NO P&L rows, no invented figures (I-56d). At the canonical fresh state the balance is the certified deterministic ZERO (cash 0 · AR 0 · AP 0 — matches modal:books-fresh), the identity holding trivially yet falsifiably. **(d) OPAQUE over the veil (the A3b/I-70 discipline)** — every panel face moves into the transparent pass at opacity 1, depthTest/depthWrite off, renderOrder 92 ABOVE the veil's 90 — it sorts after the veil and fully covers it. **(e) VG8n kill-first (4 checks; STATE not clocks — I-60f; material/geometry/STATE not pixels — I-57c), each proven by a mutant that FAILS IT BY NAME with the gate COMPLETING (40/41, not a crash), then restored to 41/41:** `ledger-renders-books-panel` (open overlay ids ≡ BOOKS_PANEL.regions ids; drop the callout region → ids [5]≠[6] fails) · `ledger-balance-identity` (identity on the stamped figures AND fidelity to the INDEPENDENT projection oracle — the gate sums raw cash/AR/AP itself, VG8b-style — AND the rendered footnote speaks those figures; `equity+1` → identity:false fidelity:false footnote-true:false fails) · `ledger-opaque` (opacity 1 + transparent-pass + over-veil; opacity→0.5 → opaque:false fails) · `ledger-flip-open-state` (closed→open→closed; closeLedger no-op → closed-after-click:false fails). **NO re-pin (I-57b): the 8 pins are all SVG-bench-derived (game.html/showcase.html); the 3D ledger touches NO SVG canonical composition — `--discharge` reproduced visual-pins.json BYTE-UNCHANGED, VG2/pins-rederive green.** ci 279/279; gate:visual 41/41 (37 pre-existing + VG8n×4). TSC NOTE (pre-existing, out of lane): the worktree's node_modules junction resolves `@tabletop/*` to the MAIN tree while the tsconfig includes the worktree's own packages/*, so `main.ts` (which bridges them via a worktree-relative f5-fixture import) shows 2 dual-realpath TS2345 errors at BASELINE — identical before and after this increment; ledger.ts + game3d.ts add ZERO new errors (game3d's graph is @tabletop-consistent). | benign | local (utilization/bench/src/ledger.ts + THIN game3d.ts registration + visual-gate.mjs VG8n; stage/surfaces/camera/onion/packs/engine/presentation + SVG bench + spike + pins UNTOUCHED) |
| I-75 | A15 — THE GAME BOX (owner-ruled 2026-08-02; the roadmap A15 row): an OPEN board-game box, LID OFF, STATIC, to the RIGHT of the board, as if unpacked (the full unboxing-as-game-start animation is a recorded FUTURE upgrade — "the box is fine for the moment"). **(a) the object** — a NEW bench-local module `box.ts` (`buildBox`): an OPEN box = a base group (a bottom slab + FOUR low walls, no top — `userData.boxBase`) plus a SEPARATE lid group (`userData.lid`: a top slab + a thin rim so it reads as a lid) set down OFF the base beside it (a clear +x gap, a slight lean) — lid OFF, not on the base. DIFFUSED LIGHT ONLY (the standing LAW): MeshBasicMaterial + LineBasic edges, ZERO light constructors (grep-verified `new THREE.*Light` = 0). Unskinned (D-1): placeholder kraft tints, no assets. **(b) thin registration in game3d.ts** — buildScene builds the box, places it at world (−140, 0, 470) [+x BEYOND the VIEWING seat's board: seat-0 board x ≈ −420], registers `focusGroups['box']` + `builtRoots` (rebuilt on every state change like the boards); the raycast pointerup gains a BOX BRANCH: `o.userData['box']` → `cam.setLastFocus('box')` + status, and NOTHING else — no fidget, no verb, no state change (seat + table + box are SELECTABLE-NOT-FIDGETABLE; the fidget grammar stays deck/discard/cards). Gate surfaces added: `boxProbe` (presence · base/lid distinct objects · lid-off-base as xz-distance beyond the base half-footprint · box world x vs seat-0 board x) and `boxScreenXY` (the BASE center projected to canvas pixels — a solid mesh the raycast hits, not the base↔lid-gap bbox centre). **(c) VG8o kill-first — the FOUR checks, EACH proven by a mutant that FAILS IT BY NAME (gate COMPLETES, never crashes; kills run + recorded BEFORE this row's commit):** box-present (a box object exists) — MUT drop `scene.add(box)` → `VG8o/box-present` FAILS "NO BOX in the scene" (37/41); box-right-of-board (box x > the viewing board x) — MUT place the box at x=−900 → FAILS "box x −900.0 > viewing-board x −420.0" (40/41, others pass — isolated); box-open-form (a lid distinct from the base, OFF it) — MUT set the lid ONTO the base → FAILS off-base:false (lid −138,470 vs base −140,470; 40/41, isolated); box-selectable (a click ANCHORS 'box', no fidget/state) — MUT drop the branch's `setLastFocus('box')` → FAILS "anchor→table:deck (want box)" with rowHash+moves STILL invariant (40/41, isolated — the invariance proves the non-fidget leg was already true, the anchor is the only failing clause). Baseline restored → **41/41 (the 37 pre-existing checks + the 4 VG8o)**; ci 279/279. **(d) RE-PIN (I-57b): NO pin moves — the box changes no CANONICAL COMPOSITION pin.** The box lives ONLY in the unpinned `game3d.html`; every visual-pin derives from `game.html` (the SVG bench `__GAME__`) + `showcase.html`. `visual-gate.mjs --discharge` re-derived the pins BYTE-IDENTICAL (git-clean — the I-71 byte-unchanged precedent); the object being in an unpinned surface is the NAMED reason, not a skipped re-pin. **(e) the I-63h depth-blind `fitAlong` trigger — which NAMES "the A15 box" — does NOT fire at THIS increment, recorded not dodged:** A15 as specified is STATIC + anchor-SELECTABLE, NOT a gated READ target. A box read via `readView('box')` is reachable (face-on along the box's +z normal) but A15 asserts SELECTION (the anchor), not a read-fit — and a proper thick/overhead box read plus re-running the depth-blind `fitAlong` mutant both live in `camera.ts` (mapRead/fitAlong), OUT of A15's lane. So the depth-blind re-kill stays deferred and the I-63h trigger RE-EXTENDS to the first increment that GATES a thick read (a FUTURE A15 read/unboxing upgrade, or the A10 ledger opened) — this sentence is the extended record. **(f) pre-existing tsc note (environmental, out-of-lane):** `npx tsc -p utilization/bench/tsconfig.json` reports 2 errors in `main.ts` (a cross-tree module-identity clash: the node_modules junction resolves `@tabletop/*` to the MAIN tree while the bench tsconfig includes the worktree's `packages/*/src`, duplicating the engine module) — PRESENT byte-for-byte on the stashed baseline at HEAD 30336a0, in a file A15 does not touch; box.ts + game3d.ts add ZERO tsc errors; not fixable in-lane (tsconfig/main.ts/node_modules are all out of A15's lane, and node_modules is a junction into the SHARED main tree). | benign | local (utilization/bench/src/box.ts [NEW bench-local module] + game3d.ts thin registration + visual-gate.mjs; engine/presentation/packs + SVG certified bench + spike + visual-pins.json UNTOUCHED) |

| I-76 | PARALLEL BATCH-1 INTEGRATED (A4/A10/A15, first multi-agent worktree batch — owner opted into the Workflow 2026-08-03): three increments built CONCURRENTLY in isolated worktrees off 30336a0 (each: instruments-first → new bench module + thin game3d.ts registration → kill-first VG checks → distinct K7), then merged SERIALLY into main (A4 ff → A10 → A15) with the shared-file conflicts (game3d.ts imports/accessors, visual-gate.mjs check blocks, this ledger's rows) resolved as UNIONS. **Integration VERIFIED: gate:visual 50/50 (37 pre-existing + VG8m·5 + VG8n·4 + VG8o·4 — no cross-increment interference), ci 279/279, gate:target 21/21·11/11, visual-pins.json BYTE-UNCHANGED (the 3D objects touch no SVG-bench pin; the I-71 not-pinned-surface precedent held across all three, so NO integration re-pin was needed — the parallel plan's feared pin-conflict class never materialized).** One merge artifact caught + fixed: the VG8n/VG8o union dropped VG8n's outer block `}` (node --check failed) — restored before commit; K7s' isolated gates were unaffected. **K7 minors dispositioned:** A4 = the I-58 strike-12 wording (corrected on I-73). A10 = (a) the 3D ledger FILLS the callout ('Cash in bank $X — profit on paper isn't cash') whereas the certified 2D popBooks leaves it UNFILLED (layouts.ts:220) — a deliberate 3D teaching-line ADD over real cash, RECORDED not silent; owner confirms at playtest whether to keep it or match the 2D blank; (b) RECORDED TRIGGER (the M-AR / I-56 R1 pattern): VG8n's balance-identity only exercises the all-ZERO fresh state, so it falsifies ADDITIVE compute errors but a SCALING mutant (assets×2) survives at zero input — WHEN a non-zero AR/AP state becomes drill-reachable (mechanics increments), VG8n MUST re-open the ledger there and re-run the scaling kill; until then this sentence is the record. A15 = box-right-of-board compares base/board CENTERS (not edges) and box-selectable uses a rowHash/moveCount proxy — both ACCEPTED boundaries (K7 confirmed the real geometry is clearly separated ~65u and the true guarantee is the structural ABSENCE of a box-fidget branch). All three K7s PASS (worst dims 8/7/8, ≥7). Owner playtest of the batch = the seal | benign | local (utilization/bench merge onto main; the three increments' own rows I-73/74/75) |
| I-77 | K-A — THE DROP-IN COMPONENT ARCHITECTURE, increment 1: a PURE, BEHAVIOR-PRESERVING refactor of the game3d.ts spine into a registry-driven harness (the approved plan "drop-in Component architecture", owner-ruled 2026-08-03: refactor FIRST, then fix the 3 broken objects K-C..E). This row is K-A ONLY — NOT the gate split (K-B), NOT the object fixes (K-C/D/E). **(1) THE CONTRACT — NEW `utilization/bench/src/component.ts` (types + one collision-check helper, ZERO runtime object logic):** `PlayAreaContext` (the portable seam a component may touch — `scene/camera/renderer`, `projection():SeatView`, `viewSeat`, `status()`, `register(root,{anchorKey})`, `submit(verb,args):bool`, `rebuild()`, `moves()`, and `theater:{glideTo,setLastFocus,focusObject,toggleRead,getMode}`); `PickInfo` (`{object,point,distance,region,focus,tags,event}` — `tags` = merged userData up the ancestor chain, NEAREST WINS, reproducing today's `while(o)` walk); `Component` (`{id,anchorKey?,persistent?,placement,build(ctx)→Object3D|null,consumeClick?(ctx,ev)→bool,onPick?(ctx,hit)→bool,tick?(ctx,t),gate()→Record}`); the `Placement` union (`bound{surface,region}` | `free{surface}` | `pile{beside,side}` | `rests{on}`); and `assignGate(target,source)` (a FLAT Object.assign that THROWS on key collision). **REGISTERED ctx-seam DEVIATION (I-58 discipline, not silent):** the plan's ctx field list did not include `moves()`; the TABLE LOG renders `controller.row().moves` (the I-52 display-only side-channel), which the SeatView projection does NOT carry — so ctx gains ONE read accessor `moves()` beyond the plan's list, harness-provided exactly like `projection()`. This is the sole seam addition. **(2) THE PLACEMENT FACET (first-class, declared per component; in K-A it is INERT METADATA — resolution to a world transform is K-C..E, so every object keeps its CURRENT explicit position/size/geometry):** die `free{surface:'table'}`; ledger `bound{surface:'seat-0',region:'front'}`; box `pile{beside:'seat-0',side:'right'}` (the CURRENT −140/0/470 spot + 170×120 size PRESERVED — the too-small/mis-placed fix is K-D); table `free{surface:'ground'}` (the union has no dedicated "I am the surface" kind — least-wrong, recorded); seats `bound{surface:'table',region:'seat'}`. **(3) THE ADAPTERS — NEW `src/components/{table,seats,ledger,box,die}.ts`, THIN, delegating to the EXISTING die.ts/ledger.ts/box.ts/surfaces.ts/stacks.ts/onion.ts UNCHANGED (grep-verified: zero edits to those six modules + camera.ts/stage.ts):** table wraps layoutFace(TOWN_TABLE)+cardStack + owns the draw-theater cluster (doDraw/flight/onion-open lifted as a unit) + the deck/discard fidget; seats wraps layoutFace(SHOP_BOARD)×N (registers each board's `seat-i` anchor itself, returns null); ledger wraps ledgerBook/openLedger/closeLedger; box wraps buildBox; die wraps buildDie/rollDie/deadRoll/tickDie + the round sequence. Each declares its placement + a `gate()` returning ONLY its own flat keys. **(4) THE SINGLE COMPOSITION POINT — NEW `src/components/registry.ts`:** `COMPONENTS = [die, table, ledger, seats, box]` — this order PRESERVES today's consumeClick/onPick precedence. **(5) game3d.ts REWRITTEN → a thin harness (518→226 ln):** engine binding (controller/wire/viewSeat/projectNow/submitVerb), manifestBackdrop, the ctx factory, builtRoots, `buildScene()` as a LOOP over non-persistent components (+ the harness chrome header), the persistent-init loop (die built AFTER the first buildScene so `table:dice` resolves), `tick()` as a LOOP (cam.tickGlide first → each component.tick → renderer.render), the pointer dispatchers (pointerdown/move + pointerup = **Phase 0** consumeClick in registry order → the drag/read GUARD → **Phase 2** raycast nearest→far, onPick in registry order, first `true` stops — EXACTLY today's control flow), the bar wiring, and `__GAME3D__` assembled as the harness keys (`ready/rowHash/moveCount/viewData` + the 15 `cam.*` camera surfaces) then each component's `gate()` merged FLAT via `assignGate` (throw on collision). **BEHAVIOR-IDENTICAL — the proof:** (a) `__GAME3D__` exposes the SAME 47 FLAT keys with the SAME values (harness 19 + table 9 + seats 4 + ledger 6 + box 2 + die 7 = 47; the 50 gate checks call `.boxProbe()/.ledgerState()/.diePhase()` as flat names — the flat assignGate reproduces every one); (b) interaction is behavior-EXACT because the registered roots are DISJOINT subtrees — each raycast chain carries exactly ONE returning-tag-type (die | ledger | seatIdx | box | focus='table'; the ledger/box `focus='ledger'/'box'` tags are NOT 'table' so table.onPick declines them), so onPick first-match-by-distance selects the same intersection + fires the same action as today's per-hit branch order, and registry order only breaks impossible ties; (c) the drag/read guard stays BETWEEN Phase 0 and the raycast; (d) camera-parented overlays (open ledger, round modal, onion) stay OUTSIDE build()/builtRoots — the owning module camera.add/removes them, the harness never enumerates camera.children. **PINS BYTE-UNCHANGED BY CONSTRUCTION:** visual-pins.json hashes game.html/showcase.html (the SVG bench __GAME__), never game3d.html — untouched here, so identical; game.html/game.ts frozen SVG bench diff EMPTY. **VERIFIED (main tree, HEAD 46fbc1d):** tsc -p bench clean (exit 0, zero errors); build+visual-gate **50/50**; visual-pins.json git-clean (byte-unchanged); npm test 279; gate:target BATTERY 21/21·DRILLS 11/11; check:tiers OK; game3d.ts 226 ln (< 300); the six wrapped modules + camera.ts/stage.ts byte-unchanged; game.html/game.ts diff EMPTY. **SEQUENCE:** K-A (this) → K-B (visual-gate.mjs → gate/run.mjs + per-object modules + tools/check-size.mjs) → K-C/D/E (fix books/box/dice with mandated placement+behavior gates). Adding object N+1 after this = one adapter module + one registry line; the spine is not edited again. **K7-KA PASS (worst dim 8; a dispatch mutant killed 10 named checks — the registry dispatch is genuinely load-bearing, not theater): D1 (build-order surfaced)** — registry order also reorders the buildScene BUILD loop (table→ledger→seats vs the old table→seats→ledger); behavior-NEUTRAL (disjoint positioned roots · distance-sorted raycast · keyed focusGroups · game3d unpinned), all gates green — the "dispatch reproduces today's flow exactly" claim spoke to DISPATCH precedence; the build reorder is neutral and now on the record. **D2** — seatGroupKeys now derives from the SEATS array (a derived attestation) vs the old live focusGroups filter; identical for the 6-seat config, recorded BENIGN-EQUIVALENT; TRIGGER: restore the live read if a seat registration could ever be conditionally dropped. **D3 (next-touch)** — table.ts imports @tabletop/presentation's beginFlourish/completeFlourish directly (matches the old spine, in-bounds — the contract forbids only spine/controller/component/stage/camera imports); route the flourish through ctx in a later increment to tighten droppability. **D5 (next-touch)** — assignGate's collision-throw path is unexercised (only the clean 47-key merge runs); a unit test asserting the throw is a next-touch. **D4** — die.ts at 306 lines &gt; the ≤300 size gate lands in K-B. | benign | local (utilization/bench/src/component.ts + src/components/*.ts NEW · game3d.ts rewritten thin · engine/presentation/packs + die/ledger/box/surfaces/stacks/onion/camera/stage internals + SVG certified bench + spike + visual-pins.json UNTOUCHED) |

| I-78 | K-B — THE GATE SPLIT + THE SIZE GATE, increment 2 of the approved "drop-in Component architecture" (owner-ruled 2026-08-03: refactor FIRST — K-A landed I-77 — then the gate split, then the object fixes K-C..E). This row is K-B ONLY — NOT any object-behavior change (grep-verified: `git diff --stat 0b37bcd -- utilization/bench/src` = die.ts 8+/101− + die-round.ts NEW ONLY; ledger/box/surfaces/stacks/onion/camera/stage/game3d + src/components ALL byte-unchanged). **(1) THE MONOLITH SPLIT — the 945-line `utilization/bench/visual-gate.mjs` → a THIN runner `gate/run.mjs` (148 ln) + 7 per-object gate modules + `gate/registry.mjs`.** `run.mjs` owns ONLY the plumbing: the http server on GATE_PORT (serving from BENCH = dirname(gate/), so the html/dist/pins root is unchanged), Playwright chromium, the `check(name,pass,detail)`/`results[]` machinery, the SVG-suite pins compare/`--discharge` (the `finalizePins(derived)` helper — grep-confirmed it lives in run.mjs ONLY), arg parsing (`--suite=svg|3d`, `--check=<VGid>`, `--discharge`), the registry filter, and the JSON print / `VISUAL GATE n/total` / exit-nonzero tail. It passes a shared helper bag `{ page, check, GATE_PORT, sha, gotoStage, waitRest, glide, hashes, info, domVsLaw, screenshot, finalizePins, discharge }` to each block's `run(h)`. **(2) THE PER-OBJECT MODULES — each `gate/<id>.gate.mjs` exports `{ suite, id, run(h) }`, its `{ }` block moved VERBATIM (names + detail strings byte-identical):** `svg` (suite svg — VG1·VG2/pins·VG3×6·VG4·VG5·VG6, it OWNS the 8 computed pins), and the 3D groups (suite 3d) `VG7` (spike), `VG8a-i` (stage: regions/standings/chrome/glide/read/six-seat-orbit/ladder/region-anchor — 15 checks), `VG8j` (deck/draw + emits VG8k/VG8l — 7), `VG8n` (ledger — 4), `VG8o` (box — 4), `VG8m` (die — 5). **REGISTERED DEVIATION (I-58, not silent): VG7 (the spike3d exhibit) is a real block in the 50 but the plan's K-B group list enumerated only VG8*; VG7 is carried as a 3d-suite module so the FULL battery reproduces the monolith's 50** (else 46). **(3) SELF-SEEDING (the plan's hard requirement — each block sets up its OWN page state so it passes alone via `--check`):** grep-confirmed exactly ONE top-of-run nav per 3D module. `VG8a-i`/`VG8m`/`VG7` ALREADY navigated (game3d/spike3d) — their inline goto+ready-wait became `h.gotoStage(...)`. `VG8j`/`VG8n`/`VG8o` INHERITED page state in the monolith (VG8j the overview page after VG8i; VG8n/VG8o likewise) — each GAINED a fresh `h.gotoStage('game3d.html')` at the top. Behavior-NEUTRAL in the full battery: VG8a-i's ops are all pure theater (camera/read/selection — no state advance), so the genesis VG8j re-navigates to equals the inherited state for every ASSERTED quantity (deck 3/discard 0, moe active); VG8n's balance identity + VG8o's static box geometry are state-independent (projection-relative / scene-geometry). The `svg` module keeps its full canonical reset (goto game.html → localStorage.clear → reload → dismiss → setCamera overview) inline. **(4) THE SUITE/CHECK FLAGS — `run.mjs` filters `registry` (current run order: svg → VG7 → VG8a-i → VG8j → VG8n → VG8o → VG8m): `--check=<id>` = that one group; `--suite=svg|3d` = that suite; default = all.** **(5) THE HELPER-BAG DEVIATIONS (I-58, on the record):** the plan's listed bag was `{page,check,GATE_PORT,gotoStage,waitRest,glide,hashes,info,domVsLaw,screenshot}`; TWO additions were needed and are named here — `sha` (the svg module computes the pin hashes) and `finalizePins` (run.mjs OWNS the pins compare/discharge but the svg block builds `derived` and calls it at the monolith's ordering point — after showcase, before VG4/VG5 — so both "run.mjs owns pins" AND result-order-identical hold). The bag's `waitRest`/`glide`/`hashes`/`info`/`domVsLaw` are the monolith's shared/repeated helpers generalized; `glide` is a generic variant (VG8a-i keeps its VG8c-named local `glide` verbatim for the timeout-check-by-name behavior). **(6) THE SIZE GATE — NEW `tools/check-size.mjs` (at repo-root `tools/`, matching check-tiers.mjs's home): fails the build (exit 1, naming the file) if any `utilization/bench/src/**.ts` exceeds 300 lines; the frozen `src/game.ts` (563 ln, sealed SVG bench) is EXEMPT. WIRED into `ci` alongside check:tiers.** **(7) THE die.ts TRIM (the one src edit K-B makes) — die.ts (306 → 213 ln) by extracting the ROUND-CARD SEQUENCE (I-55a: openRoundSequence/advanceRoundModal/dismissRoundModal/roundModalState + mountRoundModal/opaqueOverVeil/roundFills/renderRound) into a SMALL subordinate module `src/die-round.ts` (118 ln) and re-exporting the four public symbols; BEHAVIOR-IDENTICAL (verbatim move, no logic change).** die.ts's PUBLIC EXPORT SURFACE is byte-identical (grep-diffed vs 0b37bcd: the SAME 14 symbols — 10 defined in die.ts + the 4 round symbols re-exported from die-round.js), so `components/die.ts` (imports advanceRoundModal/roundModalState + re-exports openRoundSequence from '../die.js') is UNAFFECTED — the die's gate checks (VG8m 5/5 in isolation) still pass. **(8) package.json rewired:** `gate:visual` → the runner (full battery, all suites); ADDED `gate:visual:3d` (= run.mjs --suite=3d) and `gate:visual:check` (build + run.mjs; a `--check` passthrough via `npm run gate:visual:check -- --check=VGx`); `check:size` ADDED and joined to `ci`; `gate:target` UNCHANGED. **(9) the monolith `utilization/bench/visual-gate.mjs` REMOVED** (git rm; its content now lives in gate/). Historical citations of visual-gate.mjs in the append-only records (this ledger's I-68/I-71/I-72 rows, ARCHITECTURE.md, RESOLUTION_RECORD.md, object-model) are NOT rewritten (append-only discipline / do-not-tidy-sealed-paths); this row is the supersession note — visual-gate.mjs's logic is now gate/run.mjs + gate/*.gate.mjs. **VERIFIED (main tree, HEAD 0b37bcd, PLAYWRIGHT_CHROMIUM_PATH=system Chrome, GATE_PORT 4174):** full battery via the NEW runner `node gate/run.mjs` → **VISUAL GATE 50/50** (identical to the monolith's 50, zero false); EACH 3D group GREEN IN ISOLATION proving self-seeding — `--check=VG8a-i` 15/15 · VG8j 7/7 · VG8n 4/4 · VG8o 4/4 · VG8m 5/5; `--suite=3d` 39/39 (VG7 4 + the 35 VG8) · `--suite=svg` 11/11 WITH VG2/pins-rederive green; `git status --porcelain utilization/bench/visual-pins.json` EMPTY (byte-unchanged — NEVER --discharge, the compare re-derived byte-equal by construction: same innerHTML → same sha); `node tools/check-size.mjs` exit 0 (22 bench src files ≤300, game.ts exempt) with die.ts now 213 ≤300; `npx tsc --noEmit -p utilization/bench/tsconfig.json` exit 0; `npm run test` 279; `npm run gate:target` BATTERY 21/21·DRILLS 11/11; `npm run ci` exit 0 (check:tiers OK · check:size OK · build · 279); `git diff --stat 0b37bcd -- utilization/bench/src/game.ts utilization/bench/game.html` EMPTY (frozen untouched). **SEQUENCE:** K-A (I-77) → K-B (this) → K-C/D/E (fix books/box/dice with mandated placement+behavior gates, each one component + one `--check`, iterated in seconds on the split gate). **NOTE (not K-B):** several untracked files predate this increment and were NOT touched (K7_AUDIT_REPORT-*.md, V2-LIVE-OBJECT-INVENTORY.md, governance/game-design/). K7-KB reviews the split's outcome-equivalence (each --check in isolation AND --suite=3d reproducing the monolith's outcomes — the plan's stated K7 obligation). **K7-KB PASS (worst dim 8): the split is FAITHFUL — a balanced-paren parser diffed every check() expression old-vs-new, all 50 names + assertion bodies BYTE-IDENTICAL (no ===→>= , no loosened tolerance, none dropped/renamed); 50/50 via the runner, each --check green IN ISOLATION; two live mutants proved load-bearing (a weakened check → runner pass:false + exit 1; a dropped self-seed → that --check fails alone); the 301-line size probe → exit 1 naming the file (game.ts exempt); die.ts trim behavior-identical (14 public exports unchanged, round-sequence a verbatim move to die-round.ts). Two COSMETIC non-blocking notes accepted: (1) a dropped self-seed surfaces as an uncaught TypeError not a named pass:false — acceptable (nonzero exit, still load-bearing; the fail-by-name discipline, I-60g, is scoped to in-block timeouts); optional next-touch = a top-of-run guard emitting a named fail if __GAME3D__ is absent; (2) bare `gate:visual:check` runs the full battery unless a `-- --check=VGx` is passed — harmless naming redundancy.** | benign | local (utilization/bench/gate/*.mjs NEW · tools/check-size.mjs NEW · src/die-round.ts NEW · src/die.ts trim · package.json · visual-gate.mjs removed; engine/presentation/packs + the 6 wrapped bench modules + camera/stage/game3d/components + SVG certified bench + spike + visual-pins.json UNTOUCHED) |
| I-79 | K-C — THE BOOKS/LEDGER FIX, increment 3 of the "drop-in Component architecture" (owner-ruled 2026-08-03, verbatim intent): the ledger, when opened, shows **P&L on the LEFT page, Balance Sheet on the RIGHT page — an open two-page book spread, both visible**; **zooming in enters READING mode of the SELECTED report (P&L default); in reading mode LEFT/RIGHT moves to the other report, the SAME as the other reading views**; **all transitions are SMOOTH animations**. **THIS ROW SUPERSEDES I-74** (the batch-1 single-panel ledger) ON THE RECORD, never silently — see the supersession breakdown at the end. **(1) THE CLOSED BOOK carries forward from I-74 unchanged** — `ledgerBook()` a thick cover mesh lying FLAT (`rotation.x=−π/2`), placement `bound{surface:'seat-0',region:'front'}` at world (−420,2,560), `userData.ledger` the open-click marker, registered `focusGroups['ledger']`; a click still FLIPS it open. **(2) OPEN = a TWO-PAGE SPREAD (NEW `utilization/bench/src/ledger-spread.ts`, 180 ln)** — on open the closed book flips into a spread standing vertical in front of the seat (anchor = the book's world pos + (0,138,0)): a LEFT page (world-x < right) rendering BOOKS_PANEL filled with the **P&L**, a RIGHT page rendering BOOKS_PANEL filled with the **REAL Balance Sheet**. Each page is a SEPARATE `THREE.Group` **SCENE-PLACED** (`scene.add`, NOT camera-parented — the I-74 camera-overlay+veil is RETIRED) and **FOCUSABLE** (`focusGroups['ledger-pnl']` / `['ledger-balance']`, so the camera read-ladder targets each page) built via `layoutFace(BOOKS_PANEL,…)` over an OPAQUE backing sheet (MeshBasicMaterial transparent:false, depth-writing → occludes the table; the I-70 discipline realized as plain-opaque since the scene-placed design has NO veil to sort over). **(3) THE FILLS (I-56d honored)** — the RIGHT page is REAL from the projection: `computeBalance` = Assets = cash + Σ receivables(holder=seat) · Liabilities = Σ debts(debtor=seat) · Equity = assets − liabilities; the footnote speaks the identity (kept byte-stable for the gate oracle). The LEFT (P&L) page is an HONEST bracketed placeholder — the slice has no revenue/COGS (I-56d) so every figure reads `[—]`, NOTHING invented (this DISCHARGES I-74's single `[P&L — next increment]` tab-label into a full honest P&L page; still zero invented figures). **(4) ZOOM-TO-READ = fit-to-frame of the SELECTED page (the "stuck zoomed" fix)** — `readSelected()` calls camera.ts `readView('ledger-'+selected)` (default 'pnl'), and on open `setLastFocus('ledger-pnl')` so the EXISTING wheel organic-read-entry (camera.ts, unedited) enters the P&L page's fit pose; `readView`/`fitAlong` put ALL EIGHT bbox corners inside NDC — not cropped/over-zoomed. **(5) LEFT/RIGHT SWITCH = a smooth glide to the other page (reuses the EXISTING pan gesture + read machinery, ZERO shared-file edit)** — the harness pointer handler already routes read-mode drags to `cam.panBy` (which clamps the look within the current page); `tickSpread()` (run each frame via the component's `tick` → `tickLedger`) watches `readState()` + `lookAtPoint().x`, and when the look is panned past a threshold TOWARD the other page (`> pnlPage.x + SWITCH_T` from P&L → Balance; `< balPage.x − SWITCH_T` from Balance → P&L) it calls `readView` on the other page — a smooth camera glide. Anti-oscillation is STATE-based: the switch is gated on `!gliding()`, so after a switch the look glides to the new page centre (offset falls back under threshold) before another switch can fire — no clock. **(6) SMOOTH (waits on STATE, never clocks — I-60f)** — the closed→spread OPEN is a per-frame ease-out lerp (`openAmt` 0→1: the pages swing apart from the anchor + ease up to full scale, settling ~15 frames, `spreadSettled()` the STATE the gate waits on); the zoom-to-read and the left/right switch are the camera glide (`tickGlide`, lerp 0.1). **RECORDED DEVIATION FROM THE PLAN (I-58, not silent):** the plan said "Use ctx.theater (readView/…)", i.e. add `readView` to the ctx seam (a game3d.ts + component.ts edit). INSTEAD, the read/switch is driven from the OBJECT MODULE `ledger-spread.ts` importing camera.ts's existing read accessors (`readView/readState/gliding/getMode/setLastFocus/sceneView/lookAtPoint`) + stage.ts (`scene/focusGroups`) DIRECTLY — the SAME bench-object-module tier coupling `ledger.ts` already had with stage.ts (I-74) and `die.ts` has via stage. This keeps the DROP-IN purity rule where it binds — the COMPONENT ADAPTER `components/ledger.ts` stays ctx-only (grep-verified: 0 stage/camera imports) — while touching NO shared file. **CONSEQUENCE: no ctx/harness/camera/stage change was needed** (the task's "STOP-and-report if you must change a shared file to get the read/pan to switch pages" never triggered — grep-verified `git diff --stat` of game3d.ts/component.ts/camera.ts/stage.ts/components-registry/gate-registry ALL EMPTY). **(7) VG8n REWRITTEN — 4 → 6 owner-behavior checks (the ones that would have CAUGHT batch-1), each KILL-FIRST-proven by a mutant that FAILS IT BY NAME with the gate COMPLETING (5/6, never a crash), then restored:** `ledger-two-page` (two focusable pages, LEFT=P&L world-x < RIGHT=Balance; MUT swap page x → left<right:false) · `ledger-renders` (both pages render NON-BLANK stamped rows [the "won't render" fix] AND Balance = the identity + the independent cash+ΣAR−ΣAP projection oracle + the footnote AND P&L = the bracketed `[—]` placeholder with NO invented `$`-figure; MUT invent `Revenue $500` → placeholder:false no-invented-figures:false) · `ledger-fit-to-frame` (the selected page's 8 bbox corners INSIDE NDC; MUT grow the page ×3 after the fit → corners outside NDC:false — the "stuck zoomed" fix) · `ledger-switch` (a LEFT/RIGHT pan gesture — driven in the gate via the REAL `panProbe` surface → the tick detects it → `readView` glides — moves the read focus P&L → Balance; MUT disable the switch condition → focus stays ledger-pnl:false) · `ledger-opaque` (both pages opacity 1 + opaque pass, not see-through; MUT backing transparent opacity 0.5 → opaque:false) · `ledger-flip-open-state` (closed→open→closed; MUT closeLedger leaves isOpen set → closed-after-click:false). All STATE/geometry, never pixels (I-57c); SELF-SEEDING (fresh game3d.html; balance identity is projection-relative). **CARRIED TRIGGER (from I-76, still open):** VG8n exercises only the all-ZERO genesis (cash 0/AR 0/AP 0), so `ledger-renders`'s identity+fidelity falsifies ADDITIVE errors but a SCALING mutant survives at zero input — WHEN a non-zero AR/AP state becomes drill-reachable, re-open the ledger there and re-run the scaling kill; this sentence carries the record forward. **VERIFIED (main tree, HEAD b16b61f, PLAYWRIGHT_CHROMIUM_PATH=system Chrome, GATE_PORT 4174):** `--check=VG8n` → **6/6 in isolation**, each kill-first-proven above; FULL BATTERY `build.mjs && gate/run.mjs` → **VISUAL GATE 52/52** (the prior 50 − VG8n's old 4 + the new 6; no OTHER check regressed); `npx tsc --noEmit -p utilization/bench/tsconfig.json` exit 0; `node tools/check-size.mjs` exit 0 (23 bench src files ≤300 — ledger.ts 135, ledger-spread.ts 180, components/ledger.ts 94; game.ts exempt); `npm run test` 279; `npm run gate:target` BATTERY 21/21·DRILLS 11/11; `git status --porcelain utilization/bench/visual-pins.json` EMPTY (the two-page ledger is a game3d WebGL surface — never pinned; every pin is an SVG-bench innerHTML hash, untouched); `git diff --stat utilization/bench/src/game.ts utilization/bench/game.html utilization/bench/src/spike3d.ts` EMPTY (frozen untouched). **SUPERSESSION OF I-74 (what changed · carries · retired):** CHANGED — the open ledger is now a TWO-PAGE spread (P&L left + Balance right) with per-report fit-to-frame read + a left/right page switch, replacing I-74's single camera-parented BOOKS_PANEL overlay that "got stuck zoomed" and showed only the Balance sheet with a P&L tab-label. CARRIES FORWARD — the closed flat book + its click-to-open + `focusGroups['ledger']` anchor; `computeBalance` and the real Balance-sheet identity (GD5/GD5b in 3D); the honest "nothing invented" P&L discipline (I-56d); the opaque-not-see-through law (I-70). RETIRED — the camera-parented overlay + the 55% veil + the transparent-pass/renderOrder-92-over-veil trick (the scene-placed opaque pages need no veil), and I-74's VG8n checks `ledger-renders-books-panel` + `ledger-balance-identity` (folded into `ledger-renders`). **LANE (exact):** `utilization/bench/src/ledger.ts` (rewritten, 172→135 ln) · `utilization/bench/src/ledger-spread.ts` (NEW sub-module, the ≤300 extraction) · `utilization/bench/src/components/ledger.ts` (adapter: added `tick` + the two-page gate surfaces) · `utilization/bench/gate/VG8n.gate.mjs` (rewritten). K7-KC reviews the owner-behavior gates' load-bearingness + the supersession faithfulness; owner playtest is the seal. **K7-KC PASS (worst dim 7): both owner-reported gate-holes CLOSED by the reviewer's own mutants — a blank page → ledger-renders:false, a stuck-zoomed page → ledger-fit-to-frame corners-in-NDC:false (±1.62/±2.80 outside; clean run ±0.94); all 6 behavior checks load-bearing (each mutant fails BY NAME, gate completes 5/6); Balance bound to the LIVE projection (a hardcode cash=500 → fidelity:false caught); P&L invents nothing (pnlNoFigures); pages genuinely P&L-left(x−580) < Balance-right(x−260); I-74 supersession stated. 4 MINOR non-blocking, recorded: (1) dead export `spreadOpen` (ledger-spread.ts) — next-touch delete; (2) the `identity` conjunct in ledger-renders is TAUTOLOGICAL (equity ≡ assets−liabilities by construction) — decorative, the load-bearing `fidelity` cross-check carries it; (3) `ledgerSelected` gate surface unasserted — cosmetic; (4) ledger-spread.ts imports camera/stage directly (the plan suggested ctx.theater) — a droppability seam ACCEPTED (the adapter is ctx-pure; matches ledger.ts's existing tier coupling, the K-A D3 pattern), tighten later. Worst-dim-7 = genesis-zero-only base case (a scaling error survives at 0) — carries the I-76 trigger.** | benign | local (utilization/bench/src/ledger-spread.ts NEW · ledger.ts rewritten · components/ledger.ts adapter · gate/VG8n.gate.mjs rewritten; game3d.ts/component.ts/camera.ts/stage.ts + components-registry + gate-registry + engine/presentation/packs + SVG certified bench + spike + game.ts/game.html/spike3d.ts + visual-pins.json ALL byte-UNCHANGED) |
| I-80 | K-D — THE GAME BOX FIX, increment 4 of the "drop-in Component architecture" (owner-ruled 2026-08-03, verbatim rulings): the batch-1 box was **mis-placed (near the table centre, tucked by a seat board) and too small**. Fixed ALL of: **(POSITION)** the box sits to the RIGHT of the whole TABLE — off its +x side, its LEFT edge clear of the table's RIGHT edge; **(SIZE)** much larger — footprint ≥ ¼ the table footprint (the table "folds in four"); **(BASE)** an OPEN box (a bottom slab + four walls, NO top); **(LID)** a SHALLOW box with NO BOTTOM and a LIP around all four edges — a bottomless open frame set OFF the base, leaning beside it, as if lifted off; **(STATIC / SELECTABLE-NOT-FIDGETABLE)** a pile beside the table, selectable via the ladder (a click anchors 'box'), the full unboxing animation a recorded FUTURE upgrade. **THIS ROW SUPERSEDES I-75** (the batch-1 box) ON THE RECORD — supersession breakdown at the end. **(1) DERIVED, not magic constants** — the adapter `components/box.ts` reads the LIVE table group's world bbox (`ctx.theater.focusObject('table')`, the SAME object buildScene places; COMPONENTS order `[die,table,ledger,seats,box]` builds the table first, so it resolves; `updateWorldMatrix(true,true)` then `Box3.setFromObject`). From it: `tb.rightX` (table right edge), `tb.centerZ`, `tb.area = size.x·size.z`. The measured table = a 100×100 LayoutDef plane under `t.rotation.x=−π/2 · t.scale(9,7,1)` at origin → world footprint x∈[−450,450] z∈[−350,350], **area 630 000, right edge +450** (the gate confirms 630000 / 450.0). **(2) SIZE math** — target ≈0.30× the table area (a margin over the ¼ minimum), box slightly landscape (`bw=1.25·bd`): `bd=√(0.30·area/1.25)=388.8`, `bw=486.0` → **footprint 189 000 ≥ ¼·630 000 = 157 500** (ratio 0.300; gate-confirmed). **(3) PLACEMENT math** — base LEFT edge = `tb.rightX + gap` (gap 50) so the group x = `tb.rightX+gap+bw/2`; left edge **500.0 > table right 450.0 (Δ50.0)**, z = `tb.centerZ` (0) so the box clears the seat boards (z=±420, no overlap). Placement metadata now `pile{beside:'table',side:'right'}` (was `seat-0`). **(4) THE OPEN BASE + BOTTOMLESS LIPPED LID (`src/box.ts`, 89 ln, pure — `buildBox(dims:BoxDims)`)** — base = 1 bottom slab (thin-Y) + 4 walls (thin-X/Z), WALL_H 60, NO top; lid = 4 short rim walls (LID_H 22, a lip on all four edges), NO bottom + NO top, `lw=bw+14/ld=bd+14`, set OFF the base at local x=`bw/2+gap+lw/2` with a −0.32 lean. Diffused MeshBasic ONLY (no source lights); unskinned (D-1). Adapter stays ctx-pure at its binding (imports ONLY THREE + component types + `../box.js` — grep-verified). **(5) VG8o REWRITTEN — 4 → 6 owner-behavior checks (the ones that would have CAUGHT batch-1's wrong-place/too-small), each KILL-FIRST-proven by a mutant that FAILS IT BY NAME with the gate COMPLETING (5/6, never a crash), then restored:** `box-right-of-table` (box base LEFT edge world-x > table RIGHT edge world-x — EDGES from the real table bbox; the batch-1 miss was a CENTRE-vs-centre compare; MUT place box at `tb.rightX−400` → false) · `box-large-enough` (box footprint ≥ ¼ table, both from real bboxes; MUT shrink to 0.05× → false) · `box-open-no-top` (base ≥4 walls + exactly 1 horizontal panel = the bottom, no top; classification by each BoxGeometry's thinnest axis: thin-Y = a face, thin-X/Z = a wall; MUT add a top slab → horizontal-panels 2 → false) · `box-lid-shallow-bottomless-lipped` (lid a DISTINCT object OFF the base [clear gap], ≥4 rim walls, 0 horizontal panels = bottomless; MUT give the lid a bottom → faces 1 → false) · `box-selectable` (a REAL click anchors 'box' with rowHash/moveCount invariant — not fidgetable; MUT drop the anchor → false). All STATE/geometry, never pixels (I-57c); SELF-SEEDING (fresh game3d.html; the box is static scene geometry). **(6) I-58 STRUCTURAL FALSIFICATION (12 strikes, all grep-verified):** base-top 0 · lid-bottom 0 · base-wall 4 · lid-rim 4 · real-lights 0 · MeshBasicMaterial present · adapter imports = {three, component types, ../box.js} · `seat-0` 0 in box files · gate edge-vars boxLeftX+tableRightX present · VG8o check() count 6 · BoxDims/derived-dims passed in · placement beside 'table' · `git diff --stat` = ONLY box.ts + components/box.ts + VG8o.gate.mjs. **VERIFIED (main tree, HEAD 9297bff, PLAYWRIGHT_CHROMIUM_PATH=system Chrome, GATE_PORT 4174):** `--check=VG8o` → **6/6 in isolation**, each kill-first-proven above; FULL BATTERY `build.mjs && gate/run.mjs` → **VISUAL GATE 54/54** (the prior 52 − VG8o's old 4 + the new 6; no OTHER check regressed — VG8o 6/6 across all 7 runs; one intermittent `VG8j/fidget-pure-theater` "neat-restored-exact" float flake seen ONCE in 7 full-battery runs, passes 3/3 in isolation and 6/7 in battery, in the DISJOINT deck lane — NOT box-touched; baseline stash-test of HEAD 9297bff ran 6/6 clean, so the flake is a tight-float intermittent, characterized-not-masked); `npx tsc --noEmit -p utilization/bench/tsconfig.json` exit 0; `node tools/check-size.mjs` exit 0 (box.ts 89, components/box.ts 145 ≤300; game.ts exempt); `npm run test` 279; `npm run gate:target` BATTERY 21/21·DRILLS 11/11; `git status --porcelain utilization/bench/visual-pins.json` EMPTY (the box is a game3d WebGL surface — never pinned); `git diff --stat utilization/bench/src/game.ts utilization/bench/game.html utilization/bench/src/spike3d.ts` EMPTY (frozen untouched). **SUPERSESSION OF I-75 (what changed · carries · retired):** CHANGED — position moved from near-centre `(−140,0,470)` beside seat-0 to the RIGHT of the whole table (left edge 500 > table right 450); size grown from 170×120 (20 400) to 486×389 (189 000, ≥¼ table); the LID re-formed from a TRAY (a top slab + one rim = a bottomed lid) into a SHALLOW BOTTOMLESS FRAME (four rim walls, no bottom, no top); VG8o's `box-right-of-board` (centre-vs-centre vs seat-0) + `box-open-form` retired for `box-right-of-table` (edge-vs-edge vs the table), `box-large-enough`, `box-open-no-top`, `box-lid-shallow-bottomless-lipped`. CARRIES FORWARD — the OPEN base (bottom + four walls, no top); the detached lid set OFF the base; STATIC-first with the unboxing animation deferred; `userData.box/focus='box'`, the `boxBase`/`lid` markers, and `box-selectable` (click anchors, not fidgetable); diffused-MeshBasic + unskinned (D-1). RETIRED — the batch-1 magic-constant footprint (BW170/BD120), the `(−140,0,470)` placement, the `beside:'seat-0'` metadata, and the tray-lid (top slab + rim). **LANE (exact):** `utilization/bench/src/box.ts` (rewritten pure builder, 73→89 ln, `buildBox(dims)`) · `utilization/bench/src/components/box.ts` (adapter: table-bbox derivation + the open/bottomless gate surfaces) · `utilization/bench/gate/VG8o.gate.mjs` (rewritten, 4→6). K7-KD reviews the owner-behavior gates' load-bearingness + the I-75 supersession faithfulness; owner playtest is the seal. **K7-KD PASS (worst dim 8): the batch-1 centre-vs-edge hole CLOSED — a box overlapping the table but centre-right → box-right-of-table:false (left-edge 57 < table right 450); geometry DERIVED (table scale ×9→13 → box tracked, ratio held 0.300); lid gate-holes caught (bottomed lid → faces:1:false; lid-onto-base → off-base:false); all 6 checks load-bearing. 3 MINOR non-blocking: (1) the `distinct` conjunct in the lid check is tautological (two separately-found objects always !==) — offBase + classification carry it; (2) defensive fallback constants {rightX:450,area:630000} in components/box.ts tableBounds — caught-not-silent (the tableFound guard fails box-right-of-table if hit); optional named-fail on absent table; (3) the lid sits ~667u off the base (satisfies 'off, leaning beside it' but aesthetically distant) — OWNER PLAYTEST is the seal. CARRIED CROSS-LANE (not K-D): VG8j/fidget-pure-theater's 1e-9 exact-float restore (VG8j.gate.mjs:121) is brittle — flaked ~1/7, PRE-EXISTING + DISJOINT (byte-untouched by K-D, deck lane); next-touch = loosen to ~1e-6 or restore-by-reseed.** | benign | local (utilization/bench/src/box.ts rewritten · components/box.ts adapter · gate/VG8o.gate.mjs rewritten; game3d.ts/component.ts/camera.ts/stage.ts + components-registry + gate-registry + the other components + engine/presentation/packs + SVG certified bench + spike + game.ts/game.html/spike3d.ts + visual-pins.json ALL byte-UNCHANGED) |
| I-81 | K-E — THE DICE FREE-TUMBLE FIX, increment 5 (the last of the "drop-in Component architecture" object fixes; owner-ruled 2026-08-03): "the dice to sit **on top of the table** and **free to tumble across the AREA of the table, not confined to a square** in the table area." Batch-1 (I-73) CAGED the die near the small TOWN_TABLE `dice` region — built at `focusObject('table:dice')` and settling IN PLACE (only y-arc + rotation changed, x/z never moved). **THIS ROW SUPERSEDES the caged-die aspect of I-73 ON THE RECORD** — supersession breakdown at the end. **(1) THE EXHIBIT NATURE + HK-11 + FIDGET ARE PRESERVED (all KEPT green):** die.ts still imports NO engine/controller and calls NO submit/emit (grep-verified NONE) — the die touches NO game state (rowHash AND moveCount invariant across the seeded roll and the fidget, gate-proven); HK-11 fires through `beginFlourish`/`completeFlourish` (`'die-throw'`), displayed up-face ≡ seeded, TRUTH WINS on a mismatch (re-settle to the seeded face), the COMMITTED forced-mismatch drill (`setForceDieMismatch`) intact; the not-your-turn lazy DEAD-ROLL fidget intact (no HK-11, no verdict, structurally no state); six pip faces 1–6; diffused MeshBasic + unskinned (D-1). **(2) THE ROLL NOW TRAVELS ACROSS THE TABLE** — `rollDie()`/`deadRoll()` compute a SEEDED landing point WITHIN the live table AREA; `tickDie()` glides the die horizontally `fromPos→toPos` (the smoothstep ease) WHILE it arcs up (sin) and tumbles (whole spins resolving to identity at t=1), settling EXACTLY on the seeded spot with the seeded face upright. The cube's UNDERSIDE rests on the table plane (`dieBaseY = topY + DIE_SIZE/2 + 0.5`, underside y = topY+0.5). **(3) THE TABLE AREA IS DERIVED, not a magic square** — the adapter `components/die.ts` reads the LIVE table group's world bbox (`ctx.theater.focusObject('table')`, the SAME object box.ts reads; the persistent die builds AFTER the first buildScene so it resolves) → `TableRect{minX,maxX,minZ,maxZ,topY}`: world footprint x∈[−450,450] z∈[−350,350] (**900×700, area 630 000**), surface top y ≈ 3.56 (gate-confirmed). A margin `TABLE_MARGIN = DIE_SIZE = 90` keeps the whole cube on the table → the landing rect is x∈[−360,360] z∈[−260,260] (usable 720×520). The OLD cage = the `TABLE_PARENT` `dice` region (`w:16,h:16` of 100 → **~144×112 world**, at the table centre) — the die is NO LONGER confined to it. **(4) AREA-FILL FIX (recorded, not silent):** the first draft drew the x/z fractions as two consecutive LCG outputs — the raw LCG's consecutive outputs from a linear roll-counter lie on a lattice LINE, so the settles marched on a diagonal (measured spreadZ 101 < the old cage depth 112). FIX: one LCG draw seeds a splitmix32 avalanche `mix32` twice (an x-fraction and a z-fraction), decorrelating them so the landings FILL the 2D area (spreadZ 101→414). Determinism preserved (same seed → same draw → same face AND same landing on replay; the LCG resets on the gate's fresh genesis nav). **(5) VG8m REWRITTEN — 5 → 7 owner-behavior checks; the two NEW ones each KILL-FIRST-proven by a mutant that FAILS IT BY NAME with the gate COMPLETING (6/7, never a crash), then restored:** `die-free-tumble` (over 6 SEEDED real-click rolls the settle SPREAD spans a LARGE fraction of the WHOLE table — spreadX 575 > 0.4·900=360 AND > 2× the old-cage width 144, spreadZ 414 > 0.4·700=280, and all 6 settles WITHIN the table bbox [`.every`]; MUT cage the landing to a fixed point [table centre] → spreadX 0 / spreadZ 0 → false) · `die-on-table` (at rest the die's bbox UNDERSIDE y ≈ the table-top y [Δ0.50 < 3] AND its centre x/z within the table bounds; MUT float `dieBaseY += 300` → underside 304 ≠ top 3.56 → false). KEPT green (behavior preserved, unchanged assertions): `die-face-count` (6 faces 1..6), `die-roll-hk11` (displayed ≡ seeded, state-invariant), `forced-mismatch-truth-wins` (lie flagged, truth wins), `die-fidget-pure` (rowHash+moveCount invariant). All STATE/geometry, never pixels (I-57c); WAIT ON diePhase STATE, never clocks (I-60f — `rollOnce` confirms `diePhase → 'rolling'` then waits on `'rest'`); SELF-SEEDING (fresh game3d.html, moe the viewing seat). **(6) I-58 STRUCTURAL FALSIFICATION (grep-verified):** `die-round.ts` byte-UNTOUCHED (git diff empty) · die.ts imports = {three, presentation flourish, stage camera, stacks lcg} — NO engine/controller/submit/emit · `components/die.ts` imports = {three, component types, `../die.js`, `TableRect` type} — ctx-pure at its binding (NO stage/camera import) · dice region `w:16,h:16` · VG8m `check()` count 7 · `git status --porcelain` code diff = ONLY die.ts + components/die.ts + VG8m.gate.mjs (+ this ledger row). **VERIFIED (main tree, HEAD b27b823 base, PLAYWRIGHT_CHROMIUM_PATH=system Chrome, GATE_PORT 4174):** `--check=VG8m` → **7/7 in isolation**, each new check kill-first-proven above, the four kept checks green; FULL BATTERY `build.mjs && gate/run.mjs` → **VISUAL GATE 56/56** (the prior 54 − VG8m's old 5 + the new 7; no OTHER check regressed — one run showed 55/56 from the KNOWN pre-existing `VG8j/fidget-pure-theater` 1e-9-float flake [I-80's carried note, DISJOINT deck lane, byte-untouched by K-E], 56/56 on re-run and VG8j 7/7 in isolation); `npx tsc --noEmit -p utilization/bench/tsconfig.json` exit 0; `node tools/check-size.mjs` exit 0 (die.ts 213→271, components/die.ts 84→103, die-round.ts untouched — all ≤300; game.ts exempt); `npm run test` 279; `npm run gate:target` BATTERY 21/21·DRILLS 11/11; `git status --porcelain utilization/bench/visual-pins.json` EMPTY (the die is a game3d WebGL surface — never pinned; every pin is an SVG-bench innerHTML hash, untouched); `git diff --stat utilization/bench/src/game.ts utilization/bench/game.html utilization/bench/src/spike3d.ts` EMPTY (frozen untouched). **SUPERSESSION OF I-73's CAGED-DIE ASPECT (what changed · carries · retired):** CHANGED — the die no longer builds at the fixed `table:dice` anchor and no longer settles IN PLACE; it rests ON TOP of the table surface (underside on the table plane) and its roll TRAVELS across the whole table area, settling at a SEEDED landing point anywhere within the live table bounds (NOT the dice sub-square, NOT a magic constant). CARRIES FORWARD — the seeded EXHIBIT nature (no engine verb, no game state), HK-11 theater-sync + the committed forced-mismatch drill, the dead-roll fidget, the six pip faces, diffused MeshBasic + unskinned (D-1), and the whole ROUND SEQUENCE (I-55a, `die-round.ts`, untouched). RETIRED — the `focusObject('table:dice')` build anchor and the in-place settle (batch-1's die stayed at the dice-region point; only the y-arc + rotation animated). **LANE (exact):** `utilization/bench/src/die.ts` (free-tumble travel in `tickDie` + the seeded area landing `seededLanding`/`mix32` + `dieRestInfo`/`dieTableRect` gate readers, 213→271 ln) · `utilization/bench/src/components/die.ts` (adapter: table-bbox `TableRect` derivation + the free-tumble/on-table gate surfaces) · `utilization/bench/gate/VG8m.gate.mjs` (5→7). K7-KE reviews the two owner-behavior gates' load-bearingness + the I-73 supersession faithfulness; owner playtest is the seal. **K7-KE PASS (worst dim 8): the 'caged' complaint CLOSED — MUT-A (cage to centre) → die-free-tumble:false; the GATE-HOLE probe MUT-B (a die jittering ±10 in place) → die-free-tumble:false (spread 16 ≪ 360) so a jittering/caged die CANNOT pass; MUT-C (float off table) → die-on-table:false; MUT-D (wrong face, no drill) → die-roll-hk11:false. Determinism re-derived by hand (spreadX 574.5/spreadZ 414.1 = the live gate exactly; same seed → same face+landing byte-identical across 3 runs); genuine 2D fill (corr −0.666, not a lattice; the 101→414 splitmix32 decorrelation real). Exhibit purity STRUCTURAL (die.ts imports no engine/controller). 3 MINOR non-blocking: (1) stale comment components/die.ts:5 (references the retired table:dice anchor) — next-touch reword; (2) latent gate-shape gap — die-free-tumble tests spreadX/spreadZ INDEPENDENTLY, so a perfect diagonal (corr ±1) would pass; the running code fills 2D (corr −0.666) so a gap not a defect — optional next-touch: an anti-collinearity/bin-occupancy assertion; (3) the withinTable `.every` conjunct is near-tautological (landings constructed inside bounds) — harmless.** | benign | local (utilization/bench/src/die.ts · components/die.ts adapter · gate/VG8m.gate.mjs; die-round.ts + game3d.ts/component.ts/camera.ts/stage.ts + components-registry + gate-registry + the other components + engine/presentation/packs + SVG certified bench + spike + game.ts/game.html/spike3d.ts + visual-pins.json ALL byte-UNCHANGED) |
| I-82 | PLAYTEST-2 INTERPRETATIONS (the owner's four 2026-08-03 messages, recorded verbatim in RESOLUTION_RECORD; every decision he didn't make lands HERE, none silent). **(a) "one of every card in the game"** read as CONTENT-TIER data: every card type in the BOTY v1 inventory (`governance/upstream/BOTY_Mechanics_Objects_Inventory.md`) instantiated as pack card ENTRIES with real anatomy/kind so the deck stacks true and the discard is explorable; EFFECTS bind only where the certified slice's EFX vocabulary already reaches — the rest are INERT EXHIBITS until their docketed ExtensionContract cycles (EFX closure law; engine FROZEN). PENDING owner confirm of this scope. **(b) Seat rows after the msg-2 retirement:** TWO rows in front of the seat board — tradespeople (attached equipment tucked underneath-but-visible) + session cards. **(c) Dice:** HOME = the table's dice-region anchor (fixed start position — the "random start" was I-81's seeded first-landing); the seeded-landing roll law (I-81) otherwise stands; SIZE = builder proposes DIE_SIZE 90→45 world units (≈ half), owner seals at playtest. **(d) "a couple seconds"** (discard return) → 2.0s, implemented as STATE (a settle timer surfaced as gate-readable state; gates wait on state, never clocks — I-60f). **(e) PHYSICS = PURE THEATER under HK-11:** RAPIER (`@dimforge/rapier3d-compat`, the owner's dice-shaker source) simulates tumble/flick/drag; displayed die results RECONCILE to the engine-seeded truth (truth wins, the committed mismatch drill carries); physics NEVER decides game state; fidget stays structurally stateless. The new bundled dependency is an EXCURSION needing its own register row when it lands. **(f) ENGINE-LANE FLAGS (out of the 3D bench lane, never absorbed silently):** fired→bottom-of-tradesperson-deck · sold/stolen→bottom-of-equipment-deck · session-card lifecycle (in play while effects live) · equipment attach/detach as game VERBS · full-deck effect RESOLUTION — all engine/pack MECHANICS; the bench presents what the projection carries; the real verbs come via their own facet increments + K7, and until then the bench stages them as recorded exhibits. **(g) K-D box:** playtest-2 gave NO verdict (unmentioned) — stays awaiting-playtest; the ~667u lid-distance flag stands. **(h) The K-C pan-switch DEFECT** (balance sheet unreachable) is superseded by design, not patched: ledger v3 = folder + rise-up + ANCHOR-per-report (click-to-anchor + the existing ladder), retiring the pan-threshold switch. | benign | records only (this row + RESOLUTION_RECORD + roadmap; zero code) |
| I-83 | P-1 — THE DICE SIZE + HOME FIX (owner-ruled 2026-08-03 playtest-2 clause 8: "way too big for the board" + "its starting position is random on the table"; build order sealed by the owner's structured choice: P-1+P-2 first). **(1) SIZE:** DIE_SIZE 90 → **45** world units (edge = 5% of the 900-wide table, was 10%); TABLE_MARGIN tracks DIE_SIZE (landing rect widens); owner seals the proportion at playtest. **(2) THE HOME (the interpretation, on the record):** the TOWN_TABLE `dice` region (x:42,y:42,w:16,h:16 — the table centre) is the die's HOME. The "random start" the owner saw = I-81's die RESTING WHEREVER IT LANDED between rolls (build position was already the fixed centre). FIX: the die is built AT HOME, every toss STARTS from home, and after a settle the result HOLDS briefly (readable) then the die GLIDES home with its up-face preserved — no teleport (the owner's no-teleport law, playtest-2 clause 5, applied to the die). Phases: idle → rolling → rest (result readable, landing anywhere on the table — the K-E free-tumble law UNCHANGED) → returning → idle-at-home. deadRoll (fidget) same cycle. **(3) HOME IS DERIVED**, not magic: components/die.ts reads the live `table:dice` region object's world bbox centre (`ctx.theater.focusObject('table:dice')`) — the region is BACK as the HOME marker only, NOT the cage (rolls still span the whole table; I-81 stands). **(4) VG8m 7 → 10 checks, kill-first:** NEW `die-size-proportion` (rest bbox EDGE — a geometry read via dieRestInfo().edge, never the constant against itself — within [0.03,0.07]×table width; MUT: DIE_SIZE=90 → 0.10 → false) · NEW `die-home-genesis` (BEFORE any roll the die mesh position ≈ the region-derived home, Δ<10 — two independent sources; MUT: build at an offset spot → false) · NEW `die-returns-home` (after 6 FULL roll cycles waited on diePhase 'idle' — STATE not clocks, I-60f — position ≈ home, Δ<10; MUT: drop the return glide → die stays at its last landing → false). KEPT: free-tumble (settles read at 'rest' BEFORE the return; thresholds unchanged) · on-table · face-count · roll-hk11 · forced-mismatch-truth-wins · fidget-pure · round-sequence, with roll steps now WAITING on 'idle' between rolls (the return is part of the cycle). **(5) CLOSES K7-KE minor 1:** the stale components/die.ts header comment (retired `table:dice` anchor wording) rewritten to the home-not-cage truth. Lane: die.ts · components/die.ts · gate/VG8m.gate.mjs + records. **OWNER 2026-08-03 (partial seal): "okay, size now looks correct."** — the SIZE clause is playtest-confirmed; home/return + the gate battery still to seal. **SIZE-GATE EXTRACTION (in-lane):** die.ts hit 311 ln → pipTexture moved VERBATIM to NEW `die-pips.ts` (27 ln); die.ts 293. VERIFIED (Linux verify copy, real toolchain): tsc -p bench exit 0 · check:size OK (24 files) · check:tiers OK · vitest 279/279 · build.mjs clean. Browser battery (VG8m 10 checks + mutants + full battery) = owner-side (the sandbox has no browser; recorded constraint). | benign | local (utilization/bench/src/die.ts · die-pips.ts NEW · components/die.ts · gate/VG8m.gate.mjs; all other code untouched) |
| I-84 | P-2 — LEDGER v3: THE FOLDER + RISE + ANCHOR-PER-REPORT (owner-ruled 2026-08-03 playtest-2 clause 6, verbatim in RESOLUTION_RECORD; DEFECT: "Right now, I cannot view the balance sheet"). **ROOT CAUSE, on the record:** K-C's Phase-0 `consumeClick` closed the OPEN ledger on ANY click — a click on the Balance PAGE closed the book instead of selecting it; the only balance path was the read-mode pan-switch, which failed the owner's playtest. **THIS ROW SUPERSEDES I-79's interaction model** (the pages + fills + opacity carry). **(1) THE FOLDER:** the closed cover gains a hinge PIVOT; on open the cover SWINGS open (a per-frame eased cover pose, coverAmt 0→1) while the two REPORTS RISE from the folder into their standing positions (applyOpenPose now lerps y from the folder level up to the standing height as it separates the pages — "flips open and the reports rise up into their position"). **(2) ANCHOR-PER-REPORT (the balance fix):** clicking a PAGE anchors it — NEW `anchorPage(kind)`: selected=kind, setLastFocus('ledger-'+kind), `readView` — the owner's exact words realized: "If I select the balance sheet, it will zoom in to reading view for the balance sheet." Click routing: `consumeClick` now RAYCASTS the click — a hit whose ancestor chain carries `ledgerPage` FALLS THROUGH to onPick (which anchors); any OTHER click still closes (the A2 any-click-closes contract narrowed by exactly the page exception). **(3) THE PAN-SWITCH RETIRED** (tickSpread's threshold block deleted; gliding/lookAtPoint imports dropped): switching reports = the LADDER — read → zoom out → click the other report → it zooms to read; plus the direct page-click while in scene. **(4) K7-KC minor 1 closed:** dead export `spreadOpen` DELETED. **(5) INTERPRETATIONS (not silent):** (a) the ledger's move to the LEFT EDGE of the seat play area is DEFERRED to Q-3 (the seat-layout increment) — one placement pass, not two; the folder keeps (−420,2,560) for P-2; (b) "folder" realized as the existing board cover + hinge (cosmetic retexture deferred to the skin era, D-1); (c) in-READ left/right switching (K-C's spec) is NOT carried — playtest-2's anchor model replaces it. **(6) VG8n REWRITTEN 6 → 6 kill-first owner-behavior checks:** `ledger-two-page-risen` (both pages focusable, P&L world-x < Balance, AND both RISEN — page centre y ≥ folder top + 50 at settle; MUT: skip the rise → false) · `ledger-renders` (CARRIED verbatim — the fidelity/no-invented-figures oracle) · `ledger-anchor-balance` (THE DEFECT KILL: a REAL click on the Balance page → readState().focus='ledger-balance' + read mode + ALL 8 bbox corners in NDC; MUT: anchorPage hardwired to pnl → false) · `ledger-anchor-switch` (real wheel-out to scene then a REAL click on the P&L page → reads pnl, corners in NDC — the full owner loop) · `ledger-opaque` (carried) · `ledger-flip-open-state` (folder click opens + coverAmt settles 1 + a non-page click closes + leaves read; MUT: cover anim dropped → false). All STATE/geometry never pixels (I-57c), waits on STATE never clocks (I-60f), self-seeding. Lane: ledger.ts · ledger-spread.ts · components/ledger.ts · gate/VG8n.gate.mjs + records. **OWNER 2026-08-03 (partial seal): "the sheets are better- they are anchored now so I can select and zoom in on one or the other."** — the ANCHOR-PER-REPORT model is playtest-confirmed; the folder FORM refined by the same message → P-2b (I-85). | benign | local (those four files; game3d.ts/component.ts/camera.ts/stage.ts + registries + engine/presentation/packs + SVG bench + pins ALL untouched) |
| I-85 | P-2b — THE FOLDED-SHEET FOLDER (owner-ruled 2026-08-03 with a reference picture, verbatim in RESOLUTION_RECORD; REFINES I-84's folder-visual clause — the SEALED anchor-per-report model and the fills/opacity laws carry unchanged). **(1) THE CLOSED FORM:** the folder = a SHEET FOLDED IN HALF lying flat — a BACK half + a slightly shorter FRONT half joined at a FOLD edge (thin boxes, manila tone, label on the front), with TWO GROUPED PAPER SHEETS visibly tucked INSIDE (two blank sheet meshes peeking past the open edge — cosmetic paper, NO figures, so nothing is invented; the REAL filled pages still build from the projection at open). **(2) THE TWO-STAGE OPEN (the owner's transform):** stage 1 — the front half FOLDS OPEN about the fold hinge (coverAmt 0→1) and the folder LIES FLAT; stage 2 — only WHEN the fold has settled (coverAmt ≥ 0.999) do the two REAL report pages build and REPOSITION to the display state: they start FLAT at the folder (rotation.x −π/2, low, small) and stand up as they rise/separate (the applyOpenPose lerp gains the stand-up rotation). The stage handoff is SEQUENCED in tickLedger (pendingOpen held until the fold settles) and RECORDED — `ledgerSpreadStartCover()` returns the coverAmt at the instant the spread fired (the gate asserts ≥ 0.999; MUT: fire the spread immediately → 0 → false). The peek sheets hide while open (visible = fold < 0.5), reappear closed. **(3) REPORT-SIZED SHEETS:** the page backing goes PORTRAIT (112×145 paper, the 100-unit BOOKS_PANEL face centered with top/bottom margins) — "The P&L and Balance Sheet are on a sheet of paper- report sized"; aspect is realization freedom (I-48b). **(4) VG8n AMENDED (same 6 names):** `ledger-two-page-risen` gains PORTRAIT (each page bbox height > width; MUT: square backing → false); `ledger-flip-open-state` gains the CLOSED-form + sequencing conjuncts (peek sheets: 2 visible closed at genesis, 0 visible open, visible again after close; spread-start-cover ≥ 0.999). All other checks carried byte-same. Lane: ledger.ts (folder build + sequencing) · ledger-spread.ts (portrait backing + stand-up pose) · components/ledger.ts (gate surfaces) · gate/VG8n.gate.mjs + records. **OWNER 2026-08-03: "so close!" + the three-objects challenge → P-2c (I-86) supersedes the cosmetic-peek mechanism before it ever sealed.** | benign | local (the same four P-2 files; everything else untouched) |
| I-86 | P-2c — THE THREE-OBJECTS LAW (owner-ruled 2026-08-03, verbatim in RESOLUTION_RECORD; SUPERSEDES I-85's peek-sheet mechanism — the owner's challenge "three objects interacting or just an animation that looks like it?" was CORRECT: I-85 used cosmetic peek meshes + separately-built display pages). **(1) PERSISTENT SHEET OBJECTS:** the two report sheets are built ONCE, WITH the folder, as CHILDREN of the folder group — real page groups (portrait paper backing + the layoutFace fills, stamped from the GENESIS projection at build; RE-STAMPED in place from the live projection at each open — the face content updates, the OBJECT persists). No peek meshes; the sheets themselves peek. **(2) REPARENTING IS THE INTERACTION:** deploy = `scene.attach(sheet)` (world transform preserved — no jump), record the in-folder HOME pose, then lerp pos/quat(slerp)/scale to the display pose (flat-in-folder → standing at ±GAP, the anchor law unchanged); retract = lerp BACK to the recorded home pose, then `folder.attach(sheet)` — the same objects return INSIDE the folder. Phases: in-folder → deploying → displayed → returning → in-folder. Cover-close is GATED on sheets-home (the folder cannot shut through its own sheets). focusGroups['ledger-pnl'/'ledger-balance'] registered at deploy, deregistered at return-complete. **(3) THE FORM FIXES:** the fold hinge moves to the LEFT edge — the front flap SWINGS LEFT to unfold (pivot at local x=−75, rotation.y = −π·amt — up and over, never through the table); the front flap is NARROWER than the back (132 vs 150, offset left) so the sheets' RIGHT edges are VISIBLE when closed (the owner's second picture). **(4) VG8n 6 → 7 checks:** NEW `ledger-sheet-objects` (THE law's teeth: the two sheets' UUIDs are IDENTICAL closed → displayed → closed-again AND their parentage walks folder → scene → folder; MUT: rebuild pages at open [the I-85 mechanism] → new UUIDs → false — a faked animation CANNOT pass); `ledger-flip-open-state` amended (peek conjuncts → form conjuncts: closed = sheets' right-edge peek past the shorter front flap > 5u + parent 'folder'; open = the front flap lies LEFT of the back [centre-x delta < −100] + the recorded stage-2 handoff ≥ 0.999; closed-again = parent 'folder' + fold shut). Carried: two-page-risen (+portrait) · renders · anchor-balance · anchor-switch · opaque. **INTERPRETATION (registered):** closed sheets legitimately SHOW their stamped content edges (real projection data, small — nothing invented; I-56d holds). Lane: the same four P-2 files + records. **OWNER 2026-08-03: "approved! … Animation looks very good. BTW, no botches." — the ledger family SEALS (P-2/P-2b/P-2c).** | benign | local (ledger.ts · ledger-spread.ts · components/ledger.ts · gate/VG8n.gate.mjs; everything else untouched) |
| I-87 | K7-P ROUND 1 (distinct reviewer, no builder context, static+unit scope — the sandbox has NO browser, so the Playwright battery + live browser mutants are an EXPLICIT owner-side obligation, not silently skipped): **RETURN, worst dim 6 — three real catches, ALL CLOSED same-session:** **D1 (blocking)** re-open during the sheets' return glide consumed the deploy into a no-op (folder open-but-empty; a FALSE spreadStartCover record written) → CLOSED: stage 2 now fires only when `sheetsHome()` — a re-open WAITS for the sheets to arrive, then deploys; the record writes only on a real deploy. **D2 (blocking)** a state-change rebuild while sheets were DEPLOYED orphaned both sheets in the scene (stale focusGroups + stale isOpen) → CLOSED: buildSheets PURGES previous sheets from any parent, drops their read anchors, leaves read mode if it was on one; ledgerBook resets isOpen/pendingAt. **D3 (blocking, record integrity)** a click during the die's readable HOLD tossed from the LANDING — falsifying I-83's "every toss starts from home" → CLOSED: rollDie/deadRoll/startRoll now require diePhase 'idle' (toss only from home; the row's wording is now TRUE in code). **Minors closed:** D5 die-home-genesis gained the INDEPENDENT `dieRegionCenter()` oracle (home ≈ the live region, not merely itself; I-83's "two independent sources" now real); D6 consumeClick skips non-Mesh (edge-line) hits; D9 anchorPage only when 'displayed'. **Carried as notes:** D7 rest-window poll flake direction-safe · D8 in-read other-page click is a silent no-op (consistent with I-84(5c), status message optional) · D10 readSelected unexercised surface + stampSheet doesn't dispose old textures (pre-existing bench pattern) — next-touch. **D4 (runnable battery) DISCHARGED by the builder session post-closure (Linux verify copy):** tsc -p bench exit 0 · check:size OK · check:tiers OK · vitest 279/279 · build.mjs clean · both gate modules import-load. Lane diff vs 249dc4b (--ignore-cr-at-eol): ONLY the 8 lane files + 3 record files + dist. **OUTSTANDING before the k7-pass tags:** the owner-side full battery + the named browser mutants (K7 re-verify protocol: reconstruct D1/D2 live, re-probe D3). | benign | records + the D1/D2/D3/D5/D6/D9 closure edits in the SAME lane files |
| I-88 | Q-1 — THE FULL DECK (owner-ruled 2026-08-03 playtest-2 clause 1: "instantiate one of every card in the game to that deck so I can see how high that stacks, and explore the discard pile"; scope CONFIRMED by the owner's structured choice: FULL v1 INVENTORY as content-tier data). **(1) WHERE:** BOTY_PACK6 ONLY — the 6-up variant is the 3D bench's sandbox (ARCHITECTURE: additive edits only); the CERTIFIED slice (BOTY_PACK/botyGenesis) is byte-untouched; BOTY6_REF hash bumps boty-6up-1 → boty-6up-2 (content changed, on the record). **(2) THE SET — 33 new card entries in NEW `packs/boty/src/cards-q1.ts`, one per v1 family from the BOTY inventory (§2/§3/§5) + the ODG-3 catalog:** VNT×5 (project·civic·routed·incident·expansion; the 6th, job, exists as job-posting) · RTM×3 (subcontract·commission·referral) · IWN×8 (threat·damages·settle·poach·mayor·referral-window·routing-window·estate; court exists as court-writ) · SVC modifiers×5 (insurance·marketing·accountant·training·security) · GBL globals×3 (boom·recession·union; levy exists as town-levy) · economy/crew/jobs staples×9 (collections·demand-roll·credit-line·factoring·crew-review·crew-hire·toolbox·defect·theft). **(3) THE EFX LAW HELD:** content adds NO law — every fx ⊆ the certified seven-verb EFX v1.1.1; exactly THREE cards carry effects where the verb is TRUE to the v1 meaning (rtm-commission = pay 1 · eqp-toolbox = capitalize 1 · iwn-settle = a gated open_window choice); the other 30 are fx-less INERT EXHIBITS until their docketed ExtensionContract cycles (spawn_venture et al.) — R-2 keeps refusing unknown fx, nothing invented. **(4) DECK ORDER:** the 33 append UNDER moe's existing 3 (draw[0]=top preserved — 'job-posting' still the first draw; VG8j's draw semantics identical); moe's PACK6 deck 3 → 36; genesis6 mirrors. **(5) VG8j AMENDED (the committed-pack oracle updated, kill-first kept):** stacks-count-true wants **36** (the new committed genesis — a wrong pack edit still fails by name); the PHYSICAL-height law re-anchored to the stackInfo sample (top-5): the sampled cards are strictly stacked ≥0.8 world units per card (the old formula spanned the whole pile via a 3-card sample — valid only when count ≤ 5); the draw check goes RELATIVE (deck N → N−1) under the absolute genesis anchor. **INTERPRETATION (registered):** "that deck" = moe's (the viewing seat's) deck — the one the owner plays; the discard becomes explorable AS cards resolve (runtime, the engine's own discard law). Lane: packs/boty/src/cards-q1.ts NEW + pack.ts (PACK6 block only) + gate/VG8j.gate.mjs + records — a PACKS EXCURSION under ARCHITECTURE's rule, named here. | benign | local (packs/boty/src/cards-q1.ts NEW · pack.ts PACK6-only · VG8j.gate.mjs · records; certified BOTY_PACK + engine/presentation + all other bench code untouched) |
| I-89 | T-1 — THE V2 TABLE ARRANGEMENT (owner-ruled 2026-08-03 with the v1-board screenshots, verbatim in RESOLUTION_RECORD; realizes the "global card row" clause of playtest-2 msg 4 + the A16 pile stacks' table presence). **(1) THE SHARED-LAW HAZARD, seen and dodged:** TOWN_TABLE is consumed by the CERTIFIED SVG bench (game.ts frozen; visual-pins hash its innerHTML; V-9 renders through it) — editing TOWN_OVERLAY would break the pins and the frozen vector. So the v2 arrangement is a NEW CHILD: `TOWN_V2_OVERLAY` / `TOWN_TABLE_V2` (`boty:town-table-v2`), extended from the SAME TABLE_PARENT through the SAME declared overlay door (override/add — I-50; adds: standings·log·art-banner·tradespeople-pile·equipment-pile; overrides: global-play·deck·discard·dice·windows), consumed by the 3D BENCH ONLY (components/table.ts + stacks.ts imports swap to V2); the certified TOWN_TABLE child + every SVG consumer byte-untouched. **(2) THE ARRANGEMENT (the owner's spec as def data, 100-unit space):** season art-banner top-left (2,2,26×22) · GLOBAL CARDS IN PLAY section to its RIGHT (global-play 30,2,68×22) · player-summary standings UNDER the season (2,28,26×32) · table log under that (2,64,26×32) · the four piles in a row — deck (30,28) · discard (46,28) · tradespeople-pile (62,28) · equipment-pile (78,28), each 12×24 · the dice spot (32,58,16×16) · windows (56,56,36×24). The DICE HOME moves with its region automatically (P-1 derives it live — no code change). **(3) THE TWO NEW PILES are STAGED EXHIBITS** (cardStack, 6 face-down cards each, pure theater): the engine slice has no tradesperson/equipment decks yet — counts bind to state when the A16 engine decks land (the I-82f engine-lane flag; presented-first-verbs-later is the owner's confirmed staging). Nothing false: they assert no game state, exactly like the die exhibit. **(4) The global-play section renders labeled and EMPTY-honest** ('routing lands at Q-2') — card routing into it is Q-2's work. **(5) GATES:** VG8a's region law + VG8h's anchors DERIVE from the def (expectedFromDefs) — the 10-region v2 def auto-tracks; NEW kill-first check `VG8j/v2-table-arrangement`: the def IS the owner's spec (season top-left; global-play RIGHT of it, top band; standings under season; the four piles in their row; the two pile STACKS exist as 6-card exhibits at their regions) — a def regression or a missing pile fails by name. Lane: packs/boty/src/layouts.ts + index.ts (ADDITIVE child export) · packs/boty/tests/boty-layouts.test.ts (the GBC-58 construction fixture's expected child list gains the v2 id — the fixture exists to make additions DELIBERATE, and this is one) · components/table.ts · stacks.ts (def-id read) · gate/VG8j.gate.mjs + records — the packs excursion named per ARCHITECTURE. VERIFIED: tsc clean · size/tiers OK · vitest 279/279 (GBC-58 re-pinned) · build clean · pins safe BY CONSTRUCTION (TOWN_TABLE data byte-identical; only new exports added). | benign | local (those files; TOWN_OVERLAY/TOWN_TABLE + game.ts/game.html + pins + engine/presentation untouched) |
| I-90 | Q-2 — THE DRAW FLOW REWORK (owner-ruled 2026-08-03 playtest-2 clauses 2–3, verbatim in RESOLUTION_RECORD: the top card "is flipped over and then we see it in the onion layer. I don't want an animation of the card rising to the onion layer it just displays the card"; the flipped card triggers a sequence → GLOBAL play · LOCAL play · or DISCARD after effects resolve). **SUPERSEDES the A2/I-67c FLIGHT theater on the record** (deck→camera flight retired; the flip/HK-11/truth-wins/onion laws all CARRY). **(1) THE FLIP AT THE PILE:** doDraw still submits through the same doors (state advances at the verb; geometry-is-the-count rebuild unchanged); the theater card now FLIPS IN PLACE on top of the deck (a small lift + a half-turn about its own axis; the midpoint face-swap trick and the committed forced-mismatch drill carried VERBATIM); at flip end HK-11 fires (completeFlourish; truth wins R-20) and the onion OPENS DIRECTLY — no travel toward the camera, the display just appears. The flipped card RESTS face-up on the deck while the onion displays it (the physical card + its readable display). **(2) THE ROUTING SEQUENCE (the skeleton, bound to ENGINE TRUTH — the registered interpretation):** on onion close the SAME theater card TRAVELS (glide + arc, ~20 frames, real movement — the no-teleport law) to its DESTINATION. `routeDestFor()` carries the three branches the owner named — global-play section · the seat's local play · the discard — chosen from the PROJECTION: today EVERY slice card resolves to the DISCARD (the engine's own law; the Q-1 set is inert-or-instant), so the visible route is deck → discard-top; the global/local branches are WIRED and fire the moment the projection carries in-play cards (their engine cycles are the I-82f flagged work — the bench will NOT fake a card sitting in the global section that state says is in the discard; no false display). During reading+routing the discard's real top mesh is HIDDEN (counts stay true — stackInfo counts meshes regardless of visibility) and REVEALED when the traveler lands, so the card never doubles. **(3) PHASES:** idle → flipping → reading → routing → idle ('flying' retired); one-theater-at-a-time guard carried. **(4) VG8j AMENDED + 2 NEW kill-first checks:** the close-click assert now WAITS on drawPhase 'idle' (the route is part of the cycle — state, never clocks); NEW `draw-flip-not-fly` (mid-flip the theater card sits AT the deck — |cardPos−deckPos| < 40 world units, vs the old flight's hundreds toward the camera; MUT: restore the flight dest → false) · NEW `route-to-discard` (the close enters 'routing', the traveler ENDS at the discard top ±20 with dest 'discard' recorded, the hidden real top revealed; MUT: skip the route [teleport] → routing never observed → false). Lane: components/table.ts · gate/VG8j.gate.mjs + records. | benign | local (those two files; onion.ts/stacks.ts/surfaces.ts + engine/presentation/packs + everything else untouched) |
| I-91 | Q-2b — THE FLICK-TO-FLIP DRAW (owner-ruled 2026-08-03, verbatim in RESOLUTION_RECORD: grab the top card, flick to flip it, "let go and THEN the pop-up shows"; a weak flick "just stays face down on the pile"; a plain tap "moves slightly in response"). **SUPERSEDES Q-2's click-to-draw** (the flip/onion/route/HK-11 laws all carry; only the TRIGGER changes: drawing now REQUIRES the flick gesture). **(1) COMPONENT CONTRACT v2 — THE GRAB PROTOCOL (a REGISTERED SPINE EXCURSION, the lane law honored by naming it):** component.ts gains three OPTIONAL hooks — `onGrabStart(ctx, hit): boolean` (pointerdown raycast, scene mode only; the first claimer owns the drag and the camera pan/orbit is suppressed until release) · `onGrabMove(ctx, ev)` · `onGrabEnd(ctx, ev): boolean` (true = the gesture consumed the click — Phase 0/2 skipped). game3d.ts's three pointer dispatchers gain the claim/route/release branches (~15 lines; the ONLY spine edit; serves every future fidget object — the R-1 physics program rides this same protocol). **(2) THE GESTURE LAW (table lane):** pointerdown on the deck's top card (viewer's turn, theater idle) CLAIMS the grab — the top card lifts and TILTS with the drag (clamped BELOW the face-reveal angle: the card's face stays unknown while it is face-down — nothing revealed before the engine speaks); pointer velocity is sampled (the dice-shaker's window pattern). RELEASE: velocity ≥ FLICK_T (0.35 px/ms) → the DRAW SUBMITS (the same doors), the flip CONTINUES from the grabbed angle with momentum (midpoint face-swap ≥ the clamp, so the reveal is always post-release and post-submit), HK-11 at flip end, the onion displays — the owner's "let go and THEN the pop-up shows". Velocity < FLICK_T (a weak flick or a tap) → NO SUBMIT: the card settles back FACE DOWN onto the pile (a nudge wiggle for the tap) — rowHash + moveCount INVARIANT, pure theater. A NOT-your-turn deck click keeps the fidget (the grab is never claimed). Phases: idle → grabbing → (flipping → reading → routing | settling) → idle. **(3) SIZE-GATE EXTRACTION:** the whole draw-theater cluster (grab/flick/flip/route + its oracles) moves from components/table.ts to NEW `table-draw.ts`; table.ts keeps build/fidget/gate delegation. **(4) VG8j REWORKED:** the draw drive becomes a REAL DRAG (mouse down → fast moves → up); NEW `draw-weak-flick` (a slow 6-px drag → gestureVerdict 'weak', deck count + rowHash + moveCount invariant, no onion; MUT: threshold 0 → every touch draws → false) · `draw-flip-not-fly` + `route-to-discard` + the reading/close asserts CARRY; the old double-CLICK exactly-one-draw premise updates to a second DRAG mid-flip (one theater at a time, still moves +1). Timing note: the weak gesture's slow input SHAPE uses real waits (input definition), assertions still WAIT ON STATE (I-60f). **INTERPRETATION (registered):** the flick threshold value + nudge/tilt magnitudes are builder-proposed; the owner seals feel at playtest. Q-2c (slot routing partition) + Q-6 (discard manipulation + the drawn-this-turn onion) FOLLOW as their own rows. Lane: component.ts + game3d.ts (the registered contract-v2 excursion) · components/table.ts · table-draw.ts NEW · gate/VG8j.gate.mjs + records. **OWNER 2026-08-03: "good" — Q-2/Q-2b SEAL (commit 6d1ea10).** | benign | local (those files; engine/presentation/packs + onion/stacks/surfaces + all other components untouched) |
| I-92 | Q-2c — SLOT ROUTING AS A DERIVED VIEW (owner-ruled 2026-08-03: after the onion closes the card "finishes flipping into the discard pile face-up now, or it's directed to the first open global slot on the left, or first session slot under the active player's seat"). **THE DERIVED-PARTITION LAW (the registered interpretation, resolving Q-2/I-90's engine-truth tension):** the bench does NOT invent in-play state — it PARTITIONS the projection's ownDiscard by CARD FAMILY (content data, NEW `CARD_FAMILY` map in cards-q1.ts: town-levy + gbl-* → GLOBAL · svc-* → SESSION · all else → the pile). Every discard card renders EXACTLY ONCE: globals as face-up cards LEFT-FILLED (discard order) into the GLOBAL CARDS IN PLAY section's slots · session cards LEFT-FILLED into a session row IN FRONT OF THE ACTIVE seat's board · the rest as the discard pile. Derived-never-stored (the reports precedent): pile + global + session ≡ ownDiscard, gate-asserted; when real in-play engine state lands (I-82f) the partition SOURCE switches, the rendering law stays. **(2) ROUTING:** table-draw's routeDestFor(cardId) now consults CARD_FAMILY; the destination MESH (the card's own rendered slot/pile position, which exists from the at-submit rebuild) is hidden during reading and is the route TARGET — the traveler lands exactly where the derived view puts the card, then reveals it (one card, never two, any destination). **(3) SESSION ROW GEOMETRY (Q-3 will refine):** a module-managed scene group (purged each rebuild — the K7-P D2 pattern), slots across the active seat board's width, on the ground in front of it (toward the table); placement feel = owner seals at playtest. **(4) VG8j +1 kill-first check `slot-partition-law`:** the family DATA pinned (gbl-boom→global · svc-marketing→session · job-posting→discard; MUT: map regression → false) + partitionView sums ≡ ownDiscard length at the drawn-one state (pile 1 · global 0 · session 0). LIVE global/session routing is playtest-sealed at depth (the gate would need 30+ flick cycles); a drill-state re-kill TRIGGER carries (the I-76 precedent) — when a drill can seed a discard containing gbl-*/svc-* cards, drive the route live and kill the left-fill. Lane: cards-q1.ts + index.ts (content data) · table-draw.ts · components/table.ts · gate/VG8j.gate.mjs + records. **OWNER 2026-08-03: "good" — Q-2c SEALS (commit 2987363).** | benign | local (those files; engine/presentation + certified pack/layout law untouched) |
| I-93 | Q-3 — THE SEAT PLAY AREA (owner-ruled playtest-2 clause 4 + the v1-board split ruling: tradespeople IN FRONT of the seat board, attached equipment underneath-but-visible, cards grabbed/thrown RESET to anchor, ledger at the LEFT edge — the I-84(5a) deferral now due). **(1) PRESENTATION EXCURSION (LAW SURFACE, named per ARCHITECTURE):** SeatView gains PUBLIC `crew: {id, outfit}[]` — crew is public state (no redaction dimension: every player sees every shop's staff, the v1 board's own truth) and the tradespeople row cannot render without it (R-19: render reads ONLY through the projector). ADDITIVE: projector maps state.crew ?? []; no existing field or test pinned. The THIRD presentation touch of the 3D program (I-65a precedent). **(2) THE COMPONENT — NEW `components/seat-play.ts` + ONE registry line (the drop-in law):** per seat, its CREW as mini standing cards in a row IN FRONT of its board (COUNT-TRUE from view.crew by outfit); the VIEWER'S seat assets (view.seats[viewer].assets — real projection data) as a compact face-up group at the row's RIGHT end. **REGISTERED INTERPRETATION:** equipment ATTACHMENT state does not exist in the slice — assets render UNATTACHED beside the crew row until the attach/detach verbs land (I-82f); visual-only attachment would be a false display (the Q-2 discipline). Owner may re-rule placement. **(3) GRAB + RESET (the contract-v2 protocol paying off):** any seat-play card grabs — drags freely on the ground plane — and on release GLIDES BACK to its anchor ("they will reset to the anchor place they're supposed to sit"); rowHash/moveCount invariant (pure theater); attach-on-drop waits for the verbs. **(4) LEDGER LEFT EDGE:** the folder derives its position from the LIVE seat-0 board bbox (left of the board's min-x, in front) — the K-D derived-not-magic pattern; I-84(5a) discharged. **(5) REGISTRY ORDER:** [die, table, seats, seat-play, ledger, box] — seats must build BEFORE seat-play/ledger (their geometry derives from board bboxes); behavior-neutral for dispatch (disjoint tag domains; the K-A D1 build-reorder precedent). **(6) NEW GATE `gate/VG8p.gate.mjs` + one registry line, kill-first:** `crew-rows-true` (per seat: rendered crew cards ≡ view.crew count by outfit — 6×1 at genesis — positioned between board and table; MUT: drop a row → false) · `seatplay-grab-reset` (a REAL 100-px drag on a crew card moves it, release → waits on the reset STATE → back at anchor Δ<5, state invariant; MUT: drop the reset glide → false) · `ledger-left-edge` (folder centre-x < the seat-0 board's min-x; MUT: the old magic position → false) · `assets-count-true` (rendered ≡ view assets; genesis 0 — the count law binds now, content arrives when new-van resolves; the zero-state trigger carried, I-76 precedent). Lane: packages/presentation/src/projector.ts (the NAMED excursion) · components/seat-play.ts NEW · components/registry.ts (one line + the order note) · components/ledger.ts (derived position) · gate/VG8p.gate.mjs NEW + gate/registry.mjs (one line) + records. **Committed 01e0ebe on the owner's "go".** | benign | local (those files; engine/packs + the certified SVG bench + pins untouched — the projector change is additive and the SVG bench reads no crew field) |
| I-94 | Q-6 — THE LIVE DISCARD PILE (owner-ruled playtest-2 clause 5 + the flick message, verbatim in RESOLUTION_RECORD: discard cards "are real cards in play in the discard pile face up. They are moved via 3d animation around. if it's click it's the 3 step animation- if grabbed, they can be tossed or dragged. Once released, after a couple seconds they return to their right spot in the order of the discard pile" · "same mechanics can open up the 'drawn this turn' onion, or just move the cards around (animate them moving, not this cheap image thing)"). **(1) THE INTERACTION GRAMMAR (mirrors the deck's, contract v2):** CLICK on the pile = the 3-step fidget cycle, now ANIMATED — the cards TWEEN to the next state's poses instead of snapping via rebuild (the owner's "cheap image thing" complaint closed): the next state's stack builds INVISIBLE, its per-card poses become tween targets, the old meshes glide there (~15 frames), then the groups swap at IDENTICAL poses (registered note: the pile is state-rendered and rebuilt on every state change — the tween is the transition theater; the pose-identical swap is invisible). GRAB a pile card (any turn — pure theater): `scene.attach` (the three-objects trick — world transform preserved), drag on the ground plane, TOSS free; on release below the flick threshold → LOOSE for ~2 s (a frame-counted hold, surfaced as STATE — gates wait on state, I-60f) → then GLIDES BACK to its exact slot IN PILE ORDER (`group.localToWorld(homeLocal)` recomputed live; `group.attach` restores the local pose exactly at arrival). FLICK a pile card (fast release) → it returns home AND the onion OPENS READING that card — "the same mechanics" as the deck; the card id read from its own stamp (renderedLines), titled `drawn · discard #k of N`. The FULL drawn-this-turn browse (v1 column 3) stays DEFERRED per the owner's "let's work on that later". **(2) REBUILD SAFETY (K7-P D2 pattern):** the table build resets any live discard gesture (a scene-attached card is purged; the fresh stack renders truth). **(3) VG8j +3 kill-first checks:** `discard-toss-return` (a REAL 120-px drag + slow release → loose observed → returns to its slot Δ<3, rowHash/moves invariant; MUT: drop the return → false) · `discard-flick-reads` (a REAL fast flick → the onion opens on THAT card → close → idle; MUT: threshold 0 or no-onion → false) · `discard-fidget-animates` (a pile click → a TRANSITIONING state is observed over >1 frame then settles at the advanced fidget state; MUT: instant-apply → never observed → false). Lane: NEW `discard-play.ts` · stacks.ts (an exported pose helper if needed) · components/table.ts (wiring) · gate/VG8j.gate.mjs + records. **OWNER PLAYTEST 2026-08-03: "other animations and the pop-up are perfect" BUT the multi-card flow fails — "if I drag some cards out of the way and then flick a card I couldn't reach before, it should pop open." ROOT CAUSE: a single-gesture lock (one `gest` — while a tossed card sat in its 2-s hold every other pile card was DEAD to input) + a lifted card lost its pile tag (scene-parented → no region in its ancestor chain → un-regrabbable). → Q-6b (I-95), same lane.** | benign | local (those files; engine/presentation/packs untouched) |
| I-95 | Q-6b — THE GESTURE POOL (the owner's multi-card ruling, closing the Q-6 playtest miss). **(1) ONE HELD, MANY LOOSE:** the discard gesture becomes a POOL — exactly one card HELD (one pointer) while any number sit LOOSE (each with its own couple-seconds hold) or RETURNING; grabbing/flicking any REACHABLE card works while others are out — "drag some cards out of the way and then flick a card I couldn't reach before" now pops its reading. **(2) RE-GRAB:** a lifted card carries a `discardLoose` marker (its stack region tag is gone once scene-parented), so a loose card can be picked up AGAIN — its ORIGINAL slot (homeLocal) rides the gesture, and the motionless-click fall-through applies only to FRESH grabs (clicking a loose card re-grabs it; it never fidgets the pile). **(3) REBUILD PURGE covers the whole pool.** **(4) VG8j +1 kill-first check `discard-multi-card` (the owner's exact scenario):** a second REAL flick-draw fills the pile to 2 (new-van routes in); drag the TOP card away slowly (it goes LOOSE); WHILE it is loose, grab + FLICK the card underneath → the reading board OPENS on it (job-posting) with one card still loose; close; wait the pool empty; the pile count intact. MUT: restore the single-gesture lock → the under-card flick is refused → no onion → false. Lane: discard-play.ts · components/table.ts (the re-grab tag branch) · gate/VG8j.gate.mjs + records. | benign | local (those three files) |

## Log

- **2026-07-25 — ledger opened at C4 anchor. I-1..I-3 registered from the C4 probe run; no builder code exists yet.**
- **2026-07-25 — F1 built (I-4..I-6). K7 round 1: RETURN — 3 blocking (D-1..D-3), 1 major (D-4), 3 minor (D-5..D-7). Falsified builder claims noted in the completion ledger. All seven closed by the builder; I-7, I-4′, I-5′ registered; suite 24/24; sent back for K7 re-verify (mutations A–E to be re-run by K7, not the builder).**
- **2026-07-25 — K7 round 2: PASS.** Mutations A–H re-run by K7 (incl. three NEW mutations F/G/H proving the fixes themselves falsifiable); both round-1 probes reconstructed live. Scores (worst dim): types 9 · M1 8 · M2 8 · M3 8 · M4 8 · core 8 — all ≥ 7, **teeth released; F2 may open**. M3 stays NOT-COMPLETE by rule (V-1/V-2 deferred to F2). Non-blocking observations registered below.
- **K7 obs-1 (registered, next-touch obligation on core):** injected guard returning `{legal:false}` without a refusal payload → submit returns `undefined` (outside SubmitResult). Unreachable with the real Guard; close with a defensive throw at next core touch.
- **K7 obs-2 (registered):** structuredClone imposes a cloneability contract on intent args — loud DataCloneError, consistent with the JSON-typed Intent.
- **K7 obs-3 (CLOSED 2026-07-25):** SP-1 executed as SUP-1 (governance/S3/, s3-anchor-v1.1); I-1 sanctioned. V-2's discharge now waits only on F2. New F7 obligation on record: packRef-mismatch at rebuild → divergence refusal.
- **2026-07-25 — EXTERNAL K7 AUDIT round 1 (independent session, no builder context; report at governance/audits/K7_AUDIT_REPORT-1.md): PASS.** Reproduced the internal round-2 verdict via its own mutation battery (A–H + 5 tier-gate forms) and found ONE gap internal K7 missed — **EA-1 (= its MUT-H): the non-finite hash guard was unfalsifiable** (deleting it left the suite green). Closures, all landed this round: EA-1 hash-integrity tests added (builder re-ran MUT-H: 2 named tests now fail on the mutant); EA-2 obs-1 CLOSED (refusing verdict without payload → HookViolation, with injection test); EA-3 stale 18/18 note annotated as superseded (append-only); EA-4 scaffold barrels ratified in the object model. Suite 28/28. External scores concur ≥ 7 all dims (M1 base-case 7 pre-closure — the EA-1 tests raise exactly that gap).
